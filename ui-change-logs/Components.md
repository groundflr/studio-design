2026-05-14 — Added components/App/AppRoleBadge/AppRoleBadge.vue — 6-role variant wrapping AppTag, 2-tier neutral palette per Problem 2.
2026-05-14 — Added components/App/AppStatusDot/AppStatusDot.vue — 6 status variants (active/pending/expired/cancelled/deactivated/removed) using existing semantic tokens (--success-*, --warn-*, --error-*, --surface-*). No new tokens added.
2026-05-14 — Added components/App/AppTabsWithCount/AppTabsWithCount.vue — horizontal tab strip with optional count chips; active state uses primary-600 border + primary-700 text; count chips swap primary-50/primary-700 (active) vs surface-100/surface-500 (inactive). No new tokens added.
2026-05-14 — Added components/App/AppModal/AppModal.vue — portal modal via Teleport; sizes sm/md/lg (400/480/640px); slots: default body, footer (conditional divider), header (override); close via × button, ESC key, or backdrop click; body scroll lock on open; closeOnBackdrop + closeOnEsc props for destructive-confirm suppression. No new tokens added.
2026-05-14 — Added components/App/AppFilterPopover/AppFilterPopover.vue — anchored filter popover with trigger button, active-count chip, ESC + click-outside close, Clear filters footer button; align prop controls left/right anchor; internal open state only. No new tokens added.
2026-05-14 — Added components/App/AppBulkActionBar/AppBulkActionBar.vue — sticky-bottom dark bulk-action bar; count + noun (auto-pluralised) label, default slot for action buttons, Clear emit; slide-up + fade Transition; v-if gates render at count > 0. No new tokens added.
2026-05-14 — Added components/App/AppAuditList/AppAuditList.vue — vertical audit timeline; dot + text + actor/timestamp per entry; default tone: primary-300 dot; warn tone: warn-500 dot; v-html on text (trusted data-layer markup only); first:border-t-0 divider pattern; AuditEntry interface exported from script block. No new tokens added.
2026-05-14 — Added components/App/AppPeekRail/AppPeekRail.vue — 325px fixed-width aside shell for the two-pane peek rail pattern. No new tokens added.
2026-05-14 — Added components/App/AppPeekEmptyState/AppPeekEmptyState.vue — helper card with title/subtitle/keyboard tip rows; default tips cover ⌘-click + ⇧-click. No new tokens added.
2026-05-14 — Added components/App/AppPeekDetail/AppPeekDetail.vue — populated detail panel with avatar/identity head, meta dl, workspace memberships, quick actions; emits close + action. Danger hover substitutes error-100 (error-50 absent). No new tokens added.
2026-06-23 — Added component-library/components/tv-avatar.js — <tv-avatar> initials/image avatar (size sm/md/lg = 28/36/64px), optional presence dot (status online/offline/active/pending/deactivated) with a surface-0 ring, deactivated grey tint; img falls back to initials onerror. Shadow DOM, token fallbacks. Registered in tv-components.js + registry.json + gallery preview. No new tokens.
2026-06-23 — Added component-library/components/tv-metric-tile.js — <tv-metric-tile> the ss2-tile extracted from system-admin-summary-v2 (icon + label, big figure + optional trend, one-line desc; compact variant for 3-up sub-tile rows; optional corner arrow). Whole tile is a button; emits tv-select{metric} to open a detail card. Inline SVG icons (users/building/layout-grid/shield/plus). Registered everywhere. No new tokens.
2026-06-23 — Added component-library/components/tv-settings-section.js — <tv-settings-section> per-section view↔edit settings card (title/subtitle, slot="view" + slot="edit", auto Cancel/Save footer in edit mode). Content stays in light DOM so prototype JS can read the inputs. Composes <tv-button>; emits tv-edit/tv-cancel/tv-save; methods enterEdit()/exitEdit(); editing attr reflected. Registered everywhere. No new tokens.
2026-06-23 — Added component-library/components/tv-peek-rail.js — <tv-peek-rail> 325px Users-tab peek rail, data-driven (set .data or a JSON data attr): avatar + identity head, role/status tv-status-tags, meta dl, quick-action list (icons, danger, dividers). Empty state by default (slot="empty" override). Composes tv-avatar + tv-status-tag; emits tv-close + tv-action{id}; methods show(data)/clear(). Registered everywhere. No new tokens.
2026-06-23 — Refactored tv-status-tag to a single core chip (feedback): fixed structure (padding/radius/left-icon-or-dot/label) driven by two axes — tone (primary/success/warn/info/error/neutral) × variant (fill | dot) — defined once in a TONES table. The semantic kinds (moderation/roles/member-states/publication/audit-events) are now thin mappings onto {tone,variant,icon,label} instead of independent colour blocks, so colour families are unified exactly as grouped: ai=admin (primary), adjusted=moderator=draft (warn), auto=owner=published=created (success), candidate=coming=promoted (info), flag=revoked (error). New core API: tone, variant, no-icon (suppress a kind's icon); existing kind/label/icon usages unchanged and backward-compatible (minor: auto/active green nudged 500→600, adjusted amber 500→600 to match their family). Gallery preview now shows the core tone×variant row + dot/icon-toggle, with kinds extrapolated below. registry.json attributes updated.
2026-06-23 — tv-field: control padding is now `padding:9px 11px !important` (+ margin:0) so a host page's `*{padding:0}` reset can't strip the input's inner padding — document rules otherwise override low-priority ::slotted() styles, which is why fields rendered with no inner padding.
2026-06-23 — tv-toggle: now animates. It was re-rendering its whole DOM on every `checked` change, so the new node started at the end state and the transition never played; now `checked` mutates the existing node (.on class + aria-checked) via _sync() instead of re-rendering, and the track/knob transitions use --dur-base/--ease-std (with a prefers-reduced-motion fast path). label/disabled still re-render.
2026-06-23 — Fix: the previous tv-field padding note put backticks (around an example selector) inside the render() template-literal comment, which prematurely closed the template string → "is not a function" thrown at runtime, leaving tv-field's shadow empty (no <slot>) so slotted inputs weren't rendered anywhere. node --check missed it (valid syntax as a tagged template). Reworded the comment without backticks; verified via headless Chrome that all components render non-empty shadows, the input is slotted, and there are no console errors.
