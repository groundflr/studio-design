# Dashboard — UI Change Log

One-line log of UI changes to the workspace dashboard prototype at `prototypes/dashboard/index.html`. Newest at the bottom.

---

- 2026-04-28 — Created `dashboard/index.html` rebuilding the screenshot at `app-screenshots/dashboard/dashboard.png` with design-system tokens from `colors_and_type.css` and Lucide icons via CDN; 240px sidebar (Studio wordmark + collapse button, "DG" workspace switcher, Summary nav with Dashboard active in primary-500, Simulations / Assessments / Sequences); centered Workspace Summary header with three-segment summary pill (7 simulations / 5 characters / 6 assessments); two-column card grid — Assets card with 2x2 tile grid (Simulations 13, Characters 3, Assessments 6, Tests 24, each with success/warn/info dot counts) and Activity card with 222 / 0 / 0 stat banner plus seven Most recent activity rows showing icon, title, time, and Abandoned/In-Progress/Pending/Graded status pills.
- 2026-04-28 — Fixed broken stylesheet/asset paths after the file was relocated from `dashboard/` to `prototypes/dashboard/` — updated `colors_and_type.css` link and the wordmark `<img>` `src` to use `../../` instead of `../` so they resolve from the new depth (was rendering as plain unstyled HTML before the fix).
- 2026-04-28 — Added a top-of-dashboard `action-card` for feedback awaiting author review: warn-toned message-square icon, "Action needed" eyebrow with alert-circle, "Feedback ready for your review" h2, summary line ("11 candidates across 3 tests"), and an inner list of three rows — each row shows a test type icon, test title, "X candidates" warn pill, and a "Review feedback →" button that deep-links to `../test-journey/?screen=test-summary` so the author lands directly on the relevant test summary; positioned above the Workspace Summary header so it's the first thing visible on resume.
- 2026-04-28 — Made the dashboard `action-card` expandable / collapsible: header now carries `role="button"`, `aria-expanded`, and `aria-controls`; a chevron-down toggle on the right rotates -90° when collapsed; clicking anywhere on the header (or pressing Enter/Space when focused) toggles `is-collapsed` on the card, which hides the per-test list and tightens the header's bottom margin. Defaults to collapsed so the alert reads as headline-only; expand reveals the three test rows and their Review buttons.
- 2026-04-28 — Bumped the Lucide CDN version on the dashboard from `lucide-static@0.475.0` (which 404'd, breaking every icon) to `@0.453.0` to match the test-journey prototype; icons across the dashboard (sidebar nav, asset tiles, activity rows, action card chevron, etc.) now render correctly.
- 2026-04-28 — Moved the `action-card` from above the Workspace Summary header to between the workspace summary pill and the Assets / Activity cards, so the workspace title hierarchy reads first and the actionable alert sits in the page body where the cards-of-interest live.
- 2026-04-28 — Added a global `a { color: inherit; text-decoration: none; }` reset to the dashboard prototype to strip the browser's default underline + blue colour from sidebar nav items, action-row links, and any other `<a>` clickable text; matching the test-journey prototype's no-underline convention.
- 2026-05-13 — Rebuilt the seven `app-screenshots/system-and-workspace-settings/` flows into `prototypes/dashboard/index.html` as additional screens reachable via the existing sidebar shell, with no UX changes from the screenshots: (1) workspace switcher in the sidebar now opens a four-item dropdown — Workspace settings (settings icon), Switch workspace (arrow-left-right + chevron-right), System Admin (settings icon), Sign out (log-out icon); (2) new `workspace-settings` screen with "Workspace Settings" h1 and Settings | Users | Integrations tabs underlined in `var(--primary-600)` — Settings tab renders the four form rows (Workspace Title text input, Workspace Profile Image card with trash + rotate-cw actions and circular preview, Submission Test Grading Model select with "Select a grading LLM" placeholder, Grading variability detection toggle ON with full descriptive paragraph) plus a primary Save button right-aligned, Users tab renders an Add users button + single-column "Full name" sortable table of nine workspace members each with a `more-horizontal` row menu, Integrations tab as placeholder; (3) new System Admin sidebar variant — Studio brand, `< System Admin` back chevron, and five nav items (Users, Workspaces, Simulations, Characters, Voices) — that swaps in when navigating to any system-admin screen and swaps back to the workspace sidebar via the back link; (4) `system-admin-users` screen with "All Users" large title, right-aligned search input + Invite User button, and a five-column sortable table (FIRST NAME active sort highlighted in `var(--primary-50)` header, LAST NAME, EMAIL, ROLE [Super/Standard], ID monospace) populated with 13 sample users matching the screenshot; (5) `system-admin-workspaces` screen with "All Workspaces" title, search input, and a four-column sortable table (TITLE active sort, CREATED BY, CREATED AT, ID) populated with 13 sample workspaces; (6) "Add users to workspace" modal triggered by the Add users button — title, close X, "Enter one or more email addresses to invite to the workspace." copy, email input, Cancel + Add buttons; (7) "Invite User" modal triggered by the Invite User button — title, close X, "Enter the email address of the user you want to invite." copy, email input, Cancel + Invite buttons. Both modals use a dark scrim overlay closing on backdrop click or Escape. All new surfaces use `colors_and_type.css` tokens only (no raw hex), Lucide icons (no emoji), sentence-case copy throughout, and a sticky bottom `.devbar` exposes direct navigation between screens + both modals + a "Toggle workspace menu" control per the project's devbar-only demo controls convention.
- 2026-05-13 — Rebuilt `product-assets/user-roles-and-permissions/ux-scratchpad-dave/user-profile.html` as a `user-profile` screen inside `prototypes/dashboard/index.html` using design-system tokens + Lucide icons (no raw hex, no ASCII glyphs, sentence-case copy). Both user-list tables now have `data-user-list` attributes (`workspace` and `system`) and their rows are `.is-clickable`; a delegated click handler reads the row's cells, calls `navigateToUserProfile({ name, email, role })`, captures whichever list screen the click came from into `listContext` (so the breadcrumb back-link and the active sidebar variant — workspace or system-admin — both follow the user back), and switches to the user-profile screen with the contextual sidebar preserved. The profile screen itself reproduces Dave's layout in the project DS: breadcrumb `Users / All users › <name>`; profile card with 64px primary-100/primary-700 avatar (initials derived from name), name + dynamic role chip, email, "<job title> · <department>" line, and a meta row with status dot + Active / Member since / Last active; right-aligned "Edit profile" button + bordered `more-horizontal` toggle that opens a `.profile-menu` dropdown (Change role / Deactivate / Remove from org, the last in `var(--error-600)`); 3-column profile-bio strip (Department, Job title, Bio) with `var(--surface-100)` dividers. Two-column body — left card "Workspace access" (count chip, 5-col table Workspace / Role / Status / Joined / actions with a "Current" pill on the active row, plus a "+ Add to workspace" footer button), right column three stacked cards (Account details meta-list with Org role / Member since / Invited by / Last active / Sign-in method; Recent activity audit list with 5 entries; Danger zone card with `var(--error-100)` border and two danger-button rows for Deactivate / Delete account). Role chip variants added to the DS layer: `.role-chip.is-org-owner` (success), `.is-org-admin` (primary-200/primary-800), `.is-admin` (primary-100/primary-700), `.is-standard` (info), `.is-moderator` (warn), `.is-viewer` (surface). New modals: "Add to workspace" (typeahead workspace search with focused state, clearable input, options dropdown with name + desc, role select [Workspace admin / Standard user / Moderator / Viewer], confirm button disabled until a workspace is selected) and "Edit profile" (wider 540px modal — avatar upload card, first/last name two-col grid, readonly email with SSO hint, job title/department two-col grid, optional bio textarea). `populateProfile()` updates name + initials + email + role chip class/label everywhere via `[data-profile-*]` hooks when arriving from a row; role mapping translates the System Admin table's "Super" → "Org admin" and "Standard" → "Standard user" so chips are consistent. Devbar gained four entries: User profile (direct screen), Modal · Edit profile, Modal · Add to ws.
- 2026-05-14 — Section 1 (Plumbing): Added Organisation sidebar variant (`data-sidebar-variant="organisation"` — brand + "Organisation" back button + Summary / Workspaces / Users nav items with `data-org-tab` attributes); added "Organisation" entry to the workspace dropdown menu between "Switch workspace" and "System Admin" using `icon-building-2`; added `<section class="screen" data-screen-name="organisation">` with page header ("Organisation" h1, Acme Org subtitle) and a 3-tab strip (Summary / Workspaces / Users) with token-based active-tab underline and count-chip slots wired to three `.org-tab-panel` placeholders; registered `'organisation': 'organisation'` in `sidebarFor`; added `activateOrgTab()` helper and `navTo()` opts for org-tab routing with URL `?tab=` deep-linking; added Organisation devbar button. All new CSS prefixed `org-`, no raw hex.
- 2026-05-14 — Section 2 (Summary tab): Filled Organisation > Summary tab with 4 metric cards (6 Workspaces / 24 Active members / 3 Pending invites / 3 Org admins) + Organisation leaders card (3 leader rows with admin-tier role badges, avatar initials, View buttons) + Recent admin activity feed (4 activity rows with dot indicators and exact mock copy/timestamps). New CSS classes: `.org-summary-metrics`, `.org-summary-metric-card`, `.org-summary-metric-number`, `.org-summary-metric-label`, `.org-summary-metric-sublabel`, `.org-summary-row2`, `.org-summary-card`, `.org-summary-leaders-header`, `.org-summary-activity-header`, `.org-summary-card-title`, `.org-summary-card-meta`, `.org-summary-card-action`, `.org-summary-leaders-list`, `.org-summary-leaders-row`, `.org-summary-avatar`, `.org-summary-leader-info`, `.org-summary-leader-name-row`, `.org-summary-leader-email`, `.org-summary-role-badge`, `.org-summary-view-link`, `.org-summary-activity-list`, `.org-summary-activity-row`, `.org-summary-activity-dot`, `.org-summary-activity-body`, `.org-summary-activity-text`, `.org-summary-activity-time`. No new tokens; `--font-display` absent in prototype so `var(--font-sans)` used throughout per spec fallback.
- 2026-05-14 — Section 3 (Workspaces tab): Filled Organisation > Workspaces tab with a toolbar (`.search-input` search field left, sort dropdown + "+ New workspace" primary CTA right) and a consolidated card (`.org-workspaces-card`) containing 4 workspace rows — L&D Hub, Sales Training, Graduate Hiring, Compliance — each showing name, members/admins/sims/last-active meta line, and Open / Manage admins / overflow-menu actions; row hover `var(--surface-50)`; no raw hex. Also corrected the Section 2 Workspaces metric card from `6` to `4` to match the 4 rows now represented. New CSS classes: `.org-workspaces-toolbar`, `.org-workspaces-toolbar-end`, `.org-workspaces-sort`, `.org-workspaces-card`, `.org-workspaces-list`, `.ws-row`, `.ws-row-main`, `.ws-row-name`, `.ws-row-meta`, `.ws-row-sep`, `.ws-row-actions`, `.ws-row-link-btn`, `.ws-row-overflow-btn`.
- 2026-05-14 — Section 4 (Users tab): Filled `#org-panel-users` with full Users tab — action row (Members/Invites/Deactivated sub-tabs with count chips 24/3/2; search input; filter popover with Role + Workspace selects + Clear footer; "+ Invite member" primary CTA with `data-org-action="open-invite-member"`); two-pane body with flex main column + 325px sticky peek rail; Members sub-panel with 10-row users table (avatar initials, name, email, org-role badge, per-workspace role-lines, status dot, last-active); Invites sub-panel with 3-row table (email, role, workspace, sent, state, actions); Deactivated sub-panel with 2-row table; peek rail empty state (icon-users, title, subtitle, kbd tips); peek rail detail state hidden by default (Sarah Chen placeholder — avatar, name, email, Org Admin badge, meta dl, 3 workspace entries, 5 quick-action buttons); bulk action bar fixed bottom hidden initially (count label, Change role / Add to workspace / Deactivate / Clear). Vanilla JS IIFE scoped to `#org-panel-users`: sub-tab switching, filter popover open/close (ESC + click-outside), row click populates peek rail (Cmd+click logs "open full profile"), checkbox set tracks selected IDs and shows/hides bulk bar with count, master checkbox selects/deselects all members rows, Clear resets selection. New CSS classes (40+): `.users-action-row`, `.users-sub-tabs`, `.users-sub-tab-chip`, `.users-action-end`, `.users-filter-trigger`, `.users-filter-count`, `.users-filter-wrap`, `.users-filter-popover`, `.users-filter-section-label`, `.users-filter-select`, `.users-filter-footer`, `.users-filter-clear`, `.users-subpanel`, `.users-body`, `.users-main-col`, `.users-peek-rail`, `.users-table-card`, `.users-table`, `.users-name-cell`, `.users-avatar`, `.users-name-text`, `.users-name-primary`, `.users-name-email`, `.role-badge`, `.role-line`, `.role-line-ws`, `.users-status`, `.users-status-dot`, `.users-time`, `.users-table-footer`, `.users-table-footer-count`, `.users-invite-actions`, `.users-link-btn`, `.users-peek-empty`, `.users-peek-empty-icon`, `.users-peek-empty-title`, `.users-peek-empty-sub`, `.users-peek-tips`, `.users-peek-tip`, `.users-kbd`, `.users-peek-detail`, `.users-peek-detail-head`, `.users-peek-avatar`, `.users-peek-head-info`, `.users-peek-name`, `.users-peek-email`, `.users-peek-badges`, `.users-peek-close`, `.users-peek-meta`, `.users-peek-section`, `.users-peek-overline`, `.users-peek-ws-list`, `.users-peek-ws-item`, `.users-peek-actions`, `.users-peek-action-btn`, `.users-peek-action-divider`, `.users-bulk-bar`, `.users-bulk-label`, `.users-bulk-divider`, `.users-bulk-btn`, `.users-bulk-clear`. Icon confirmations: `icon-filter` confirmed present in Lucide 0.453.0. Alpha overlays on bulk bar (`rgba(255,255,255,0.1)` hover, `rgba(220,60,40,0.22)` danger hover) documented as intentional token exceptions.
- 2026-05-14 — Section 5 (Per-row peek rail data + organisation user profile screen): Part A — replaced hardcoded Sarah Chen peek rail content with a 10-entry `ORG_USERS` data structure (matching the Members table u1–u10) inside the `#org-panel-users` IIFE; added stable `data-peek-field="..."` attributes to the peek rail detail HTML; rewrote `showPeekDetail()` to call `renderPeekDetail(user)` which sets avatar initials, name, email, org-role badge (conditionally shown), status dot + label, all meta dl fields, and workspace list dynamically per clicked row; Cmd+click now opens the org user profile screen and calls `renderUserProfile()`. Part B — added `data-screen-name="organisation-user-profile"` screen scoped to the organisation sidebar (sidebarFor mapping + navTo branch that highlights "Users" nav item); two-column grid (2fr / 280px+ 1fr, collapses at 900px): left main card with 4 sections (Identity with 64px avatar, full name, org-role badge, status dot, email, org-line, Edit profile + Add to workspace + 32px overflow icon-button with 4-item dropdown; Bio with department/job title/bio dl; Workspace memberships table with Role/Added/Added by columns and Change role + Remove link-buttons per row; Activity list with coloured dot entries); right side card with At a glance dl (5 rows) and Danger zone (Deactivate + Delete rows with `.btn-sm` danger buttons). `renderUserProfile(user)` populates all `data-profile-field="..."` hooks scoped to the profile screen. "Open full profile" quick action in peek rail triggers profile screen. Breadcrumb "Users" link navigates back to Organisation screen with Users tab active. CSS prefixed `org-profile-` throughout to avoid collision with the existing `user-profile` screen's `.profile-*` classes.
- 2026-05-14 — Section 6 (Three org modals): Added shared `.org-modal-*` CSS shell (backdrop blur-2px, 12px-radius dialog, header/body/footer structure, sm/md/lg size modifiers, form-group/label/input/select/textarea/role-card/info-block sub-styles); built three new modals outside `.app`: (1) `#org-invite-member-modal` (md) — email input with Send disabled until email present, workspace select (L&D Hub / Sales Training / Graduate Hiring / Compliance), 4 role cards in 2-col grid (Workspace Admin / Standard User default-selected / Moderator / Viewer) with selected state via color-mix, optional personal-note textarea, Send logs payload + closes; (2) `#org-add-to-workspace-modal` (md) — title dynamically set to "Add [Name] to workspace" via `openOrgAddToWorkspace(name)`, workspace select (all 4 static, filter-to-unmembered is a follow-up), role select defaulting to Standard User, info block (spec §5 P4 note), Add logs payload; (3) `#org-edit-profile-modal` (md) — full name / job title / department / bio fields pre-filled via `openOrgEditProfile(user)`, Save logs payload; global `openOrgModal` / `closeOrgModal` / `closeAllOrgModals` helpers; ESC closes topmost open org-modal; backdrop click closes; delegated `data-org-modal-close` / `data-org-modal-open` handlers; `ORG_USERS` map exposed as `window._orgUsersMap`, `data-peeked-user-id` stamped on `#users-peek-detail` on row click; peek rail "Edit profile" (`data-action="open-org-edit-profile-peek"`) and "Add to workspace" (`data-action="open-org-add-to-ws-peek"`) wired to respective org modals; org-user-profile screen "Edit profile" (`data-action="open-org-edit-profile"`) and "Add to workspace" (`data-action="open-org-add-to-ws-profile"`) wired; org Users-tab invite CTA (`data-org-action="open-invite-member"`) wired; devbar gained 3 entries: Org · Invite member / Org · Add to ws / Org · Edit profile.
- 2026-05-14 — Section 7 (Workspace Members screen): Added "Members" nav item to workspace sidebar (icon-users, `data-nav="workspace-members"`, positioned after Sequences); added `'workspace-members': 'workspace'` to `sidebarFor`; added new `data-screen-name="workspace-members"` screen (dashboard-shell layout) with `workspace-members-header` (h1 "Members", subtitle "Manage who has access to this workspace."); action row reuses `.users-action-row`, `.org-tab`-based sub-tabs (Members 6 / Invites 2 / Deactivated 1), `.search-input`, scoped filter popover (`#ws-members-filter-popover` — Role + Status selects), "+ Invite member" CTA pre-filling `#invite-workspace` to `ld-hub` and opening `#org-invite-member-modal`; two-pane body reuses `.users-main-col` + `.users-peek-rail` with `ws-` prefixed IDs; Members sub-panel with 6 L&D Hub users (Tobias/Sarah/Marcus as Workspace Admin, Dyl as Standard, Iona as Viewer, Elena as Standard) each showing a single workspace-scoped role badge via `.workspace-members-role-cell` (not a multi-row stack); Invites sub-panel with 2 rows (melissa pending, daniel expired); Deactivated sub-panel with 1 row (Hugo Greer Standard); peek rail shows L&D Hub–scoped single role row, not all workspaces; scoped JS IIFE (`wmScreen` guard) handles sub-tab switching, filter popover, row click → `renderWorkspaceMembersPeek()`, Cmd+click → `renderUserProfile()` + `navTo('organisation-user-profile')`, checkbox + master checkbox + bulk bar (`#wm-bulk-bar`) with count label; devbar gained "Workspace Members" button. New CSS: `.workspace-members-header`, `#ws-members-filter-popover`, `.workspace-members-role-cell`.
- 2026-05-14 — Full 7-section composition pass complete: screens added — Organisation (Section 1), Summary tab (2), Workspaces tab (3), Users tab (4), Organisation user profile (5), Workspace Members (7); modals built — Invite member, Add to workspace, Edit profile (6); `prototypes/dashboard/index.html.section4-backup` can now be deleted.

- 2026-05-14 — Organisation > Users > Members: swapped workspace/role order in the "Workspaces & roles" cell (workspace name now first, role badge second) and switched the row from flex to a 2-column grid (120px workspace column + auto badge column, 10px gap) so workspace names left-align and role badges left-align within each user.
- 2026-05-14 — Organisation > Users: added padding-top: 20px to #org-panel-users so the Members/Invites/Deactivated sub-tabs sit further below the parent tab strip (total ~48px from the strip baseline).
- 2026-05-14 — Organisation > Users > Members: tightened role-badge chips inside .role-line (padding 2px 8px → 1px 6px, line-height 20px → 16px, justify-self: start) so chips hug their text. Scoped to .role-line; role badges elsewhere unchanged.
- 2026-05-18 — Problem 2 Role Chip (Sections A–D): Added `.tb-role-chip` (two-tier: `is-admin-tier` primary-50/primary-700, `is-neutral-tier` surface-100/surface-600) to all eight authenticated screen headers via `.tb-chip-slot` placeholders; single `#tb-role-popover` with role name, capability summary, cross-workspace role list, and "Learn more" link (href="#", blocked on Problem 1 dependency); popover dismisses on Escape + click-outside, ARIA wired (aria-haspopup, aria-expanded, role="dialog"); global `PERSONA_DATA` map covers six personas (Org Owner u1, Org Admin u2/u3, Workspace Admin u3, Standard u4, Moderator u5, Viewer u6); chip re-renders on every `setScreen` call; org/system-admin screens show persona's org role (or suppressed if none); workspace screens show operational workspace role (Org Owner/Admin appear as Workspace Admin per spec); devbar gained Persona group (6 buttons) + Workspace group (4 buttons), default persona u4 (Standard User, non-admin tier visible at first paint); existing `.role-chip` variants on profile page untouched.
- 2026-05-18 — Reverted the Problem 2 in-header role-chip work above: restored `prototypes/dashboard/index.html` from the pre-chip backup — removed `.tb-role-chip` styles, `.tb-chip-slot` placeholders from all 8 screens, `#tb-role-popover`, `PERSONA_DATA` / `ORG_SCREENS` / `renderAllChips` script, and the Persona + Workspace devbar groups. Decision: role identity will live only on the existing user-profile screen via the existing `.role-chip` variants; no top-bar / in-header chip needed.
- 2026-05-18 — P5 Step 2: Added dashboard welcome hero state triggered by `?welcome=1&role=<standard|moderator|viewer|empty>`. Hero is a full-width `<section aria-label="Welcome">` with primary-400 left accent rail, sparkles icon block, greeting h1 ("Welcome to David Guetta, Jane"), role copy line ("Joined as [Role]"), variant-specific orientation paragraph, and a two-button action row (primary CTA + ghost dismiss). Standard variant: hero above ws-header, cards remain visible, "Start exploring" CTA dismisses; Moderator/Viewer: full takeover — ws-header, action-card, and dashboard-grid hidden via `.dashboard-shell.is-takeover`; dismiss replaces hero with a placeholder panel (icon-clipboard-list/icon-eye, heading, body copy, "Contact Sarah Chen" mailto CTA); Empty: hero above cards, "Contact Sarah Chen" mailto CTA. Dismiss persisted to `sessionStorage` per role key (`traverse.welcomeDismissed.<role>`). Devbar gained a "Welcome state" group — Off / Standard / Moderator / Viewer / Empty buttons (active state highlights current) + "Reset dismiss" clears sessionStorage and re-renders. Backup saved at `index.html.pre-p5-welcome-backup`. All tokens from `colors_and_type.css`; icons from Lucide 0.453.0 (icon-sparkles, icon-clipboard-list, icon-eye confirmed present).
- 2026-05-18 — P5 Part 3: Extended the workspace dropdown trigger into an inline workspace switcher. The sidebar trigger now shows workspace name ("L&D Hub") and initials ("LH") instead of persona name. Popover gained a top workspace-list group (role="group", aria-label="Switch workspace") rendered from `SARAH_WORKSPACES` — three rows (L&D Hub / Sales Training / Graduate Hiring) each showing workspace name, member-count meta line derived from ORG_USERS, a per-workspace role badge, and a "Current" pill on the active row; active row has a primary-500 left rail + color-mix highlight and is non-clickable. Removed the redundant "Switch workspace" menu item. Clicking a non-active row updates `currentWorkspaceId`, swaps the sidebar identity (initials + name), re-renders the list, closes the popover, and shows a slide-in `.ws-switch-toast` for ~2.2 s. Devbar gained a "Reset workspace" button restoring the default L&D Hub state. Sarah's ORG_USERS entry (u2) updated: Graduate Hiring role changed from `viewer` to `moderator`, demonstrating three distinct roles across three workspaces. Note: dashboard content cards do not update on workspace switch — sidebar identity only. Backup at `index.html.pre-p5-part3-backup`.
- 2026-05-18 — P5 Part 3 (revision): Replaced inline workspace list in the sidebar dropdown with a right-flyout sub-drawer. "Switch workspace" restored as a menu item (icon-arrow-left-right + chevron-right trail); clicking it opens a `.ws-sub-drawer` panel (280px, `var(--card)` bg, 0.5px border, 10px radius, `var(--shadow-md)`) positioned `left: calc(100% + 6px)` from the dropdown — the main dropdown stays open. Drawer header: back chevron (`icon-chevron-left`) + uppercase overline label; body contains the three workspace rows (same data/styling as before). Active menu item gains color-mix highlight + `aria-expanded`. Keyboard: Right-arrow/Enter on menu item opens drawer, Left-arrow/Escape from inside drawer returns focus to menu item; first Escape closes drawer only, second closes dropdown. Clicking a workspace row closes drawer + dropdown and fires `switchWorkspace()` + toast. Click-outside the entire system closes both panels. All ARIA updated (`aria-haspopup="menu"`, `aria-expanded`, `role="menu"` on drawer). Backup at `index.html.pre-p5-part3-revision-backup`.
- 2026-05-18 — P5 Part 3 (hover revision): Switched the "Switch workspace" sub-drawer trigger from click to hover. mouseenter on the menu item opens the drawer after an 80ms hover-intent delay; mouseleave schedules a 200ms close that's cancelled if the pointer moves into the drawer itself (drawer also listens for mouseenter/mouseleave to keep the panel sticky when the user travels diagonally between menu item and drawer). Click on the menu item is now a no-op. Keyboard path unchanged: Enter / Right-arrow on the focused menu item still opens the drawer and moves focus into it.
- 2026-05-18 — P1 (Capability discoverability — Option A): Added `data-viewer-mode="other"|"self"` attribute to the `organisation-user-profile` screen to toggle between admin-viewing-another (other) and user-viewing-themselves (self). Self-view differences driven by CSS attribute selectors (`[data-other-only]` / `[data-self-only]`): breadcrumb collapses to "My profile" with no separator; "Add to workspace" header button suppressed; overflow menu swaps to Change password / Copy user ID / Sign out; a new "Your permissions in [Workspace]" section (section 2.5, `data-self-only`) appears between Bio and Workspace memberships, rendering 3–5 role-specific bullet points resolved from the current persona's (Sarah, u2) role in `currentWorkspaceId` plus an Org Admin addendum; workspace-membership row actions (Change role / Remove) and the "+ Add" section link are suppressed; Activity section title becomes "Your recent activity" with first-person copy; Danger zone replaces Deactivate + Delete with a single "Close my account" row that opens a typed-name confirmation modal (#close-account-modal). Entry paths: (1) new "My profile" item (icon-user) in the workspace dropdown above the divider sets viewer-mode to self and navigates to the profile; (2) devbar "Profile viewer mode" group (Other (Admin) / Self (Me)). Capability section re-renders when workspace is switched via the P5 Part 3 sub-drawer (MutationObserver on `#ws-switch-toast`). `window._navTo`, `window._renderUserProfile`, `window._openOrgModal`, `window._closeOrgModal`, and a `window._currentWorkspaceId` getter exposed for cross-IIFE access. Backup: `index.html.pre-p1-backup`. Deferred: disabled-with-tooltip pattern blocked on Clarification 4 (no enumeration from product team).
- 2026-05-18 — Added click-to-toggle collapsible sidebar across all three sidebar variants (workspace, system-admin, organisation): CSS adds `.is-collapsed` modifier and `.sidebar-collapsed` on `.app` (grid-template-columns transitions from `240px 1fr` to `64px 1fr` in 150ms cubic-bezier); hidden in collapsed mode: wordmark, `.sb-workspace` name + chevron, `.sb-eyebrow`, `.sb-item .label`, `.sa-back span`; icon column and avatar initials remain; active state (primary-500 background on `.sb-item.active`) preserved; `panel-left-close` → `panel-left-open` icon flip on all three `.js-sidebar-collapse` buttons with `aria-label` + `aria-pressed` sync; workspace dropdown anchored at `width: 220px` in collapsed mode so it still renders legibly; `title` attributes added to all nav items as tooltip labels; state persisted to `localStorage` under key `traverse.sidebarCollapsed.dashboard` and restored before first paint.
- 2026-05-18 — Fixed broken Lucide icon references that were rendering as empty `<i>` elements (visible as missing icons on profile-screen overflow buttons, action-card eyebrow, and 40+ table-row overflow menus): `icon-more-horizontal` → `icon-ellipsis` (44 replacements; renamed in Lucide; covers both `.row-menu-btn` and `.org-profile-icon-btn` instances on user-profile / organisation-user-profile / system-admin-users / workspace-settings / org users / workspace members screens), `icon-alert-circle` → `icon-circle-alert` (1 replacement; action-card eyebrow). Backup at `prototypes/dashboard/index.html.pre-icon-rename-backup`.
- 2026-05-19 — Converted devbar tab groups into dropdowns. A runtime IIFE (`<script>` at end of body) scans `.devbar` for labelled groups (a `.dev-label` plus consecutive `.dev-btn` siblings up to the next `.dev-label` or `.dev-spacer`) and wraps each into a `.dev-dd` dropdown: trigger pill shows "LABEL · active value ▾", click opens a `.dev-dd-menu` popover above the devbar (`bottom: calc(100% + 6px)`, `var(--surface-900)` bg, primary-500 highlight on active item), ESC + click-outside close. Original `.dev-btn` elements stay in the DOM with `display: none` so every existing click handler fires untouched when a menu item delegates the click. Two implicit labels are auto-injected so dashboard's leading unlabelled clusters become real dropdowns: "Screen" before the first `data-nav` button, "Modals" before the first `data-action^="open-"` or `data-org-modal-open` button. Duplicate labels in the same devbar are merged (e.g. would handle two "Step" labels if present). MutationObserver on each original button keeps the trigger's value text in sync when external code toggles `.is-active`/`.active`. Result: ~30 individual buttons in the dashboard devbar collapse into 4 dropdowns (Screen / Modals / Profile viewer mode / Welcome state) + 2 standalone action buttons (Toggle workspace menu / Reset workspace).
- 2026-05-19 — Renamed the "Standard User" role to "Member" across all display copy in the dashboard (43 → 0 occurrences): role badges and dropdowns, welcome-hero copy ("Joined as Member" / "You have Member access — …"), audit/activity feed text ("as Member", "to Member", "from Member"), modal role-grid + select options, devbar welcome-variant button label, role-badge map labels, "Default role is Member." modal note, system-admin Users table role cells. Also resolves PRD Clarification 2 (default invite role = Member) and Clarification 1 (pure RBAC access model — no per-asset Viewer or per-group Moderator assignment). Internal identifiers preserved as `'standard'` / `.is-standard` / `data-role="standard"` to avoid a JS-conditional refactor. Backup at `prototypes/dashboard/index.html.pre-member-rename-backup`.
- 2026-05-19 — Merged Workspace Members screen into Workspace Settings › Users tab and added persona-conditional workspace dropdown. §1 Sidebar: removed "Members" nav item; workspace dropdown items now rendered by `renderWorkspaceMenu()` based on persona tier (Org Admin: workspace settings / switch workspace / organisation settings / system admin / sign out; Workspace Admin: workspace settings / switch workspace / sign out; Member/Mod/Viewer: switch workspace / sign out); devbar "Persona" group added (Sarah Chen Org Admin / Priya Nair Workspace Admin / Dyl Mahlangu Member). §2 Routing: all four `.ws-row` items in Org > Workspaces tab made clickable with `data-org-ws-id` / `data-org-ws-name` attributes; clicking a row or its "Open" button navigates to Workspace Settings defaulting to the Users tab, shows a breadcrumb nav ("Organisation › Workspaces › [name]"), updates the title and settings-name input for the selected workspace. Breadcrumb back links return to Org › Workspaces or Org › Summary. §3 Workspace Settings Users tab: replaced legacy 9-user single-column table with a full two-pane users management surface — 4 sub-tabs (Members/Candidates/Invites/Deactivated), search input, filter popover (Role + State selects), "Invite member" CTA; members table with checkbox + name/email + single role column; candidates table dynamically rendered from `WORKSPACE_CANDIDATES` keyed by workspace ID (ld-hub/sales-training/grad-hiring/compliance, 2 candidates each); invites and deactivated sub-panels; 325px peek rail with member detail (avatar + name + email + role badge + meta dl + quick actions) and candidate detail (candidate-tier styling + tests taken meta + quick actions); bulk bar (change role / deactivate / clear; "Change role" hidden on candidates tab); `.role-badge.is-candidate-tier` added (info-100 bg / info-500 text). Retired `workspace-members` screen: HTML block removed, CSS block removed, `sidebarFor` entry removed, devbar button removed, Section 7 IIFE removed. Backup at `prototypes/dashboard/index.html.pre-settings-merge-backup`.
- 2026-05-19 — Fixed the workspace dropdown menu: (a) labels were rendering on two lines because rendered items used `<span><i>` and didn't match the parent `.ws-menu-item`'s 3-cell grid (`24px 1fr 12px`); rendered items now include `<i class="lead">` + `<span class="label">` + `<i class="trail">` so labels span the full width correctly; (b) "Switch workspace" was appearing twice (once as the static hover sub-drawer trigger at top, once duplicated inside the persona-rendered list) — removed from the rendered list entirely; the static hover trigger is the sole entry. (c) Rewrote `renderWorkspaceMenu()` with explicit persona-conditional visibility: Switch workspace visible only when `workspaceCount > 1` (toggles the static element + its trailing `<hr>`); Workspace settings visible only for Org Owner / Org Admin / Workspace Admin; Organisation settings visible only for Org Owner / Org Admin; System admin visible only when `isSuperAdmin: true` (internal Traverse profiles); Sign out always present. Order in dropdown: Switch workspace (if multi-ws) → Workspace settings → Organisation settings → System admin → Sign out. (d) Expanded PERSONAS map to 7 demo variants covering every combination: Tobias O'Brien (Org Owner multi-ws), Sarah Chen (Org Admin multi-ws), Priya Nair (WS Admin multi-ws), Marcus King (WS Admin single-ws), Dyl Mahlangu (Member multi-ws), Iona Andrade (Member single-ws), Internal Admin (Super Admin). Devbar Persona group rebuilt with all 7. Global state renamed `_currentPersonaId` → `_currentPersonaKey` to reflect the named-key indexing.

- 2026-05-28 — Reworked the Org > Invite member modal (`#org-invite-member-modal`). (a) Form fields no longer read as disabled: `.org-modal-input/.select/.textarea` background switched from `var(--surface-100)` to `var(--surface-0)` with a slightly darker `surface-300` resting border and a `surface-400` hover border so they look clickable and active at rest. (b) Reordered the form: Email → Role → Workspace → Personal note (Workspace moved below Role since Role determines whether a workspace is needed). (c) Email is now explicitly required (`required` attr + red `*` `.req-hint` indicator on the label). (d) Added a new top-level role: **Organisation admin** — surfaced as a full-width `.org-modal-role-card.is-full` in a new `Organisation` section above the existing four workspace roles (which now sit under a `Workspace` section label). Roles are still a single radiogroup; `data-role-scope="org"` vs `"workspace"` differentiates the two. Picking Organisation admin hides the Workspace field and drops its `required`; picking any workspace role shows it and reinstates `required`. (e) Workspace `<select>` now has a `"Select workspace"` placeholder `<option value="" disabled selected>` instead of defaulting to L&D Hub. (f) Send-invitation enable/disable logic rewritten to gate on `email AND role AND (org-admin OR workspace)`; payload now sends `workspace: null` when Org admin is selected. CTA button on the Users tab still reads "Invite member".
- 2026-05-28 — Replaced the Org > Invite member modal's "Organisation admin" role card with a toggle row. New `.org-modal-toggle-row` (border + radius + bg, primary-tinted `.is-on` state) sits above the role grid: title "Organisation admin", helper "Full access to all workspaces", and a reused `.toggle` switch on the right (`#invite-org-admin-toggle`). Toggle is OFF by default; Workspace role + Workspace dropdown are visible, Member is preselected. Flipping the toggle ON adds `.is-hidden` to both `#invite-workspace-role-group` and `#invite-workspace-group` and drops the workspace `required` attribute; flipping OFF restores both. Renamed the role section label to "Workspace role". JS rewritten: validation now keys off `orgAdminToggle.checked` rather than a `data-role-scope` attribute on the cards (attribute removed); send payload sends `role: 'org-admin'` with `workspace: null` when toggle is on, else `role: <card data-role>` with the chosen workspace.
- 2026-05-28 — Refined Org > Invite member modal selection model + input strokes. (a) Member role is no longer preselected — all four workspace-role cards start with `is-selected` removed and `aria-pressed="false"`. (b) Workspace dropdown is now hidden by default and only revealed once a workspace role card is selected (and the Org admin toggle is off), making the role→workspace correlation explicit; toggling Org admin on still hides both sections regardless of role state. Helper `syncOrgAdminState()` renamed to `syncVisibility()` and now reads both `isOrgAdmin()` and `getSelectedCard()` to compute workspace visibility + `required`. (c) Aligned input strokes with the role/toggle card strokes: `.org-modal-input/.select/.textarea` resting border moved from `surface-300` back to `surface-200` (same as `.org-modal-role-card` and `.org-modal-toggle-row`); hover state changed from `surface-400` to `primary-300` to mirror the role-card hover.
- 2026-05-28 — Org > Invite member modal: removed all required-asterisk `.req-hint` indicators from the Email, Workspace role, and Workspace labels for visual consistency, since the new inline footer hint now signals the next required action. Added `.org-modal-footer-hint` (small `surface-500` grey text, `margin-right: auto` so it sits left of the Cancel + Send invitation buttons in `.org-modal-footer`) with id `#invite-footer-hint` and `aria-live="polite"`. `syncSendBtn()` now updates the hint each tick: empty email → "Add email address"; email entered but no role → "Select role"; workspace role chosen but no workspace → "Select workspace"; ready (org-admin toggle on with email, or email + workspace role + workspace) → hint hidden via `.is-hidden` and Send invitation becomes active. Send-button activation rule confirmed: `email AND (orgAdmin OR (workspaceRole AND workspace))`.
- 2026-05-28 — Org > Invite member modal: collapsed the Organisation-admin toggle row and the Workspace dropdown into a single bordered **Access level** group sitting between Email and the (conditional) Workspace role grid. New container `.org-modal-access` (border `surface-200`, radius 8, padding 12/14) holds two `.org-modal-access__row` blocks separated by an `.org-modal-access__or` divider (`uppercase "or"`, surface-200 hairlines via `::before/::after`). Top row: existing toggle wired to `#invite-org-admin-toggle`, helper text "Full access to all workspaces". Bottom row: the workspace `<select>` (`#invite-workspace`) with the "Select workspace" placeholder. Mutual exclusivity in JS — picking a workspace flips Org admin off; switching Org admin on resets the workspace to the placeholder; whichever side is inactive picks up `.is-disabled` (opacity 0.5 + `pointer-events:none`) so the either/or is visible at a glance. The Workspace role group (`#invite-workspace-role-group`) starts with `.is-hidden` and is revealed only when a workspace value is set (and Org admin is off); inside it, Member is now `is-selected` by default in markup with a new `.org-modal-role-chip` "Default" pill (small primary-tinted rounded chip via `color-mix(primary-600, transparent 88%)`) sitting inline next to the role name. Removed the previous `.org-modal-role-note` "Default role is Member. Least-privilege per spec §5 P3." line. Send-button rule simplified to `email AND (orgAdmin OR workspaceSelected)` since Member auto-fills the role once a workspace is picked. Footer hint progression shortened to: "Add email address" → "Select access level" → (ready).
- 2026-05-28 — Org > Invite member modal: removed the `.is-disabled` dim on the Organisation admin row when a workspace is selected, so users can still flip the toggle on to switch from a workspace selection to org-wide access. Workspace row continues to dim when Org admin is on (the toggle is the obvious way back). Mutual-exclusivity reset behaviour on change is unchanged.
- 2026-05-28 — Split the invite/add-user flow into two surface-aware modals. **Org settings:** modal title "Invite member" → "Invite user", footer button "Send invitation" → "Invite user", and Org > Users tab CTA "Invite member" → "Invite user" (internal modal id `#org-invite-member-modal` and IDs preserved to keep existing JS wiring intact). Devbar pill renamed to "Org · Invite user" and a new "Workspace · Add user" pill added. **Workspace settings:** added new modal `#org-ws-add-user-modal` reusing the `.org-modal-*` styling — title "Add user", footer button "Add user" — with a deliberately narrower form: Email → preset Workspace (new `.org-modal-readonly` `surface-50` chip showing the current workspace name, no dropdown / no Access level container / no Org admin toggle) → Workspace role grid (always visible, Member preselected with `Default` chip) → Personal note. New dedicated IIFE drives it (`#ws-add-email`, `#ws-add-role-grid`, `#ws-add-send-btn`, `#ws-add-footer-hint`) — send enables on email-only since workspace is preset and Member is default; footer hint progression is just "Add email address" → (ready). Workspace Users-tab CTA renamed "Invite member" → "Add user" and its handler now opens the new modal, populating `#ws-add-workspace-name` from `WSUT_WS_NAMES[wsutCurrentWsId]` (no more pointing at `#invite-workspace` on the org modal + disabling it).
- 2026-05-28 — Sidebar: added a bottom-pinned user-profile block (`.sb-user-wrap` / `.sb-user`) to all three sidebar variants (workspace, system-admin, organisation). New `.sb-user-wrap` uses `margin-top: auto` with a 1px `surface-200` top border to anchor the block at the bottom of the sidebar shell; `.sb-user` mirrors `.sb-workspace` layout (28px avatar grid + name + chevron-up). Clicking the row opens a `.sb-user-menu` popover above the row (`bottom: calc(100% + 6px)`, white card with `surface-200` 0.5px border, 10 radius, soft shadow) containing two menu items: **View profile** (`data-nav="user-profile"` — picked up by existing nav routing) and **Sign out** (`is-danger` red variant — no-op for prototype, logs to console). Open/close behaviour wired in a new IIFE: toggle on click, click-outside-closes-all, ESC closes-all, multiple-menu safety (each toggle finds its own menu via parent). Collapsed-sidebar styling: avatar-only, menu pops to the right instead of above. The `renderSbUser()` helper inside the existing persona IIFE pushes the current persona's name + auto-derived initials into every `[data-sb-user-name]` / `[data-sb-user-avatar]` slot, and fires on init plus on every `_setPersona()` change so all three variants stay in sync. **Sign out removed from the workspace dropdown menu** — the `items.push({ label: 'Sign out', ... })` line in `renderWorkspaceMenu()` was deleted; the workspace dropdown is now purely about workspace + admin entry points (Switch workspace / Workspace settings / Organisation settings / System admin), and Sign out lives exclusively on the new bottom sb-user menu.
- 2026-05-28 — Devbar no longer overlays the bottom of the viewport. Removed `position: fixed; bottom: 0; left: 0; right: 0; z-index: 100;` from `.devbar` so it lays out as a normal static block at the end of `<body>`, below the `.app` shell (which keeps `min-height: 100vh` and fills the viewport). Net effect: the page container ends at the viewport bottom, the new bottom-pinned `.sb-user-wrap` profile block is fully visible, and the devbar sits below the fold — scroll past 100vh to reach it. Visual treatment (surface-900 background, top box-shadow) preserved so the devbar still reads as a distinct strip when scrolled into view.
- 2026-05-28 — Switched the page shell to a flex-column body so the devbar is always visible without covering the sidebar. `html, body { height: 100%; }`, `body { display: flex; flex-direction: column; min-height: 100vh; }`; `.app` lost `min-height: 100vh` and now uses `flex: 1 1 auto; min-height: 0;` so it claims all remaining viewport height above the devbar; `.devbar` got `flex: 0 0 auto` so it stays its natural height at the bottom of the viewport. Removed the now-redundant `.content { padding-bottom: 80px; }` override that used to reserve space under the floating bar. Net effect: viewport splits cleanly into App-area + Devbar-strip, both always on-screen; the sidebar's bottom-pinned profile block sits flush against the devbar's top edge with no overlay; `.content` keeps `overflow: auto` so long pages scroll inside the app without pushing the devbar offscreen.
- 2026-05-28 — Workspace-row dropdown now varies by persona tier. `renderWorkspaceMenu()` computes `wsRowStatic = !canAdminWorkspace && !hasMulti` and `hasMulti = p.workspaceCount > 1`. Three behaviours: (a) **Single-workspace member/viewer/moderator** — `.sb-workspace` gets `.is-static` (new CSS: `cursor: default`, hover background suppressed, chevron `display: none`), `aria-haspopup` is removed and `aria-disabled="true"` added; the click handler on `#ws-toggle` now early-returns when `.is-static` is present, so the row reads as a plain label. (b) **Multi-workspace, no admin rights** — dropdown still opens, Switch workspace is the only item, divider is hidden. Divider visibility logic tightened: it now keys off `(hasMulti && items.length > 0)` rather than `hasMulti` alone, so it only renders when at least one persona-conditional item follows. (c) **All other personas** (Org owner / Org admin / Workspace admin / Super admin) — unchanged: Switch workspace (if multi) → divider → Workspace settings / Org settings / System admin. Re-evaluated on every `_setPersona()` call so the devbar Persona switcher reflects the right state immediately.
- 2026-05-28 — Widened the sidebar workspace dropdown so "Organisation settings" fits on a single line. `.ws-dropdown` switched from `left: 0; right: 0` (stretched to the sidebar column width, ~216px content area, which forced the label cell to wrap) to `left: 0; right: auto; min-width: 100%; width: 264px;` — the dropdown still anchors flush-left under the workspace row, but spills ~24px to the right of the sidebar column so the longest label renders on one line. No change to padding, grid columns, or item styling.
- 2026-05-28 — Organisation page restructure. (a) Promoted the Invite user + New workspace CTAs to the page-level header. `.org-page-header` became a flex container (`display: flex; justify-content: space-between; align-items: flex-start; gap: 16px`) with two children: `.org-page-header__text` (heading + sub) on the left and `.org-page-header__actions` (flex row, 8px gap, `flex-shrink: 0`) on the right. Buttons sit in this fixed order: **New workspace** (`.btn-secondary`, `console.log('New workspace …')`) on the left, **Invite user** (`.btn-primary`, `data-org-action="open-invite-member"` → opens `#org-invite-member-modal`) on the right. Because the header sits outside `.org-tab-panel`, both CTAs are now visible on Summary / Workspaces / Users without per-tab duplication. (b) Deleted the per-tab originals — removed the `.btn-primary` "New workspace" button from `.org-workspaces-toolbar-end` and the `.btn-primary` "Invite user" button from `.users-action-end` (sub-tabs, search, filter all preserved). (c) Removed the **Organisation leaders** user-list card from the Summary tab — the entire `<div class="org-summary-card">` block under `.org-summary-row2` (Tobias / Sarah / Marcus rows + view links) was deleted. `.org-summary-row2` simplified from `grid-template-columns: 1fr 1fr` (with a 900px breakpoint) to a permanent `1fr` so **Recent admin activity** spans full width. (d) Landing on Org settings from the dashboard's workspace dropdown now always opens the Summary tab — `renderWorkspaceMenu()`'s Organisation settings item carries `orgTab: 'summary'`, and the menu-item renderer was extended to apply `item.orgTab` as a `data-org-tab` attribute, which the existing `[data-nav]` click-delegate already routes through `navTo('organisation', { orgTab })`.
- 2026-05-28 — Organisation page fixes. (a) Root cause for "Users content showing under Summary / Workspaces": `#org-panel-users { display: flex }` at line 2315 had higher specificity than `.org-tab-panel { display: none }`, so the Users panel was always rendered regardless of which tab was active. Scoped the rule to `#org-panel-users.is-active { display: flex; flex-direction: column; gap: 18px; padding-top: 20px; }` so the `.org-tab-panel { display: none }` default takes over when the Users tab isn't active. The other two panels (`#org-panel-summary`, `#org-panel-workspaces`) had no such override and were already correct. (b) Reverted the page-header CTA experiment — removed the `.org-page-header__actions` flex container + both buttons; `.org-page-header` returned to its original simple block layout (heading + sub-line only); removed the supporting `.org-page-header__text` / `.org-page-header__actions` CSS. (c) Restored per-tab primary CTAs in their original contextual positions: **New workspace** (`.btn-primary`) back inside `.org-workspaces-toolbar-end` (after the Sort select); **Invite user** (`.btn-primary`, `data-org-action="open-invite-member"`) back inside `.users-action-end` (after the Filter popover). Net: each tab carries its own contextual primary action again, and tab content no longer bleeds between tabs.
- 2026-05-28 — Organisation + Workspace settings parity pass. (a) Restored the page-header CTAs on Organisation settings — `.org-page-header__actions` flex container with **New workspace** (`.btn-secondary`) on the left and **Invite user** (`.btn-primary`, `data-org-action="open-invite-member"`) on the right; per-tab primary CTAs (New workspace inside `.org-workspaces-toolbar-end`, Invite user inside `.users-action-end`) preserved so both views exist together. (b) Workspace settings page header restyled to match Org: replaced `<h1 class="settings-title">` (700/32px) with a full `.org-page-header` block — `.org-page-header__text > h1#ws-settings-title` (600/28px) + `<p class="sub" id="ws-settings-subtitle">Manage members, integrations and settings for this workspace.</p>`. Existing dynamic title rename (`"L&D Hub settings"` on drill-in) keeps working since `#ws-settings-title` is preserved. No CTAs added to this header per spec. (c) New sidebar variant `data-sidebar-variant="workspace-settings"` mirroring the Organisation sidebar pattern: brand row + `Workspace` back link → dashboard + `.sb-nav` with three items (`Settings` / `Users` / `Integrations`) each carrying `data-nav="workspace-settings"` + `data-ws-tab` + matching Lucide icons (`icon-settings`, `icon-users`, `icon-plug`) + bottom-pinned `.sb-user-wrap` (identical View profile / Sign out menu). `sidebarFor['workspace-settings']` flipped from `'workspace'` to `'workspace-settings'`. Wiring: new `activateWsSettingsTab(name)` helper toggles tab buttons + tab panels + sidebar items in one pass. `navTo()` extended with a `workspace-settings` branch + `opts.wsTab` (mirrors the existing `opts.orgTab` handling); the global `[data-nav]` click delegate reads `data-ws-tab` and forwards it. The existing `.tabs` click handler short-circuits to `activateWsSettingsTab()` when the clicked tab lives inside `[data-screen-name="workspace-settings"]`, so the sidebar stays in sync regardless of entry point. `openWorkspaceSettings()` (drill-in from Org > Workspaces) simplified to a single `navTo('workspace-settings', { wsTab: 'users' })` call instead of manually toggling `.tab`/`.tab-panel`. (d) Workspace settings Users sub-tabs trimmed: **Candidates** removed (the chip button + the entire `.users-subpanel[data-wsut-subpanel="candidates"]` table block). Internal `renderWsutCandidates()` and `wsutCandTbody` references left in place — they already early-return on missing DOM (`if (!wsutCandTbody) return;`), so no JS error path. Remaining order: Members / Invited / Deactivated; "Invites" tab label renamed to **Invited**. (e) Organisation Users sub-tabs: same "Invites" → **Invited** rename for parity.
- 2026-05-28 — Organisation header CTAs are now tab-aware and the per-tab duplicates are gone. Tagged the two header buttons with `id="org-header-new-workspace-btn"` / `id="org-header-invite-user-btn"` (+ `data-org-header-cta` for clarity). New `syncOrgHeaderActions(tabName)` helper, called at the end of `activateOrgTab()` so it fires on every tab change (and on initial entry via `navTo('organisation', { orgTab })`). Rules: **Summary** → both buttons visible, New workspace styled `.btn-secondary`, Invite user `.btn-primary`; **Workspaces** → only New workspace, promoted to `.btn-primary` since it stands alone; **Users** → only Invite user (primary). Visibility toggled via the `hidden` attribute. Removed the per-tab CTAs that previously duplicated these: deleted the `.btn-primary` New workspace from `.org-workspaces-toolbar-end` and the `.btn-primary` Invite user from `.users-action-end`; sort dropdown, search, filter popover, and Users sub-tabs left intact. Header markup is the single source of truth for these two actions now.
- 2026-05-28 — Tab-aware Organisation header CTAs now actually toggle: root cause was `.btn { display: inline-flex }` overriding the native `hidden` attribute (specificity beats it). Added `.btn[hidden] { display: none !important }` so the existing `syncOrgHeaderActions()` logic (Summary → both; Workspaces → New workspace only, promoted to `.btn-primary`; Users → Invite user only) renders correctly.
- 2026-05-28 — Workspace dropdown trimmed and a new sidebar Settings section added. (a) `renderWorkspaceMenu()` no longer pushes the **Organisation settings** item — that entry point now lives exclusively on the sidebar. Switch workspace + Workspace settings + System admin remain; divider stays on the original "show only when ≥1 persona item sits below Switch workspace" rule. (b) New persona-conditional **Settings** section on the workspace sidebar variant — `<span class="sb-eyebrow" id="sb-settings-eyebrow">Settings</span>` + `<nav class="sb-nav" id="sb-settings-nav">` with two `.sb-item`s: **Organisation** (`data-nav="organisation"` + `data-org-tab="summary"`, `icon-building-2`, id `sb-settings-org`) above **Workspace** (`data-nav="workspace-settings"` + `data-ws-tab="settings"`, `icon-settings`, id `sb-settings-ws`). (c) New `renderSbSettings()` inside the persona IIFE drives visibility per tier: **org-owner / org-admin** see both rows; **workspace-admin** sees only Workspace; **member / viewer / moderator** see neither — when no rows are visible the eyebrow + nav container are hidden too so there's no empty header. Hooked into init + `_setPersona()` alongside `renderWorkspaceMenu()` / `renderSbUser()` so devbar persona switches re-render instantly. The existing `[data-nav]` click delegate already forwards `data-org-tab` + `data-ws-tab`, so both items route through `activateOrgTab()` / `activateWsSettingsTab()` without extra wiring.
- 2026-05-28 — Workspace settings Users peek panel now populates like the Organisation users peek. Root cause: `showWsutMemberPeek(row)` was querying mismatched attribute keys (`role-badge`, `job-title`, `member-since`, `last-active`) while the peek HTML used `org-role-badges`, `jobTitle`, `memberSince`, `lastActive` — so most fields silently rendered as `—`. Status dot / label, sign-in method, and the explicit "Role in this workspace" badge were never set at all. Fix: exposed `ROLE_BADGE_MAP` + `ORG_ROLE_MAP` on `window` from the Org users IIFE so the workspace IIFE can reuse them without duplicating the source of truth, and rewrote `showWsutMemberPeek()` to mirror the Org `renderPeekDetail()` flow — name / email / initials (with row-text fallback when `_orgUserById` lacks the id), org-level role badge via `ORG_ROLE_MAP`, status dot+label class swap (`is-active` / `is-pending` / `is-deactivated` / `is-cancelled`), workspace-scoped role badge cloned from the row's `.role-badge`, and the meta `<dl>` (`jobTitle`, `department`, `memberSince`, `lastActive`, `signInMethod`) wired via a shared `setField()` helper that defaults to `—`. Row highlight + aria-label preserved. Net effect: clicking a workspace-settings members table row now surfaces the same rich profile as the Org Users tab peek does.
- 2026-05-28 — Fixed root cause for the Workspace settings peek doing nothing on row click: the members table rows use `data-wsut-user-id="u1"` etc., but every JS path (the row-click delegate that calls `showWsutMemberPeek`, the bulk-select handlers, `clearWsutSelection`, and the master-checkbox path) was selecting / reading `data-wsut-member-id`, which never matched. Renamed all 6 JS references from `data-wsut-member-id` to `data-wsut-user-id` so `tr[data-wsut-user-id]` actually catches the rows. Row click now fires `showWsutMemberPeek(row)` with the real id (e.g. `'u1'`), which feeds the previously-fixed peek populater — name / email / status / org badge / ws role / meta fields all light up.
- 2026-05-28 — Workspace settings peek "Open full profile" now opens the same Org user profile screen as the Organisation peek. There was no JS handler bound to `#wsut-peek-open-profile-btn` previously — the button was inert. Added a click handler scoped to the workspace-settings IIFE that resolves the peeked user from `window._orgUserById[wsutPeekedMemberId]`, calls the already-exposed `window._renderUserProfile(user)` (the same populater the Org peek uses), then `navTo('organisation-user-profile')`. Same target screen, same data shape, same sidebar transition. Also fixed the peek close buttons (`#wsut-peek-close-btn` / `#wsut-cand-peek-close-btn`) which were referenced in markup but never wired — the existing close delegate only matched `[data-wsut-action="close-peek"]`; widened the selector so the X buttons also fire `hideWsutPeek()`.
- 2026-05-28 — Sidebar bottom-menu "View profile" now opens the same Org user-profile screen (the rich layout used by the Org and Workspace settings peeks) instead of the legacy `[data-screen-name="user-profile"]`. All four `.sb-user-menu-item` View profile buttons (workspace / system-admin / organisation / workspace-settings sidebar variants) switched from `data-nav="user-profile"` to `data-action="view-profile"` so the global `[data-nav]` delegate no longer routes them. The sb-user-menu IIFE now handles `data-action="view-profile"`: resolves the current persona via `window._currentPersonaUserId`, hands the matching `_orgUserById[id]` to `window._renderUserProfile(user)`, then `navTo('organisation-user-profile')`. To support this, the persona IIFE now exposes `window._currentPersonaUserId` (the persona's user id, e.g. `'u1'`) alongside `_currentPersonaKey`, refreshed on every `_setPersona()` call so devbar persona switches reflect instantly. Legacy `user-profile` screen is left in place (devbar still has a button), but the sidebar route no longer points at it.
- 2026-05-28 — Temporarily hid the "Feedback ready for your review" action card on the dashboard by adding the `hidden` attribute to `<section class="action-card" id="action-card">` (the entire eyebrow + title + sub + collapsible list block). Markup, IDs, and JS handlers are all preserved; remove the `hidden` attribute on `#action-card` to bring it back when the feedback-review feature resumes.
- 2026-05-28 — Added a Summary tab to Workspace settings, mirroring the Org Summary layout. Tab strip order is now **Summary / Settings / Users / Integrations** (Summary is `is-active` by default in markup; the Settings panel lost `is-active` and picked up the `hidden` attribute, matching the Users/Integrations defaults). New `[data-tab-panel="summary"]` reuses the Org Summary primitives — `.org-summary-metrics` row with four `.org-summary-metric-card`s (Simulations · 13, Characters · 3, Assessments · 6, Tests · 24, each with a `.org-summary-metric-sublabel` of breakdown numbers from the dashboard Assets section), and `.org-summary-row2` containing a single `.org-summary-card` with `.org-summary-activity-header` (title "Recent activity" + "View activity log →" action) and an `.org-summary-activity-list` populated with five recent activity rows lifted from the dashboard Activity card (Improve Top Turns, Debugging and Enhancing a Web Application, Fundamentals of HTML and CSS (Copy), Dyl FS Test, Fire Simulation Get To Know — status appended inline in `<em>`). No new CSS or JS — `activateWsSettingsTab()` already handles arbitrary `data-tab` values, the existing sidebar-sync code, the tabs delegate, and the `openWorkspaceSettings()` drill-in (which still defaults to `wsTab: 'users'`) all work unchanged. Sidebar variant `data-sidebar-variant="workspace-settings"` got a Summary `.sb-item` (`icon-layout-dashboard`, `data-ws-tab="summary"`) inserted as the first row so the nav order matches the tab order.
- 2026-05-28 — Entering Workspace settings always lands on the Summary tab now, regardless of entry point. (a) `openWorkspaceSettings()` drill-in from Org > Workspaces switched from `navTo('workspace-settings', { wsTab: 'users' })` to `wsTab: 'summary'`. (b) Dashboard sidebar's Settings-section Workspace item (`#sb-settings-ws`) flipped from `data-ws-tab="settings"` to `data-ws-tab="summary"`. (c) Workspace dropdown's "Workspace settings" item now carries an explicit `wsTab: 'summary'` (was relying on the markup default); the dropdown menu-item renderer was extended to apply `item.wsTab` as `data-ws-tab`, matching the existing `item.orgTab` → `data-org-tab` plumbing. (d) The workspace-settings sidebar variant's own "Settings" `.sb-item` (id-less; `data-ws-tab="settings"`) intentionally left alone — it's the in-screen tab switcher, not an entry point.
- 2026-05-28 — Org > Add-to-workspace modal aligned with the rest of the role-selector pattern. (a) The `<select id="atws-role">` dropdown was replaced with a 4-card `.org-modal-role-grid` (`#atws-role-grid`) reusing the exact card structure from the Invite user / Add user modals — Workspace admin, Member (with the `.org-modal-role-chip` `Default` chip and `is-selected` / `aria-pressed="true"` in markup), Moderator, Viewer. New scoped IIFE delegates clicks on `#atws-role-grid` to swap `is-selected` / `aria-pressed` (radio behaviour). The confirm handler now reads `data-role` from the selected card instead of `atws-role.value`; if nothing's selected it falls back to `'standard'`. (b) Stripped the "(spec §5 P4)" tail from the info-block copy — it now reads "…take effect immediately on the user's next action." with no spec reference.
- 2026-05-28 — Added a New workspace modal (`#org-new-workspace-modal`) wired to the Org page-header CTA. Modal markup mirrors the Invite user shell — header (`New workspace`) + body with two `.org-modal-group` fields (`#new-ws-name` `<input>`, required, autofocused, with placeholder `"e.g. Sales training"`; `#new-ws-description` `<textarea>` marked `— optional`) + footer with `#new-ws-footer-hint` (`Add workspace name` until populated) and a `Create workspace` primary button (`#new-ws-create-btn`, `aria-disabled` until the name field is non-empty). Trigger plumbing: `#org-header-new-workspace-btn`'s `onclick="console.log(…)"` swapped for `data-org-modal-open="org-new-workspace-modal"`, picked up by the existing global modal-open delegate. New devbar pill (`Org · New workspace`) added next to `Org · Add to ws` for direct access. A new IIFE handles validation (`syncCreateBtn()` enables the button on input + flips the footer hint to empty + `.is-hidden`) and on click console.logs `{ name, description }` and closes the modal. The per-tab New workspace button still lives in the page header — only the click target changed.
- 2026-05-28 — Vercel launchpad gets Organisation + Workspace settings entry points. `vercel-setup/index.html` got two new `.row` cards inserted between Dashboard and User onboarding: **Organisation settings** → `/dashboard?screen=organisation`, **Workspace settings** → `/dashboard?screen=workspace-settings`. Both reuse the existing `/dashboard` Vercel rewrite — only the query string differs. To make the landing actually highlight the Summary tab in both the tab strip and the sidebar nav, the dashboard's initial-state handler was extended: when `?screen=organisation` is detected on load it now calls `navTo('organisation', { orgTab: 'summary' })`, and `?screen=workspace-settings` calls `navTo('workspace-settings', { wsTab: 'summary' })` — both route through the existing `activateOrgTab` / `activateWsSettingsTab` helpers so the tab + sidebar sync exactly the same way a real click would. Other `?screen=` values still fall through to plain `setScreen()`.
- 2026-06-04 — Welcome hero (P5) restyled across all welcome states. (a) Icon block swapped `icon-sparkles` → `icon-handshake` (verified in Lucide 0.453.0). (b) Container padding reduced from `40px 40px 36px` to `16px` all round. (c) Drop shadow widened and lightened: `var(--shadow-md)` replaced with a fainter wider-spread pair `0 8px 24px -4px rgb(0 0 0 / 0.06), 0 2px 8px -2px rgb(0 0 0 / 0.04)`. (d) Role line now renders the role as a `.role-chip` — "Joined as" stays plain text followed by a chip (`#welcome-role-chip`) reusing the profile-screen chip modifiers via a new `ROLE_CHIP_CLASSES` map (standard/empty → `is-standard`, moderator → `is-moderator`, viewer → `is-viewer`); `welcome-role-line` is no longer JS-populated wholesale. (e) All CTAs removed — `.welcome-hero__actions` block (Start exploring / Dismiss / Contact Sarah Chen / Got it) deleted along with `CTA_LABELS`, `DISMISS_LABELS`, and the CTA/dismiss wiring; the hero is now dismissed solely by a new `.welcome-hero__close` icon button (`icon-x`, 28px, absolute top-right 12px) wired to the existing `dismiss(role)` → sessionStorage flow. Post-dismiss placeholder panel untouched (its "Contact Sarah Chen" CTA still uses the retained `.welcome-hero__cta` class).
- 2026-06-04 — Welcome hero refinements + new primary chip variant. (a) Banner padding now `24px 32px` (was 16px all round). (b) Icon swapped `icon-handshake` → `icon-party-popper` (user-picked from four verified Lucide 0.453.0 options). (c) Icon-to-content gap reduced `32px` → `16px`. (d) `.welcome-hero__copy` lost its `max-width: 540px` so the explainer text spans the banner's full width. (e) New `.role-chip.is-primary` variant added (indigo palette: `--primary-50` bg / `--primary-600` text — one step lighter than `.is-admin`'s primary-100/700 so Member and Workspace admin chips stay distinguishable). All Member chips migrated from `.is-standard` → `.is-primary`: welcome banner chip, all-users table row, profile `roleClassMap` ('standard' / 'standard user' keys + fallback), and the welcome `ROLE_CHIP_CLASSES` map (standard/empty). The old `.role-chip.is-standard` rule (info-100/info-500) was removed — no references remain.
- 2026-06-04 — Welcome hero: workspace-admin state added + layout refinements. (a) New `admin` welcome state across all P5 maps — `ROLE_LABELS.admin: 'Workspace admin'`, `HERO_COPY.admin` ("You have Workspace admin access — you can manage users, content, and settings across this workspace."), `ROLE_CHIP_CLASSES.admin: 'is-admin'` (primary-100/700 chip). Not a takeover state — dashboard cards stay visible, same as Member. Reachable via `?welcome=1&role=admin` and a new devbar button (`#wb-admin`, "Workspace admin") inserted between Off and Member; all existing generic wiring (dismiss, sessionStorage key, devbar delegate) handles it unchanged. (b) Banner left/right padding reduced 32px → 24px (now `padding: 24px`). (c) Greeting dropped one type size: 24px (`--fs-2xl`, ds-h3 slot) → `var(--fs-xl)` 20px (ds-h4 slot), now tokenised. (d) "Joined as" role line removed entirely — the role chip now sits inline next to the name inside the greeting (`<h1>` is now flex/wrap with 10px gap, wrapping `#welcome-greeting` span + `#welcome-role-chip`); `.welcome-hero__role-line` CSS deleted, greeting margin-bottom 6px → 8px.
- 2026-06-04 — Welcome hero: greeting-to-copy spacing tightened — `.welcome-hero__greeting` margin-bottom 8px → 4px.
- 2026-06-04 — Welcome hero: responsive scaling + animated dismiss. (a) Greeting font-size is now fluid — `clamp(var(--fs-lg), 0.9rem + 0.55vw, var(--fs-xl))` scales 18px→20px with viewport width; greeting span and copy got `overflow-wrap: break-word` (+ `min-width: 0` on the span) so text always wraps inside the container at any width. (b) ≤640px media query steps the icon block down one size: 52px→40px box, 14px→10px radius, 24px→20px glyph. (c) New dismiss exit animation — `.is-leaving` runs `welcome-hero-out` (`var(--dur-panel)` 500ms, forwards): subtle anticipation dip ~10px down with a 7px micro-settle bounce, then accelerates up to -88px while fading out; per-keyframe cubic-beziers, `pointer-events: none` while leaving. Close-button JS now adds `.is-leaving`, waits for `animationend` (name-checked), then runs the existing `dismiss(role)` sessionStorage flow; guards against double-clicks mid-flight. `prefers-reduced-motion: reduce` skips the animation entirely (CSS `animation: none` + JS matchMedia check dismisses instantly).
- 2026-06-04 — Welcome hero: all role chips in the banner now use the primary state chip. `ROLE_CHIP_CLASSES` map deleted — `renderWelcome()` always sets `role-chip is-primary` (indigo: primary-50/primary-600) for every state (Workspace admin, Member, Moderator, Viewer, Empty). Per-role chip colours (`is-admin`, `is-moderator`, `is-viewer`) remain in use on user tables and profile only — scope decision: banner states identity, tables compare roles. RoleChip also documented as a core component in `design-system/traverse-design-system.md` §7.12 (base spec + full variant table + usage rules, `is-primary` cemented as the default state chip).
- 2026-06-04 — Welcome hero: assigned/unassigned variants for Moderator and Viewer + copy trim. (a) Both unassigned copy strings lost their trailing "[Admin name] manages access for this workspace." sentence (user edit) — now just "Your workspace admin will assign you to submission groups so you can start grading." / "…assign you to content so you can start reviewing." (b) Two NEW welcome states: `moderator-assigned` ("You have Moderator access — you can view and grade the submissions assigned to you.") and `viewer-assigned` ("You have Viewer access — you can view the content and submissions assigned to you.") — deliberately non-specific about what was assigned. Both are non-takeover (dashboard cards stay visible) and dismiss like Member (no post-dismiss placeholder); existing `moderator`/`viewer` keys remain the unassigned takeover states. (c) Devbar: Moderator/Viewer buttons relabelled "Moderator · unassigned"/"Viewer · unassigned"; new "Moderator · assigned" (#wb-moderator-assigned) and "Viewer · assigned" (#wb-viewer-assigned) buttons. All generic wiring (sessionStorage keys, URL param, devbar delegate) handles the new keys unchanged. welcome-banners.design.md updated in step (§2/§5/§6/devbar/walk-throughs/§12 + production note: assigned vs unassigned must derive from real assignment data at render time, not the onboarding URL).
- 2026-06-17 — Workspace switcher now doubles as the org switcher (single pattern, two render modes). (a) Normal personas are unchanged — the sidebar switcher still opens a flat workspace list (`SARAH_WORKSPACES`) exactly as before. (b) Super users (the `sysadmin`/Internal Admin persona, `isSuperPersona()` = `window._currentPersonaKey === 'sysadmin'`) get the same drawer rendered as workspaces grouped under org headings, driven by a new explicit cross-org membership list `SUPER_ADMIN_ORGS` (Traverse: Internal Tools / Platform QA; Northwind Group: L&D Hub / Graduate Hiring; Helix Labs: Research Pilot). The list deliberately spans only the orgs/workspaces the super user actually belongs to — not every org in the system. (c) Clicking any row switches directly across orgs; `switchWorkspace()`/`updateSidebarIdentity()`/`showWsToast()` gained a 4th `orgName` arg. Sidebar identity now wraps name + a new `#ws-sidebar-org` sub-line (`.sb-workspace .ws-id` flex column) that shows the current org — populated for super users only, hidden (and collapsed-sidebar-hidden) otherwise. Cross-org toast reads "Switched to <ws> · <org>". (d) `renderWsList()` factored into a shared `buildWsRow(ws, orgName)` + flat/grouped branch; new `.ws-org-group` / `.ws-org-group-head` / `.ws-org-group-name` / `.ws-org-group-count` styles (uppercase org overline + "N workspaces" count, divider between groups); drawer body capped at `max-height: 360px` with scroll. (e) Persona switching calls new `window._syncWorkspaceSwitcher` (= `resetToPersonaDefault()`) so the switcher swaps flat↔grouped and resets to the persona's default workspace (super users land on Traverse › Internal Tools with org context; everyone else on L&D Hub). `sysadmin` persona `workspaceCount` 4 → 5 to match the dataset.
- 2026-06-17 — Super-user switcher: roomier org groups + scale features (search, A–Z jump, alphabetical sort). (a) Spacing between org groups increased — `.ws-org-group + .ws-org-group` margin-top 2px → 14px, padding-top 2px → 12px (divider kept); heading padding rebalanced to `4px 10px 6px 14px`. (b) New drawer tools bar `#ws-drawer-tools` (shown for super users only, hidden for normal users) holds a reused `.search-input` (`#ws-search-input`, placeholder "Search organisations or workspaces") + an A–Z jump strip `#ws-alpha-index`. (c) `renderWsList()` now sorts orgs alphabetically (`localeCompare`), filters live against the query (whole org if its name matches, else only matching workspaces), and renders an empty state ("No organisations or workspaces match …") when nothing matches. (d) A–Z index renders one `.ws-alpha-chip` per first-letter actually present (post-filter); clicking scrolls the body to that letter's first org via `data-letter-anchor` + `section.scrollTop` (drawer body is now `position: relative` + `scroll-behavior: smooth`). Each letter's first org heading also gets a `.ws-org-letter` cue badge. (e) Search wiring: input re-renders the body on each keystroke (input sits outside the re-rendered `#ws-list-section`, so focus is retained); Escape clears the query first (second Escape bubbles to close the drawer); Arrow keys are kept inside the field so they don't trigger the drawer's close shortcut. Search clears on drawer open/close and persona change; openDrawer re-renders so it always reopens unfiltered. (f) Demo `SUPER_ADMIN_ORGS` expanded to 11 orgs across letters A/B/C/D/E/H/N/O/S/T/W (Acme Corp → Westbridge University) to exercise the many-org case; super users now land on a stable home workspace (`DEFAULT_SUPER_WS_ID = 'internal-tools'`, Traverse) regardless of the alphabetical display sort. Normal-user (flat) switcher behaviour unchanged — no search, no index.
- 2026-06-17 — Super-user switcher refinements: no role badges, vertical A–Z rail with scroll-spy, wider panel. (a) Super users have a single all-access standing (not a per-workspace admin/moderator/member), so the grouped rows no longer show a role badge or member-count meta — `buildWsRow()` gates both on `showRole = !isSuperPersona()`; `role` keys removed from `SUPER_ADMIN_ORGS` data. Normal-user rows keep their role badges. (b) The A–Z index moved from a horizontal strip in the tools bar to a vertical rail (`.ws-alpha-rail`, id kept `#ws-alpha-index`) running down the left edge of the scrolling list. New `.ws-drawer-main` flexbox wraps the rail + `.ws-drawer-body` (body now `flex:1; min-width:0`). The horizontal `.ws-alpha-index` flex-wrap styles and the per-heading `.ws-org-letter` cue badge were removed (the rail now owns letter indication). (c) Scroll-spy: new `updateActiveLetter()` highlights the rail letter whose org section sits at the top of the scroll viewport (`.ws-alpha-chip.is-active` → primary-600 fill); wired to the list's `scroll` event (once, since the element persists across re-renders) and called after each render + jump. Inactive chips read as muted surface-400. (d) The super-user panel widens to make room for the rail — `.ws-sub-drawer.is-super { width: 340px }` (normal users stay 280px); `is-super` class toggled in `renderWsList()`, width transition added. Empty search-results state hides the rail so there's no empty bordered column beside the message.
- 2026-06-17 — Switcher drawer: removed the top header (back chevron + "Switch workspace" overline). The `.ws-drawer-header` / `.ws-drawer-back` / `.ws-drawer-overline` markup + CSS and the back-button JS wiring (`wsDrawerBack` var + click handler) were all deleted as unnecessary — the drawer opens on hover, so the search bar (super users) or workspace list (normal users) is now the first element. Drawer still closes via mouse-leave, Escape, or Left-arrow (which return focus to the Switch-workspace menu item); the `#ws-sub-drawer` aria-label "Switch workspace" is retained for screen readers.
- 2026-06-17 — Super-admin switcher A–Z rail now shows the full alphabet. `renderAlphaIndex()` rewritten to always render A–Z (plus a trailing '#' bucket only when a non-letter org is present); letters that have no matching org get `.ws-alpha-chip.is-empty` (greyed surface-300, `disabled`, no `data-jump-letter`, `aria-hidden`) so they're visible but non-interactive — only present letters are clickable/scroll-spy-active. Chip metrics shrunk to fit all 26 in the 360px panel (height 16→12px, font 10→9px, radius 4→3px, rail gap 1→0, padding 8→6px); resting colour bumped surface-400→500 for legibility at the smaller size. Active + hover states unchanged.
- 2026-06-17 — Added the login launch screen (workspace-picker takeover) to the dashboard prototype, rebuilt from app-screenshots/login/login-launch-screen.png. Full-viewport `#login-launch` overlay (`position:fixed; z-index:1200`) on a surface-50 dotted-grid background: fixed `studio_full_light.svg` wordmark top-left, settings + sign-out icon buttons top-right (thin divider between). Centred two-column body — left: Traverse brand mark, `<h1>` "Continue building in your workspace." (700/44px, -0.02em), sub-copy, and a primary "+ New Workspace" button; right: a `--radius-xl` white card with a surface-100 search input ("Search") and four workspace rows (52px `--radius-lg` avatar + stacked name / "N members"): David Guetta · 9 members, Traverse Studio Demo · 16 members (studio_square_light.svg tile), Ai Certified · 11 members, Lara's Workspace · 1 members. Hidden by default; opened via a new devbar "Login launch" button (`data-action="open-login-launch"`); dismissed by clicking any workspace row, New Workspace, sign-out (`data-ll-dismiss`), or Escape; search filters rows live by name. Verified with a headless-Chrome render against the source screenshot. Two faithful substitutions (no source asset in repo): the colour waves-and-arrow brand mark is recreated as inline SVG (three overlapping primary-200/400/900 circles + white wave + arrow), and David Guetta's photo avatar falls back to "DG" initials.
- 2026-06-17 — Login launch screen is now role-driven, reflecting the persona selected in the devbar (content/list/actions vary; layout + structure unchanged). The copy block (`#ll-title`, `#ll-sub`), primary action (`#ll-action`), and card list (`#ll-list`) are rendered by JS from a per-persona access model keyed to `window._currentPersonaKey`; `_setPersona()` now calls `window._renderLoginLaunch()` so switching persona updates the screen live. Variants: (1) Org owner / Org admin (multi-ws) — full list + "New Workspace" create button; owner gets create-oriented sub-copy. (2) Workspace admin (multi-ws) — list of managed workspaces, "Select a workspace you manage…", no create button. (3) Member (multi-ws) — "Choose a workspace to continue." + "Select a workspace to continue your simulated work experiences.", no create button. (4) Single-workspace user (member/WS admin) — shows only that workspace, "You have access to <name>. Continue where you left off.", a primary "Continue →" button instead of create, and the search bar hidden. (5) Super user (sysadmin) — workspaces grouped under org sub-headings across all orgs (Traverse / Northwind Group / Helix Labs), "You have access to workspaces across multiple organisations…", plus create button. Create permission = org-owner | org-admin | super only. New CSS: `.ll-org-group` / `.ll-org-head` (org sub-headings in the card) + `.ll-empty` no-results message; search now also hides org groups with no visible rows. All five variants verified with headless-Chrome renders. Member workspace counts pluralise correctly now ("1 member").
- 2026-06-17 — Fixed: the prototype devbar was hidden behind the login launch takeover (the `#login-launch` overlay is `position:fixed; z-index:1200` and the devbar sits in normal flow). While the launch screen is open, `body.ll-open .devbar` is now pinned (`position:fixed; bottom:0; z-index:1300`) so it shows above the overlay and stays interactive — the Persona control updates the launch screen live. open()/close() toggle the `ll-open` body class; clicking any devbar screen-nav (`[data-nav]`) button now also closes the takeover.
- 2026-06-17 — Added Organisation Management + Add organisation to System Admin (super-user only, inherited from the existing super-gated System Admin entry). (a) System Admin sidebar reordered + new item: Organisations / Workspaces / Users / Simulations / Characters / Voices (Organisations new, `icon-building-2`, `data-nav="system-admin-organisations"`; Workspaces moved above Users). The "System admin" dropdown entry now lands on `system-admin-organisations`. (b) New `system-admin-organisations` screen mirrors the All Workspaces list exactly — `settings-shell` + `settings-title` "All Organisations" + `table-toolbar` (search + primary "Add organisation" CTA opening `org-new-org-modal`) + `data-table` with columns Name / Owner / Created at / ID / row-actions, seeded with 10 orgs (`#sa-org-tbody`). Registered in `sidebarFor` + two devbar buttons (SA · Organisations nav, SA · Add organisation modal). (c) New `#org-new-org-modal` mirrors the New workspace modal (md dialog, same header/body/footer + button hierarchy + footer-hint validation): fields Organisation name (required) and Default workspace name (required, pre-filled "Default Workspace") with new `.org-modal-field-hint` helper text ("Every organisation needs a workspace to land in. You can rename it later. You'll be added as the organisation owner."). Create button disabled until both fields filled; footer hint cycles "Add organisation name" → "Add a default workspace name" → ''. (d) Submit creates org + its default workspace in one action: prepends a new row (Owner = current super user via `[data-sb-user-name]`, "Just now", generated ID) and shows the existing toast ("Organisation created"), then resets the form. Verified all three surfaces (list, modal, create→row+toast) with headless-Chrome renders.
- 2026-06-17 — System Admin restyle + System summary + System nav entry. (a) The System Admin Organisations and Workspaces lists were converted from `.data-table` to the Organisation-page workspaces list pattern — `.org-workspaces-toolbar` (search + `.org-workspaces-sort` select; Organisations also keeps the primary "Add organisation" CTA) over an `.org-workspaces-card` > `.org-workspaces-list` of `.ws-row` items (bold name + `.ws-row-meta` line + Open link-btn + overflow). Org rows read "N workspaces · Owner X · Created date"; workspace rows read "Created by X · Created date". The add-organisation create flow now prepends a `.ws-row` `<li>` (id `sa-org-list`) instead of a table row; the unused `fakeId()`/ID column were dropped. (b) New `system-admin-summary` screen built from the org-summary primitives (`.org-summary-metrics` + `.org-summary-row2`): four metric cards (Organisations 10, Super users 6, Workspaces 42, Members 318) and a "Recent super-user activity" card listing super-user admin actions. Added "Summary" as the first item in the System Admin sidebar; registered in `sidebarFor`; new devbar "SA · Summary" button. The workspace-dropdown "System admin" entry now lands on the summary. (c) New "System" item added to the workspace sidebar Settings section, above Organisation (`#sb-settings-system`, `icon-shield`, → `system-admin-summary`), gated to super users only via `renderSbSettings` (`isSuper = p.isSuperAdmin`); non-super personas don't see it. Verified all screens + both sidebar states with headless-Chrome renders.
- 2026-06-17 — System Admin › Workspaces list now shows the organisation each workspace belongs to. Added a `.ws-row-org` span as the first item in every `.ws-row-meta` line (e.g. "Northwind Group · Created by Erynn Higgins · Created 18 Feb 2025"), assigned across the 10 seed organisations. New `.ws-row-org` style (medium weight, surface-900) emphasises the org as the grouping attribute. Verified with a headless-Chrome render.
- 2026-06-17 — Made the organisation/workspace card-row lists sortable by name (A–Z / Z–A) and date of creation (newest / oldest): System Admin › Organisations (`#sa-org-list`), System Admin › Workspaces (`#sa-ws-list`), and Organisation settings › Workspaces tab (`#org-ws-list`). Each `.org-workspaces-sort` select now carries `data-sort-target="<listId>"` with four options (`name-asc`/`name-desc`/`created-desc`/`created-asc`); the Organisation-settings select's old Last active/Members options were replaced. New sort engine (`window._applyListSort`, delegated `change` listener, applied to each list on load): name from `.ws-row-name`, created time from a `data-created` ISO attr if present else parsed from the "Created <date>" meta text. Added `data-created` to the four org-settings workspace rows (which show activity, not creation dates). The add-organisation create flow now stamps the new row with `data-created` (today) and re-applies the active org sort so it lands in the correct position. The Workspace settings page has no org/workspace list (its list is Users), so it was out of scope. Verified A–Z + newest-first on SA Workspaces and the org-settings list via headless-Chrome renders.
- 2026-06-17 — Replaced the native sort `<select>` on the three sortable lists (SA Organisations, SA Workspaces, Org-settings Workspaces) with a custom SaaS-style "Sort by" control: a bordered trigger ("⇅ Sort: <current> ⌄", with a focus ring + chevron flip when open) opening a popover `.sort-menu` (card + shadow) listing options under a "SORT BY" label, name/date groups split by a divider, and a primary check on the active option. New `.sort-control`/`.sort-trigger`/`.sort-menu`/`.sort-opt` styles. The control stores its value in `data-value` (vs the select's `.value`); the sort engine was generalised to read `data-value`, plus open/select/outside-click/Escape handlers were added. Selecting an option updates the trigger label, marks the active row, closes the menu, and re-sorts. The add-organisation re-sort hook and init now target `.sort-control[data-sort-target]`. Old `.org-workspaces-sort` select CSS is now unused. Verified closed/open/select states via headless-Chrome renders.
- 2026-06-17 — Added "Filter by organisation" to the System Admin › Workspaces list. New filter control in the toolbar (left of the sort control) reusing the sleek sort-control chrome: trigger "Org: <current> ⌄" (building icon) opening a popover with "All organisations" + each distinct org (built dynamically from the rows' `.ws-row-org`, alphabetised, divider after All, check on active). Marked with `data-filter-target="sa-ws-list"` (vs `data-sort-target`); the shared option-click handler branches to `applyFilter` for filter controls and `applySort` otherwise. `applyFilter` toggles `el.hidden` on rows whose org ≠ selection; added `.ws-row[hidden] { display:none }` (needed because `.ws-row`'s `display:flex` overrides the bare `[hidden]`). Filter + sort compose (sort re-appends all rows incl. hidden; filter just toggles visibility). `.sort-menu` gained `max-height:320px; overflow-y:auto` for the longer org list. Verified open + filtered (Traverse → 4 rows) via headless-Chrome renders.
- 2026-06-17 — Added deep-link URL params so the prototype landing page can target new surfaces: `?login=1` opens the login launch on load; `?persona=<key>` presets the logged-in persona (applied after the screen router so `?screen=system-admin-*&persona=sysadmin` shows with the super-user identity). Wired in the login-launch IIFE. (Companion changes outside the prototype: `vercel-setup/index.html` landing page regrouped into Application / Login landing / Settings / Onboarding sections with new rows — three login-landing role variations + "System settings" (Super user badge) — and a new design doc `features/system-admin/system-admin.design.md` plotting the super user flows, added to `features/README.md`.)
- 2026-06-19 — Revised the per-row actions on the Organisation settings › Workspaces list (`#org-ws-list`, four rows). Removed the "Manage admins" link-button; renamed "Open" → "View"; and converted the overflow ellipsis button into a drawer menu (`.ws-row-menu` reusing the org-profile overflow-menu chrome) with seven items in three groups: Add user / Manage users / View activity log · Rename / Archive · Delete (danger). Each item leads with a Lucide icon (`user-plus`, `users`, `history`, `pencil`, `archive`, `trash-2`). New CSS (`.ws-row-menu-wrap` positioned wrapper, `.ws-row-menu(-item/-divider)`, `is-danger` variant, active-state on the trigger) and a delegated open/close handler (`closeWsRowMenus` — toggles one menu at a time, closes on outside/item click + Escape, syncs `aria-expanded`). Menu lives inside `.ws-row-actions` so existing row-click → View navigation stays suppressed for menu interactions.
- 2026-06-19 — Standardised the overflow/drawer ("kebab") menu trigger to the vertical ellipsis (`icon-ellipsis-vertical`, Lucide `ellipsis-vertical`) across the whole dashboard prototype — swapped all 46 `icon-ellipsis` instances (workspace rows, users-table row menus, profile menus, SA lists). Documented the convention in CLAUDE.md (Iconography), the design-system markdown (§6.2 + inventory), so it applies going forward.
- 2026-06-19 — Added overflow drawer menus + clickable bodies to the four metric cards on all three Summary surfaces (System / Organisation / Workspace), reusing the `.ws-row-menu` chrome inside an absolutely-positioned `.org-summary-metric-menu` (top-right). New CSS: `.org-summary-metric-card` is now `position:relative`; `.is-clickable` cards get a hover border/shadow + pointer; number/label gain `padding-right` to clear the corner button; `.users-table tbody tr[hidden]{display:none}` for role filtering. **Organisation summary** — Workspaces (body→Workspaces tab; menu: Add workspace → `org-new-workspace-modal`), Active members (body→Users·Members; menu: Invite member → `org-invite-member-modal`, Manage members), Pending invites (body→Users·Invited; menu: Review invites), Org admins (body→Users·Members filtered to org admins; menu: Manage org admins). **System summary** — Organisations (body→SA Organisations; menu: Add organisation), Super users (body→SA Users; menu: Manage super users), Workspaces (body→SA Workspaces; menu: View all workspaces), Members (body→SA Users; menu: Manage users). **Workspace summary** — Simulations/Characters/Assessments/Tests get a "Create …" menu each but no body navigation (the asset libraries aren't screens in this admin prototype). JS: delegated body-click → `SUMMARY_NAV` map and menu-item → `SUMMARY_ACTION` map (both ignore clicks inside `.ws-row-menu-wrap`); exposed `window._activateOrgTab`; added a functional role filter (`applyRoleFilter` + exposed `window._activateOrgUsersView`) wired to the Users Role select (new "Org admin" option) that hides member rows by role badge — "Org admin" matches the org-level badge (Org Owner/Org Admin) in the name cell. Verified all three summaries (vertical-ellipsis menus render, Org-admins card → Users·Members showing exactly 3 org-level admins of 10, System Workspaces card → All Workspaces) with no console errors via Chrome.
- 2026-06-19 — Two follow-ups to the summary-card menus. (a) Moved the kebab triggers to the **top-right corner** of each metric card — the earlier `.org-summary-metric-menu { position:absolute }` was losing to `.ws-row-menu-wrap { position:relative }` (defined later, equal specificity), so it sat in normal flow at the bottom-left. Bumped specificity to `.org-summary-metric-card .org-summary-metric-menu` (top:8px / right:8px). (b) The org Users **Filter** button now shows an **active state** when a role filter is applied: tinted (`.is-active` → primary-50 bg + primary border/text), a "1" count badge (`#users-filter-count`), and an inline **× clear** button (`#users-filter-clear-inline`) beside it that removes the filter. All three are driven by `applyRoleFilter` (toggled on for any non-"All roles" value, off when cleared) and kept in sync with the popover's "Clear filters". Verified via Chrome: Org-admins card → filtered members + active Filter chip with "1"; × resets trigger/badge/select and restores all 10 rows. No console errors.
- 2026-06-19 — Fixed: the members-list Filter count chip ("1") stayed visible after the filter was cleared. The `.users-filter-count { display: inline-flex }` author rule was overriding the `hidden` attribute's UA `display:none`, so toggling `hidden` had no visible effect. Added `.users-filter-count[hidden] { display: none }` (specificity beats the base rule). Verified: chip shows when a role filter is active and fully hides (computed display none, width 0) when cleared.
- 2026-06-19 — Wired up the invited-users list actions (Resend / Cancel / Re-invite) on both the Organisation Users › Invited list (`#org-panel-users`) and the Workspace settings Users › Invited list. Each invite `<tr>` now carries `data-invite-row` / `data-invite-email` / `data-invite-state`, the Sent cell is marked `data-invite-sent`, and buttons carry `data-invite-action`. A single delegated handler runs the state machine: **Resend** (pending/expired → Pending, stamps Sent "Just now"), **Cancel** (→ Cancelled, swaps actions to Re-invite), **Re-invite** (cancelled → Pending, stamps "Just now", restores Resend/Cancel). Each rewrites the State cell (dot + label) and Actions cell, and shows a toast (reusing `#ws-switch-toast`): "Invitation resent to …", "Invitation to … cancelled", "Invitation re-sent to …". Also fixed a blocking layout bug surfaced by this: the 325px peek rail (only meaningful for Members) stole half the width and clipped the Invited/Deactivated action columns under `overflow-x:hidden` — added `.users-body.is-peek-hidden .users-peek-rail{display:none}` toggled in `activateUsersSubtab`/`activateWsutSubtab` so the table goes full-width on non-Members sub-tabs. Verified all three transitions (state, action swap, Sent stamp, toast) and the full-width invited table via Chrome; no console errors. Note: count chips ("Invited 3", "Pending invites") count invite records and intentionally don't change on cancel (the row persists as Cancelled, matching the seed pattern).
- 2026-06-19 — Added confirm/info dialogs to the invited-users actions so Resend / Cancel / Re-invite now confirm before running (previously they fired immediately). One reusable modal (`#invite-confirm-modal`, built on the existing `.modal-overlay`/`.modal-card` pattern) is populated per action from an `INVITE_CONFIRM` config: **Resend** → "Resend invitation" + "A new invitation email will be sent to <email>. Their previous invite link will stop working." (primary confirm); **Cancel** → "Cancel invitation" + "<email> will no longer be able to join with their current invite. You can re-invite them at any time." (danger confirm, dismiss relabelled "Keep invitation" to avoid Cancel-cancels-Cancel ambiguity); **Re-invite** → "Re-invite user" + "A fresh invitation email will be sent to <email>." (primary confirm). The email is bolded in the body. Clicking a row action now calls `openInviteConfirm(action,row)` (stashes the pending action); the dialog's confirm button runs `performInviteAction` (the state transition + toast) and closes; dismiss/X clears the pending action without changing anything. Verified via Chrome: Cancel dialog (danger) → confirm flips marcus to Cancelled+Re-invite; Resend dialog (primary, info copy) → dismiss leaves james Expired untouched. No console errors.
- 2026-06-22 — Added a Settings tab to the Organisation screen alongside Summary / Workspaces / Users (both the tab strip and the organisation sidebar nav, `data-org-tab="settings"` → `#org-panel-settings`, `icon-settings`). The panel holds an "Organisation details" `settings-section` (Organisation name text input `#org-settings-name-input` pre-filled "Acme Org", plus an organisation-logo `image-upload`) with a "Save changes" primary button, and a "Billing" section carrying a `COMING` badge + dashed `settings-placeholder` card ("Billing isn't available yet …") reserving the spot for future billing/subscription settings. Wired Save (`#org-settings-save-btn`) to reflect the new name in the org header sub-line and show the "Organisation settings saved" toast. Added `settings` to the deep-link tab allowlist in `navTo` (`?tab=settings`); the existing `activateOrgTab` panel toggle and `syncOrgHeaderActions` (hides New workspace / Invite user on Settings) needed no other changes. New CSS: `.settings-coming-badge`, `.settings-placeholder(-title/-sub)`. Validated statically (extension was disconnected so not yet visually verified in-browser) — structure mirrors the existing tabs exactly.
- 2026-06-22 — Added overline/kicker headings to the Organisation and Workspace page titles. New `.org-page-header__overline` style (uppercase, 11px 600, `--primary-600`, letter-spaced) sits above the `<h1>`. Organisation page: the `<h1>` is now the organisation name ("Acme Org", `#org-page-title`) with an "Organisation" overline above it; the sub-line dropped its redundant "Acme Org · " prefix (now "Billing, workspaces, and the shared user pool that spans them."). The org Settings "Save changes" handler now writes the new name into `#org-page-title` (instead of the sub-line). Workspace settings page: added a "Workspace" overline above the existing `#ws-settings-title` (which still reads "<name> settings"). Both headers share `.org-page-header`, so one style covers both. Not yet visually verified — the Chrome extension is disconnected.
- 2026-06-22 — Added live-presence surfacing to System Admin (dev request). (a) System summary: new "live presence" row at the very top (`.sa-presence-metrics`, two cards) — "Users online" (47) and "Candidates online" (128), each with a green online dot (`.sa-online-dot`, `--success-500` + soft `--success-100` halo) next to the count and the session count as the sublabel ("63 active sessions" / "141 active sessions"), since a single user can hold multiple concurrent sessions. Sits above the existing static metric cards. (b) All Users table (`[data-user-list="system"]`): new "Status" column (added `<th>` + colgroup `<col>`, widths rebalanced) filled by JS with Online (green `status-dot is-active`) / Offline (grey `is-deactivated`); online rows with >1 session show "· N sessions" (`.sa-session-note`). (c) All Workspaces list (`#sa-ws-list`): each row's meta line now leads with live presence "<n> online · <m> sessions" (green `.ws-row-online` + small dot) before the org/creator/created info. Per-row presence is generated by a small deterministic IIFE (seeded by row index, idempotent) — illustrative sample data, not wired to real sessions. New CSS: `.sa-presence-metrics`, `.sa-presence-figure`, `.sa-online-dot(.is-sm)`, `.ws-row-online`, `.sa-session-note`. Kept the session subcounts (they read cleanly as sublabels/notes rather than clutter). Validated statically; not yet visually verified (Chrome extension disconnected).
- 2026-06-22 — Workspace settings page title is now the workspace name (not "<name> settings"), completing the overline pattern: WORKSPACE / "L&D Hub" (parallel to ORGANISATION / Acme Org). Changed the default `#ws-settings-title` to "L&D Hub" and updated `openWorkspaceSettings` to set `titleEl.textContent = wsName` (dropped the " settings" suffix). The subtitle still carries the settings context.
- 2026-06-22 — Reordered the Workspace settings page tabs from Summary · Settings · Users · Integrations to **Summary · Users · Integrations · Settings** (moved Settings to the end). Tab-button order only (`.tabs` strip, `data-tab` panel toggling unchanged — panels are shown/hidden by `data-tab`, so DOM panel order is irrelevant).
- 2026-06-22 — Redesigned the System summary metric cards (all 6) to feel livelier without breaking the system (no gradients, no decorative/coloured shadows — every colour token-derived). New shared `.metric-card` scaffold: a colour-coded 40px rounded icon tile (tint via `color-mix(var(--accent) 12%, surface-0)`, icon in the accent), a bolder 30px figure, and an optional pill `.metric-trend` (is-up success / is-down error / is-flat neutral) on the figure baseline; cards lift 2px on hover and the tile deepens to 18%. Per-card accent identity: Organisations → primary-600 (building-2, "+1"), Super users → cat-purple (shield, "Stable"), Workspaces → cat-sky (layout-grid, "+3"), Members → cat-orange (users, "+24"). The two **live presence** cards (Users / Candidates online — the ones called out as boring) got a distinct treatment: success accent, a `users-round` / `graduation-cap` icon tile, and a "Live" badge with a pulsing ring (`.sa-live-dot::after` scale+fade keyframe, disabled under `prefers-reduced-motion`) replacing the old static green dot. The overflow menu moved from a corner-absolute position into the card's top row beside the icon (`.org-summary-metric-menu` overridden to `position:relative`); clickable-body nav (`data-summary-nav`) and the menu wiring are unchanged. Other summary pages (Organisation/Workspace) are untouched — they don't carry the new `.metric-card` class. Icons verified present in Lucide 0.453.0. Not yet visually verified in-browser (Chrome extension disconnected).
- 2026-06-24 — Workspace settings › Users tab migrated to the tv-* component system (V2→V1 bridge, step 2). (a) The hand-rolled 325px peek rail (empty + member-detail + candidate-detail markup, ~145 lines) was replaced by a single `<tv-peek-rail id="wsut-peek-rail">`; `showWsutMemberPeek`/`showWsutCandPeek`/`hideWsutPeek` rewritten to build a `.data` object (name, email, deactivated, status, role tags echoed from the row's own tv-status-tags, meta dl, quick-action list) and call `rail.show()`/`rail.clear()` instead of poking `[data-wsut-peek-field]` nodes. (b) Member/deactivated row avatars → `<tv-avatar>` (deactivated row uses the greyed `deactivated` variant); invited-state dots (Pending/Expired) → `<tv-status-tag kind="pending|expired">`. (c) Row quick actions now route through a shared `<tv-modal-card id="wsut-modal">`: Add user (invite, email-validated → gated primary), Change role (role select; on save updates the row's role tv-status-tag + re-peeks), Deactivate (danger confirm), Edit profile; Open full profile still navigates to the org user profile. The "Add user" CTA was repointed from the legacy `org-ws-add-user-modal` to the modal-card invite recipe. (d) Peek-rail collapse rule (`is-peek-hidden`, non-members sub-tabs) and the sticky-rail CSS extended to cover `#wsut-peek-rail` alongside the legacy `.users-peek-rail` (org tab still uses the latter — untouched, that's step 3). Verified via headless Chrome: no load errors, row→rail (name + 3 tags + 4 actions), Change-role modal opens with the select, avatars render. Org Users tab + profile peek unchanged.
- 2026-06-24 — Organisation › Users tab migrated to the tv-* component system (V2→V1 bridge, step 3; mirrors step 2). (a) All 12 member/deactivated row avatars → `<tv-avatar>` (deactivated rows use the greyed variant). (b) The hand-rolled org peek rail (empty + detail markup, ~100 lines) replaced by `<tv-peek-rail id="org-peek-rail">`; `renderPeekDetail(user)` rewritten to build a `.data` object (org-role tag, status tag, meta dl incl. one row per workspace membership with its role, quick-action list) and call `rail.show()`; `showPeekDetail`/`hidePeekDetail` now use the rail; Cmd/Ctrl-click → full profile preserved. (c) Peek quick actions route through a new shared `<tv-modal-card id="org-users-modal">`: Edit profile, Add to workspace (workspace + role selects), Change role (org-role select), Deactivate (danger confirm), plus Open full profile (navigates) and Reactivate for deactivated users. (d) Peek-rail collapse + sticky CSS extended to `#org-peek-rail`. The legacy `org-invite-member-modal` / `org-add-to-workspace-modal` / `org-edit-profile-modal` remain for the org header CTAs + profile screen (untouched — profile is step 6); the two dead `open-org-*-peek` delegated handlers are now unreachable (buttons removed) and will be cleaned in step 6. Verified via headless Chrome: row→rail (name, status+role tags, 7 meta rows, 5 actions), Add-to-workspace modal opens with its select, avatars render, no errors. Org table role/status tags were already tv-status-tag (unchanged).
- 2026-06-24 — Settings tabs (Organisation + Workspace) reworked to a view→edit-in-modal model (V2→V1 bridge, step 4; per user direction — supersedes the per-section tv-settings-section footer idea). Each Settings tab now shows a read-only `.settings-view` (labelled values in a bordered `dl`) with a single `Edit` (`tv-button`); Edit opens ONE `tv-modal-card` (size lg) containing all settings, with Save/Cancel pinned to the modal's own footer (not the page). Workspace modal: title (tv-field), profile image (image-upload in a tv-field), grading model (select), grading-variability `tv-toggle`. Org modal: org name (tv-field, keeps id `org-settings-name-input`), logo, and a Billing row shown disabled + COMING. On Save, each modal writes values back into the read-only view and syncs the title (`#ws-settings-title` / `#org-page-title`) + a toast, then closes. The old per-section `org-settings-save-btn` delegated handler is now inert (button removed; the modal replicates its title-sync). Org + workspace are structurally identical. Verified via headless Chrome: both Edit→modal→Save flows update the view and title; billing toggle is disabled; no console errors. Note: the `tv-settings-section` component is currently unused as a result of this decision (left in the library).
- 2026-06-24 — System Admin lists migrated to tv-* (V2→V1 bridge, step 5). (a) All Users table (`[data-user-list="system"]`): the JS-coupled Role text and the inserted Online/Offline status cell are now `tv-status-tag` — Role → `kind="admin"` for Super / `kind="neutral"` for Member; Status → `kind="active" label="Online"` (with the "· N sessions" note kept) / `kind="deactivated" label="Offline"` (done in the existing presence IIFE, idempotent; this is the "careful" JS-coupled conversion flagged in the handoff). (b) New shared `#sa-modal` (`tv-modal-card`, injected once) wired via a delegated click handler: SA Users `.row-menu-btn` → "Manage user" (platform-role select on save + Deactivate danger); SA Organisations / Workspaces `.ws-row-overflow-btn` → "Manage organisation/workspace" (Rename on save + Delete/Archive danger). All actions update the row in place and/or toast, then close. The All-Workspaces per-row presence ("N online · M sessions") was left as-is (it's a presence indicator, not a status chip). Verified via headless Chrome: role + status render as tags, user/org row menus open the modal with the right heading + controls, no console errors.
- 2026-06-24 — Profiles migrated to tv-* (V2→V1 bridge, step 6; the persona-coupled "careful" conversions). (a) user-profile: header role chip + Account-details meta role (`data-profile-role-chip` / `data-profile-meta-role`) → `tv-status-tag`; the 3 static workspace-access table role cells → `tv-status-tag`; header avatar → `tv-avatar`. `populateProfile()` rewritten to drive these by attribute — a legacy `role.cls` → tone-kind map (is-org-owner→owner, is-org-admin/is-admin→admin, is-primary→neutral, is-moderator→moderator, is-viewer→neutral) sets `kind`+`label` on the tag, and `[data-profile-avatar]` tv-avatars get `name` instead of textContent (the edit-modal preview div still gets initials). (b) organisation-user-profile: identity avatar (`data-profile-field="avatar-initials"`) → `tv-avatar` and org-role-badge → `tv-status-tag`; `renderUserProfile()` updated to set the avatar `name` and render the badge as a tag (owner for org-owner, else admin). (c) Removed the two dead `open-org-*-peek` delegated handlers (their buttons went away in step 3; they referenced the removed `#users-peek-detail`). Verified via headless Chrome across the static + live render paths: chips/avatars are tv-* with shadow content, 0 legacy `.role-chip` left on user-profile, org-profile avatar name + role-badge tag populate, no console errors. The welcome-hero role chip (`#welcome-role-chip`) and the invite-modal `org-modal-role-chip` "Default" were intentionally left (out of profile scope). DEFERRED (lower-risk / already-functional, not yet done): profile Edit / Deactivate / Delete / Close-account → `tv-modal-card` (still on the legacy modals + danger buttons), recent-activity → `tv-audit-row`, and the profile status dot → `tv-status-tag`.
- 2026-06-25 — Organisation screen, workspaces list: renamed the first workspace row label from "Compliance" to "Human resources" (visual-edit overlay). Updated the visible `.ws-row-name` plus the row's display-name data attributes (`data-org-ws-name`, `data-org-ws-name-open`) so the View detail panel stays coherent; kept the internal `compliance` id slug (`data-org-ws-id` / `data-org-ws-open`) and left unrelated "Compliance" references on other screens and in JS data untouched.
- 2026-06-26 — Workspace Summary header: renamed the demo workspace title from "David Guetta" to "Nicki Minah" (`.ws-title` + `WORKSPACE_NAME` welcome-hero constant).
- 2026-06-29 — Began migrating the dashboard onto the component library: replaced the page headers + tab strips on the Organisation, Workspace (Settings) and System Admin pages with `<tv-page-header>` and `<tv-tab-group>`/`<tv-tab>`, keeping each page's existing copy. **Workspace**: header (overline "Workspace" / title "L&D Hub" / description) → `tv-page-header#ws-settings-header`; `.tabs` strip → `tv-tab-group#ws-tabs` (Summary/Users/Integrations/Settings). **Organisation**: header → `tv-page-header#org-page-header` with the two CTAs ("New workspace", "Invite user") slotted into `slot="actions"` so their ids/data-attrs/onclick survive; `.org-tabs` strip → `tv-tab-group#org-tabs` (Summary/Workspaces/Users/Settings). **System Admin**: the five `settings-title is-large` h1s (All Users, System summary, System summary v2, All Organisations, All Workspaces) → title-only `tv-page-header`; the v2 summary's live-presence widget is slotted into the header's actions area. Re-wired the existing JS rather than duplicating it: `activateWsSettingsTab`/`activateOrgTab` stay the source of truth but now drive `tabGroup.value` (instead of toggling `.tab`/`.org-tab` classes), and each group's `tv-change` calls back into them; navTo's fallback, the profile-back "restore Users tab", and the org delegated click handler updated accordingly; dynamic title writes (`ws-settings-title`/`org-page-title`) re-pointed to `header.setAttribute('title', …)`. `syncOrgHeaderActions` still toggles the slotted CTAs by id. Also swapped the page's head Lucide CDN link for the vendored embedded `../../fonts/lucide/lucide.css` — this fixed tab/icon tofu (the CDN `@font-face` for "lucide" conflicted with the one the components inject, and failed offline/over file://). Verified via headless interaction tests (tab switching, CTA promotion, title updates) + screenshots of all three pages.
- 2026-06-29 — System Admin: added the page-tabs component to the section and built the 3 missing pages so all 7 side-menu destinations exist. Each SA page now carries a 7-tab `<tv-tab-group class="sa-tabs">` (Summary · Organisations · Workspaces · Users · Simulations · Characters · Voices) below its `tv-page-header`, with `value` preset to that page's tab. A tab click routes through a delegated `tv-change` handler → `navTo('system-admin-' + value)`, reusing the existing screen navigation; the SA sidebar items stay the source-of-truth nav and sync via `setScreen` (added `data-nav` to the previously-placeholder Simulations/Characters/Voices items). Built 3 new screens — `system-admin-simulations`, `system-admin-characters`, `system-admin-voices` — each with the header + 7-tab strip + a dashed placeholder card (icon + title + one-line description), and registered them in the `sidebarFor` map (→ `system-admin` variant). The sidebar is kept and synced (matching the Org/Workspace pattern) rather than removed. Verified headlessly: the strip renders 7 tabs, a tab click navigates to the right screen with the correct active tab + header, and the sidebar item highlights in sync; screenshot confirms layout.
