# New Test Page — UI Change Log

One-line log of UI changes to the `new-test` screen in `prototypes/test-journey/index.html`. Newest at the bottom.

---

- 2026-04-24 — Implemented Group 1 feedback generation settings: split "Results and Feedback" into three subsections (Generation / Delivery / Display); added Tone, Length, Detail Level, and Feedback Event Type selectors; added Custom Prompt and Additional Context textareas with live word counts; renamed master toggle from "Generate report" to "Enable Feedback" (collapses generation fields when off); left-aligned all inputs and toggles under feedback generation; added `role="switch"` + `aria-checked` and keyboard activation for accessibility.
- 2026-04-24 — Restructured pill tabs from two to three: renamed "Settings" → "Test Settings" and "Grading" → "Grading Criteria"; added new "Feedback Settings" tab containing the three feedback subsections; renamed the inner section header "Grading" inside Test Settings to "Grading Variability Detection" to avoid clashing with the new Grading Criteria tab label.
- 2026-04-24 — Moved "Grading Variability Detection" setting out of Test Settings and into Grading Criteria (placed at the top of the "Grading Settings" section); extended the "Enable Feedback" master toggle so it now also hides the Feedback Delivery and Feedback Display subsections alongside the generation settings; updated the off-state notice copy to cover all feedback settings.
- 2026-04-24 — Removed the numeric step indicators (1, 2, 3) from the Test Settings / Grading Criteria / Feedback Settings pill tabs.
- 2026-04-28 — Restructured Feedback Settings → Feedback Generation as an either/or pairing: moved Feedback Event Type to the top, wrapped Tone/Length/Detail Level in a "Standard configuration" card, added an "Or take more control" divider, and placed Custom Prompt directly underneath in its own card with a richer multi-line placeholder example; populating the custom prompt now greys out and disables the standard config selects, highlights the custom card, and shows an inline "Replaced by your custom prompt below" banner.
- 2026-04-28 — Added an "Alternative view — tabbed" comparison card directly below the existing either/or pair (existing pair untouched) that renders Standard configuration and Custom prompt as two tabs inside a single card; populating the alt custom prompt shows an "Overridden" badge on the Standard tab, dims the Standard panel, disables its selects, and rings the card with the primary tint, mirroring the override behaviour of the sequential view for direct comparison.
- 2026-04-28 — Greyed out the Feedback Display subsection (header + all four display option toggles) by wrapping it in a `.feedback-display-greyed` container that applies opacity 0.45, `pointer-events:none`, and `aria-disabled="true"`; also set the inner switches' tabindex to -1 so they're not keyboard-reachable while greyed.
- 2026-04-28 — Group 2 Sub-section A: added "Feedback" third pill-tab to `submission-results`; built split-screen Feedback panel (left: submission doc + grade mini-table, right: feedback content); DRAFT/UNDER REVIEW/APPROVED/FAILED status badge; contenteditable rich-text region with formatting toolbar; Edit/Save draft/Approve action hierarchy with confirmation step; pending-skeleton + failed-with-retry states toggled by demo buttons; `aria-live` region for status announcements; all new styles use design tokens.
- 2026-04-28 — Group 2 Sub-section B: added collapsed "View history" version log panel (toggle in status bar, expandable list with version number / editor / timestamp / AI vs Current tags); added "Revert to original" bar + confirmation alertdialog (visible only when edited version exists, uses `icon-rotate-ccw`, restores AI content and collapses to single-version history on confirm); added Run 1 / Run 2 grade comparison section to left context pane (visible only when variability flagged, highlights mismatched scores in warn colour); extended demo state switcher with "Edited version", "Variability flagged", and "No edits" scenario buttons.
- 2026-04-28 — Group 2 Sub-section C: added "Quality signals" indicator row in the right content pane (below status bar, above history panel) with three pill chips — AI confidence (High/Medium/Low with shield-check/gauge/alert-triangle icon and success/warn/error token colours), Variability flag summary chip (No variability/Variability flagged — distinct from the Run 1/Run 2 comparison table in the left pane), and Review priority (Normal/High/Low with flag icon); chips are always visible in the ready state and hidden during pending/failed states; added "Low confidence", "High variability", and "High priority" demo scenario buttons; "High variability" also activates the left-pane grade comparison; "Ready" and "No edits" buttons reset all indicators to defaults; `aria-live` region reused for chip-change announcements.
- 2026-04-28 — Removed raw-hex `border-color` overrides (`#bbf7d0`, `#fde68a`, `#fca5a5`) from `.fb-confidence-*`, `.fb-variability-flagged`, and `.fb-priority-high` chip states — these violated the tokens-only rule; chips now rely on the base `border: 1px solid transparent` plus their token-driven `-100` fill and `-500/-600` text for visual distinction.
- 2026-04-28 — Removed all 11 demo/scenario buttons from the Feedback tab status bar and absorbed the bottom-right Studio/Candidate journey switcher into a new sticky **`.devbar`** developer-tools bar fixed to the bottom of the viewport (full-width, dark `var(--surface-900)` background, monospace font, 44px tall, intentionally styled to read as tooling rather than product); buttons are grouped under labels View / Feedback status / Feedback scenario / Quality signals, separated by thin dividers; existing `data-view-toggle` and `data-fb-demo` handlers continue to work unchanged. App shell height changed to `calc(100vh - var(--devbar-h))` so the bar never overlaps content. Convention from this point forward: **demo/scenario controls live ONLY in `.devbar`, never on prototype surfaces**.
- 2026-04-28 — Fixed broken asset paths after the file was relocated from `test-journey/` to `prototypes/test-journey/` — updated the `colors_and_type.css` `<link>`, the favicon `<link>`, and both wordmark `<img>` `src` attributes (Studio + Candidate sidebars) to use `../../` instead of `../` so they resolve from the new depth.
- 2026-04-28 — Surfaced "feedback ready for review" in two places on the test-journey prototype: (1) **Test list** — added a new "Feedback" column to the left of "Connections" (160px wide, sortable header) showing a warn-100 `feedback-ready-pill` with "N ready" when `feedbackReady > 0` and a muted em-dash otherwise; seeded `feedbackReady` counts on three test rows (Musango UX 5, Debugging and Enhancing 4, Fundamentals of Web Development 2). (2) **Test summary** — added a `.feedback-ready-banner` (warn-100 background, message-square icon in a white tile, primary "Review feedback" button) between the page header and the summary filters, with `aria-live="polite"`; the button uses `data-goto="submissions-list"` so the existing screen router takes the author to the submissions list. Also added an entry-hash handler on init that reads `?screen=` or `#name` and calls `showScreen()`, so external entry points (e.g. dashboard "Review feedback" buttons) can deep-link straight to a screen.
- 2026-04-29 — Renamed the existing `data-screen="new-test"` to `data-screen="new-test-submission"` (HTML + showScreen call); existing Submission new-test panel contents untouched. Replaced the plain "New" button in the test-list toolbar with a dropdown trigger (`#newTestBtn`) and menu (`#newTestMenu`) with six items — Likert / MCQ / Written / Submission / Simulation / [sep] / Import (Import is a no-op placeholder); menu opens on click, closes on outside-click or selection. Scoped the `data-builder-tab` click handler to the closest `.screen` so multiple builders' tabs no longer cross-fire.
- 2026-04-29 — Built four new per-type new-test settings screens — `new-test-likert`, `new-test-mcq`, `new-test-written`, `new-test-simulation` — each with the test-type's exact tab structure per the screenshots (Likert / MCQ / Written: Settings | Questions | Grading; Simulation: Simulation | Grading). Settings panels mirror each screenshot: Likert has General + Presentation + Proctoring (no Results and Feedback); MCQ and Written add a Results and Feedback section with a Generate report toggle; Simulation has a single General → Simulation Connection select. All form fields are interactive (text, textarea, select, file-upload drop zone, switches, number input). All Questions and Grading panels start in an **empty state** with "No questions" / "No grading categories" placeholders + an "Add Question" / "Add Category" dashed-outline button. Add Question on Likert/MCQ/Written renders a question card with QUESTION/CHOICES sub-tabs, Heading/Text content chips (×) and addable Image/External Link chips (+); Likert Choices uses a numbered option list with Add Option; MCQ/Written Choices uses option cards with a green correct-answer toggle, per-card delete, 0 pts label, and an Add Choice dashed card. Add Category renders a grading category card with collapsible levels, an Add Level button, and per-level percentage / name / description inputs. All "delete last item" actions restore the empty-state placeholder. New shared CSS patterns added: `.q-card`, `.q-subtabs`, `.q-chips`, `.opt-list`, `.choice-grid` / `.choice-card` (with `.is-correct`, `.is-add`), `.grade-cat` / `.grade-cat__level`, `.upload-drop`, `.builder-empty`, `.builder-add-row`, `.new-test-menu` (positions the existing `.menu` shell relative to the New button).
- 2026-04-30 — Added a top-of-column **Regrade submission** button (`#modRegradeBtn`, `.btn.sm.outline` with `icon-refresh-ccw`) inside the Moderation tab on the submission-results screen; placed inside a new `.mod-toolbar` wrapper at the top of the left grid column (above the Grading Category 1 / 2 panels), so the Submission panel on the right grid column does not shift.
- 2026-04-30 — Renamed the **Moderation** result-tab to **Review & Moderation** (uses `&amp;` entity); added a matching **Review & Moderation** `<h3>` section title to the left of the Regrade submission button inside `.mod-toolbar` (now `justify-content: space-between` so title sits left and button sits right).
- 2026-05-18 — Added click-to-toggle collapsible sidebar to both sidebar shells (studio + candidate): `js-sidebar-collapse` class on both collapse buttons wired by a shared IIFE at script-load; `setSidebarCollapsed()` adds `.is-collapsed` to all `.sidebar` elements and `.sidebar-collapsed` to `#app`; collapsed width 64px via CSS (`grid-template-columns` transitions from `240px 1fr` to `64px 1fr` in 150ms cubic-bezier); hidden in collapsed mode: wordmark, workspace name + chevron, `.sb-eyebrow`, `.ni-label`, `.badge`; icon column centred; `panel-left-close` → `panel-left-open` icon flip with `aria-label` + `aria-pressed` updates; `title` attribute added to all nav items as collapsed tooltip; state persisted to `localStorage` under key `traverse.sidebarCollapsed.test-journey`; restored on page load before first paint.
- 2026-05-18 — Fixed broken Lucide icon references that were rendering as empty `<i>` elements: `icon-more-horizontal` → `icon-ellipsis` (7 replacements; row overflow menus), `icon-alert-triangle` → `icon-triangle-alert` (3 replacements), `icon-check-circle` → `icon-circle-check` (2 replacements). All renamed in Lucide between earlier versions and 0.453.0. Backup at `prototypes/test-journey/index.html.pre-icon-rename-backup`.
- 2026-05-19 — Converted devbar tab groups into dropdowns via a runtime IIFE (same payload as dashboard / user-onboarding). The test-journey devbar uses explicit `.devbar__group` wrappers, so the converter picks those up directly: View / Feedback status / Feedback scenario / Quality signals each become a single dropdown trigger. Original `.devbar__btn` elements stay in the DOM (`display: none`) and continue to receive the delegated click. Trigger uses the same `.dev-dd` CSS family (`.devbar` scoped) — primary-500 highlight on active item, dark popover above the devbar.
- 2026-05-28 — Sidebar: added a bottom-pinned user-profile block (`.sb-user-wrap` / `.sb-user`) to both sidebar shells (`data-shell="studio"` and `data-shell="candidate"`). Uses `margin-top: auto` + a 1px `surface-200` top border to anchor at the bottom of the flex column. Clicking opens an above-row `.sb-user-menu` popover (white card, `surface-200` 0.5px border, 10 radius, soft shadow) with two items: **View profile** and **Sign out** (`is-danger` red variant — both no-op for prototype, log to console). New IIFE at the end of the page script handles toggle, click-outside-closes, ESC closes, and the menu-item action handlers. Collapsed-sidebar styling mirrors the dashboard's: avatar-only row, menu pops to the right of the rail. Mirrors the new bottom-user pattern landed in `prototypes/dashboard/` so the navigation matches across both prototypes.
- 2026-05-28 — Devbar no longer overlays the bottom of the viewport. Removed `position: fixed; left: 0; right: 0; bottom: 0; z-index: 100;` from `.devbar` so it sits as a normal static block below the `.app` shell. `.app` height changed from `calc(100vh - var(--devbar-h, 44px))` (which used to reserve room beneath the fixed bar) to `100vh` so the app fills the viewport entirely; the devbar sits below the fold and is reachable by scrolling. The new bottom-pinned `.sb-user-wrap` profile block is now flush with the viewport bottom inside the sidebar instead of being clipped/covered by the floating bar.
- 2026-05-28 — Switched the page shell to a flex-column body so the devbar is always visible without covering the sidebar. `html, body { height: 100%; }`, `body { display: flex; flex-direction: column; min-height: 100vh; }`. `.app` changed from a fixed `height: 100vh` to `flex: 1 1 auto; min-height: 0` so it claims all remaining viewport height above the devbar; `.devbar` got `flex: 0 0 auto` (keeps the `--devbar-h: 44px` natural height) so it always sits at the bottom of the viewport. Both shells always on-screen, no overlay on the sidebar's bottom profile block, and `.content-window` keeps its internal `overflow: auto` so the main column scrolls inside the app rather than the page.
- 2026-06-08 — Rebuilt the test-journey sidebar to match `prototypes/dashboard/`. Replaced the old `.sb-top` / inline-SVG `.nav-item` shells with the dashboard's shell: `.sb-brand` (logo + collapse), `.sb-eyebrow` section labels, `.sb-nav` / `.sb-item` items using Lucide icon-font (`<i class="sb-ico icon-…">`), `surface-50` background + `border-right`, primary-500 active pill. Three variants now share the shell and toggle via `data-sidebar-variant` + `.is-active` (visible) instead of inline `display`: **workspace** (main nav mirroring the dashboard — Summary: Dashboard / Simulations / Assessments / Sequences, Settings: Organisation / Workspace), **studio** (Assessments sub-section — Tests active / Assessments / Rubrics COMING, default landing) and **candidate** (feedback view, static workspace row). Two-level drill follows the dashboard pattern: workspace **Assessments** item (`data-sidebar-goto="studio"` + `data-goto="test-list"`) drills into the studio sub-section; the studio `.sa-back` button ("Assessments", `data-sidebar-goto="workspace"`) returns to the main nav. Added a visual-only workspace dropdown (`#ws-toggle` / `#ws-dropdown`: current-workspace row with `Current` pill, a second workspace, divider, "Workspace settings" action) opened/closed by a new IIFE. `setView()` now calls `setSidebar(variant)` (studio for author view, candidate for feedback) rather than toggling `[data-shell]`; collapse IIFE and bottom `.sb-user-wrap` profile block carried over unchanged. Collapsed-rail rules updated for the new classes (centre `.sb-item` icon, hide labels/eyebrows/badges, `.sa-back` chevron-only, dropdown anchored 220px).
- 2026-06-08 — Sidebar workspace row now names the workspace instead of a person: changed the top `#ws-toggle` row and the candidate variant's static `.sb-workspace` row from "David Guetta" (gradient avatar) to **L&D Hub** with an "LH" initials avatar, matching `prototypes/dashboard/`. Also updated the workspace dropdown's current-workspace `.ws-list-row` name to "L&D Hub". Bottom `.sb-user-wrap` profile block (the logged-in user) left unchanged.
- 2026-06-08 — Tightened the main content gutter: `.main-wrap` padding reduced from `12px 12px 12px 0` to a uniform `6px`, so the `.content-window` sits a few pixels from the page edges on all sides (including a small gap from the sidebar) instead of a thick top/right/bottom margin.
- 2026-06-08 — Submission results screen: added spacing between the top-right action buttons (Download CSV / Submission Details, `.sub-top-actions`) and the test header bar. Increased `.sub-hero` top margin from `16px` to `40px` (`margin: 40px 40px 0`), pushing the gradient hero down so it clears the absolutely-positioned button row instead of sitting tight against it.
- 2026-06-10 — Submission results: reworked the results tabs and added an audit-trail History tab. Renamed the "Review & Moderation" tab (and its in-panel `.mod-toolbar` heading) to just "Moderation", so the tab row reads Results / Moderation / Feedback / History. Added a fourth `data-result-tab="history"` tab (`#historyTab`, `hidden` by default) that only surfaces once changes have been made to the submission. New `data-result-panel="history"` holds a `.audit-trail` of `.audit-item` accordion rows: each row shows a type chip + icon-badge that visually distinguishes a **Grading level** change (indigo `--primary` chip, `icon-sliders-horizontal`) from a **Feedback** change (sky `--info` chip, `icon-message-square`), the editor's initials avatar + name + role, and the date · time. Clicking a row expands it in place (`.is-open`) to reveal a before → after `.audit-diff` (level + score for grading changes, text blocks for feedback changes) — no navigation away. Seeded three demo entries (2 grading-level, 1 feedback). Wired a `data-audit-toggle` accordion handler and a new "Audit history" devbar group (`data-audit-demo` No changes / Has changes) that shows/hides the History tab and falls back to Results if changes are cleared while History is open. All colours/spacing via design tokens.
- 2026-06-10 — Reworked the History tab audit trail into a vertical timeline. Replaced the flat `.audit-trail` stack with `.audit-timeline` of `.tl-item` rows laid out as a 3-column grid (date/time gutter · node rail · change card). The middle `.tl-rail` draws a continuous 2px `--surface-200` connecting line (trimmed at the first/last node so it doesn't overshoot) with a `.tl-node` dot colour-coded by change type — indigo `--primary-500` for grading-level, sky `--info-500` for feedback (each with a tinted ring) — so the node now carries the type colour. The left gutter shows the node's date (`.tl-date`) above its time (`.tl-clock`), right-aligned beside the dot. Cards keep the click-to-expand before→after diff; removed the now-redundant 38px `.audit-type` icon badge and moved its icon inline into the `.audit-tag` chip, and dropped the date/time from the card meta (now lives on the node) leaving just the editor avatar + name + role.
- 2026-06-10 — Simplified the History timeline and grouped it by day. Replaced the per-node date gutter (`.tl-item` 3-col grid) with `.tl-day` groups: each date is now a single larger neutral "main" node (`.tl-day-node`, `--surface-600`) headed by the date label + a change count, and the day's changes hang off it as smaller type-coloured sub-nodes (`.tl-change-node`, indigo `--primary-500` grading / sky `--info-500` feedback) on one continuous spine — so the date no longer repeats at every node. Removed all bordered/stroked containers: dropped the `.audit-item` card border, the bordered accordion, and the boxed before/after `.audit-diff__col` panels. Grading-level changes now show their before→after inline on one line (`.tl-diff`: muted "was" → bold "now" with small score subtext), no expand. Feedback changes use a lightweight borderless `.tl-reveal` toggle ("View change"/"Hide change") that reveals plain Previous / Updated-to text blocks (label + text only, no box). Net effect: far fewer lines/boxes on the page, date shown once per day.
- 2026-06-10 — Reworked the History timeline interaction + layout. The day's changes are now indented in from the date node (`.tl-day-list` `margin-left: 34px`) and laid out side by side as wrapping flex cards (`flex: 1 1 300px; max-width: 380px`) rather than stacked. Increased the gap between date groups from 24px to 44px. Standardised the expand affordance for both change types: replaced the grading inline diff and the feedback "View change" text dropdown with a single eye-icon button (`.tl-eye`, `icon-eye` ↔ `icon-eye-off`, top-right of each change). All change detail — grading before→after `.tl-diff` and feedback Previous/Updated text — now lives inside `.tl-change-detail` and is hidden until the eye is toggled (JS toggles `.tl-change.is-open`, swaps the icon + aria-label). Moved `.audit-target` out of the head row onto its own line under the time + type chip. No bordered containers; main spine + date nodes unchanged.
- 2026-06-10 — Rebuilt the History timeline as a GitHub-style branch graph. The main trunk now runs centred through the day nodes (`.audit-timeline::before` at x=12, `--surface-300`, 2px) and the day nodes were enlarged to 16px (centred on the trunk, z-index above it). Reverted the side-by-side change layout: `.tl-day-list` is `display: block` so each change is on its own row, stacked (`.tl-change + .tl-change` 26px gap). Each change is tapped off the trunk by its own faint, thinner curved branch line — a `.tl-change::before` elbow (`border-left` + `border-bottom` + `border-bottom-left-radius`, 1.5px `--surface-200`) curving from the trunk (x=12) into a smaller 11px type-coloured node (x=40). Content padded to `padding-left: 56px` so it clears the node; eye toggle + hidden detail behaviour unchanged.
- 2026-06-10 — History timeline tweaks: swapped the order in `.tl-change-head` so the type chip (Grading level / Feedback) comes before the time. Replaced the per-change eye toggle (`.tl-eye`, `icon-eye`/`icon-eye-off`) with a rotating down-chevron button (`.tl-toggle`, `icon-chevron-down`) that rotates 180° on `.is-open` via CSS; JS now just toggles `.is-open` + keeps the aria-label in sync.
- 2026-06-10 — History timeline: gave each change a faintly shaded body. Wrapped each line item's content in `.tl-change-body` (`--surface-100`, the inactive-tab surface, 10px radius, no border) so the chevron reads clearly against it; the trunk + branch line + node now sit in a clean white gutter to the left of the shaded card (node border kept `#fff`). Moved the `.tl-toggle` chevron into the card top-right (8/8); open state goes to a white chip on `--primary-700`. Tightened inter-item gap to 14px and nudged the node down (top 24px) to line up with the card's header row. For feedback changes, the Previous / Updated-to blocks now sit side by side in a 2-col grid (`.tl-detail-pair`) instead of stacked.
- 2026-06-10 — History timeline: removed the `max-width` caps on `.audit-timeline` (760px) and `.tl-change` (600px) so each shaded line item now spans the full content width (within the panel's 40px gutters).
- 2026-06-10 — History timeline expanded view: increased the gap between the change meta (who/role line) and the detail from 12px to 18px, and put the detail on a slightly darker panel (`.tl-change-detail` now `--surface-200`, 8px radius, 14/16 padding) nested inside the `--surface-100` card.
- 2026-06-10 — Submissions list: added a "Changes" column + filter to surface which submissions have an audit trail. New column sits between "Submitted (graded)" and "Score": default is a muted "No changes" (`.no-changes`); edited submissions show a primary `.changes-pill` with a history icon, the change count, and small type dots (indigo grading / sky feedback, matching the History tab) plus a title tooltip listing the edited types. Added a `changes` field to the `subs` data (null = no changes; otherwise `{count, types}`) — seeded "bad essay" (3, grading+feedback) and "fundementals of ux design ai" (1, grading); all others null. Added a "Changed only / No changes / All submissions" select (`#changeFilter`) to the submissions toolbar that filters the table via a new `subsChangeFilter` state + re-render, letting admins see only edited submissions.
- 2026-06-10 — Submissions list: made the `.changes-pill` a clickable button (`data-goto-history`, hover to `--primary-100`) that deep-links straight to that submission's History tab — opens `submission-results`, unhides + activates the History tab/panel, and syncs the devbar audit-history state to "Has changes". `stopPropagation` prevents the row's default navigation from also firing.
- 2026-06-10 — Added a "changed submissions" review mode driven from the submissions-list "Changed only" filter. When that filter is active, clicking a changed submission (row or `.changes-pill`) now enters a review session: a full-width sticky banner (`#changedReviewBanner`, `--primary-700` band pinned to the top of the content window) shows "Viewing changed submissions" + position ("1 of N") on the left, and on the right a prev/next click-through ("Next changed submission", disabled at the ends) plus an "Exit change state" button. Each step lands the submission on its History tab, open. Exit clears the filter (`#changeFilter` → All), re-renders the list, and returns to the submissions list. Made the History timeline data-driven: replaced the hard-coded `.audit-timeline` markup with `#auditTimeline` populated by `renderHistory(name)` from a new `auditTrails` model (per-submission days/items), and added `renderChangeItem`. Added per-submission hero updates (`#subHeroName/#subHeroBig/#subHeroPct`) via `setSubmissionHero`. Seeded trails for "bad essay" (3 changes) and "fundementals of ux design ai" (1). Explicit nav (breadcrumbs, view toggles) ends the review session; `.screen[data-screen="submission-results"]` is now `position: relative` so the floating top actions drop below the banner.
- 2026-06-10 — Softened the changed-review banner and added a pill hover tooltip. Banner: moved out of `#contentWindow` to a body-level `position: fixed` bar (top:0, full width, `z-index: 60`, no radius) so it sits on top of the page rather than inside the rounded container; restyled from the solid `--primary-700` band to a subtle white bar (bottom border + soft shadow), smaller 12–13px text, primary-600 history icon. Replaced the "1 of N" position with a count chip (`#changedBannerCount` = total changed submissions). Relabelled the nav button to "Change to next submission" (now wraps around; removed the prev button) and the exit to "Exit". Body gets `changed-review-active` (padding-top: 45px) while the banner shows so content isn't covered. Submissions list: the `.changes-pill` is now wrapped in `.changes-cell` with a dark hover tooltip (`.changes-tip`) that breaks the count down by type with coloured dots — e.g. "2 grading-level changes" (purple) + "1 feedback change" (blue). Changed the `subs.changes` model from `{count, types}` to per-type counts `{grading, feedback}`; total + presence dots derive from it.
- 2026-06-11 — Submissions list: moved the "Changes" column from after "Submitted (graded)" to second-from-last, so the order is now Submission · Groups · Submitted (graded) · Score · Ranking · Variability · Changes · Actions (header + row cells updated to match).
- 2026-06-11 — History tab: grading-level changes now show the moderator's rationale in the expanded view. Added a `rationale` field to each grading item in the `auditTrails` model and rendered it (in `renderChangeItem`) as a white comment card (`.tl-rationale`) beneath the before→after diff — editor avatar + name + a "Rationale for change" tag (message-square-quote icon) + the comment text — making clear it's a comment left by the person who made the change explaining why. Feedback changes are unchanged.
- 2026-06-11 — History tab: added a Changes ⇄ Full history scope toggle and system events. Renamed the heading from "Change history" to "History" and reworded the description. Added a segmented `.audit-scope` toggle (Changes default / Full history) in the head right column. Refactored the `auditTrails` model from per-day arrays to a flat `events` list per submission and added system (default) events with no admin user — Submission received, Submission graded, Regrade requested — marked `kind: 'system'`. `renderHistory` now filters by `historyScope` (changes hides system events; full shows all), groups events by date at render time, and labels the per-day count "changes" or "events" accordingly. New `renderSystemItem` renders system events lighter (transparent `.tl-change-body--system`, grey `--surface-400` node, `.audit-tag--system` chip with an event icon — upload / circle-check / refresh-ccw — and an always-visible detail line, no expand). Added a third "Submission event" legend item shown only in full view, and a `.tl-change-node--system` colour. Default remains the changes-only view.
- 2026-06-11 — Reworked the History tab to a flat-list + side-peek pattern (mirrors the users-list peek), removing the nested card-on-card fills. The panel is now a two-column `.audit-body`: flat clickable timeline rows on the left, a sticky `.audit-peek` detail card on the right (340px). Dropped all row fills — rows are plain text on the timeline with only a hover (`--surface-50`) and selected (`--primary-50`) tint; removed the chevron/eye toggle and the inline `.tl-change-detail`/`.tl-rationale`/`.tl-detail-pair` cards. Clicking a row (whole row is the affordance) selects it and renders its full detail in the peek: type chip + target + who/date, then before→after + "Rationale · {name}" for grading, Previous / Updated-to for feedback, or the event detail for system events. The most recent event auto-selects so the peek isn't empty; an empty state shows otherwise. Refactored render: `renderChangeItem`/`renderSystemItem` → a single `renderEventRow` (summary only, `data-event-index`), plus `renderPeek` + `selectPeek` and a `currentEvents` lookup; the row-click handler replaces the old `data-audit-toggle` accordion handler.
- 2026-06-11 — Added a devbar-toggled alternative "Settings style" History view (default view kept exactly as-is). New devbar group "History view" (Default / Settings style, `data-history-view`) toggles `is-settings` on `#auditWrap` and re-renders. The settings view borrows the dashboard workspace/org-settings + users-table feel: the History area sits on a tinted `--surface-50` background with white elements on top — the timeline becomes a single white list card (trunk/branch/nodes hidden), the date becomes a table group header, and each log row is styled like a users-table row (white, 13/16 padding, bottom divider, hover `--surface-50`, selected `--primary-50`). The detail peek does not auto-select in this view: it starts on the empty state, restyled to mirror the users-list "Peek at a user" prompt (primary-50 icon tile + "Click on an item" title + sub), prompting the user to click a row. Same markup/data as the default view; everything is skinned via the `is-settings` class and a `historyView` state.
- 2026-06-11 — Devbar fix: the prototype-controls bar now wraps to multiple rows (`flex-wrap: wrap`, `min-height` instead of fixed `height`, vertical padding + row-gap; dropped `overflow-x: auto` / `white-space: nowrap`). With six control groups it was overflowing off the right edge on narrower viewports and the rightmost groups (Audit history, History view) were only reachable via a non-obvious horizontal scroll — they now always show.
- 2026-06-11 — Settings-style History view: brought the branching graph back into the white list card. Removed the rules that hid the trunk/branch/nodes in `.is-settings`; restored the 50px left gutter on rows and a 38px left pad on the date header so content/labels clear the nodes. Lifted the trunk (`.audit-timeline::before`) to `z-index: 1` so it stays continuous over the date header bands, and nudged the change node (`top: 22px`) + branch curve to line up with the taller table-style rows. Day node + per-row type-coloured nodes now sit in the gutter beside the users-table-style rows. CSS-only, scoped to `.audit-wrap.is-settings`; default view unchanged.
- 2026-06-11 — Settings-style History view: removed the branching gutter again (reverted the trial). Re-hid the trunk/branch/nodes in `.is-settings`, reset rows to `padding-left: 0` and the date header to `9px 16px`, so it's a clean full-width table-style list with no timeline gutter. Default timeline view keeps its branching graph.
- 2026-06-12 — History tab: replaced the "Changes / Full history" segmented toggle (read as extra tabs under the real tab row) with a single low-chrome text link beside the History heading — "Show full history ›" ⇄ "‹ Show changes only" (`.audit-scope-link`, `#historyScopeToggle`). Same `historyScope` state and system-event reveal + legend behaviour; JS now flips the scope and swaps the link's label/chevron instead of toggling two pill buttons.
- 2026-06-12 — History header refinements (both default + settings-style views): moved the scope toggle inline next to the "History" heading and relabelled it "Show full log" ⇄ "Hide full log" (removed the chevron). Removed the grading/feedback/submission-event legend entirely. Restructured `.audit-head` to mirror the body columns (`.audit-head__main` flex:1 + a 340px `.audit-head__rail` matching the peek, gap matched per view) so the heading + description span the event-list width and stop before the peek rail instead of the full page width.
- 2026-06-12 — History (default timeline view): removed the auto-select of the first event so the peek now starts in its inactive empty "Click on an item" state, matching the settings-style view. `renderHistory` always calls `renderPeek(null)`; a row must be clicked to activate the peek (`selectPeek` still wired to the row-click handler).
- 2026-06-15 — History log "middle-ground" variant (default timeline view): event rows are now bordered, contained, selectable cards (`.tl-change-body` gets a 1px `--surface-200` stroke, 10px radius, white surface, 12/16 padding; hover lifts the surface, selected state intensifies to `--primary-400` border + inset ring). Timestamp moved to the top-right of each card (`.tl-time` `margin-left:auto`) alongside a new subtle expand chevron (`.tl-expand-btn`, `data-expand-toggle`). Cards are collapsed by default and gain an inline detail panel (`.tl-change-detail`) showing the same content as the peek — extracted into a shared `peekBody(ev)` builder. Chevron toggles the inline expand independently; clicking the card body drives the right peek and re-clicking the selected card deselects it (peek returns to empty). Selecting a different card collapses the previous one. System/automated events now use an outlined ghost pill (`.audit-tag--system` transparent bg + border) vs the filled colour pills for grading/feedback. Date-group spacing relaxed to 32px, intra-group card spacing tightened to 8px. Peek empty state copy updated to "Select an event to view details". Settings-style view flattens the new card border and hides the chevron/inline detail (scoped overrides). Default vertical-node timeline + branch graph and the show/hide full log toggle carried forward unchanged.
- 2026-06-15 — Submission-results tab row (Results / Moderation / Feedback / History): added a sliding active-state animation. The active "pill" is now a single `.tabs-pill__thumb` element that animates its `transform: translateX()` + width between tabs (200ms `--ease-std`) instead of each tab toggling its own white background. Scoped via a `.tabs-pill--slide` modifier on the results pill (the five builder/config pills keep the instant active style). The active tab's own background/shadow is suppressed so the moving thumb reads as the active state; tab label colour cross-fades. `positionResultTabThumb()` sizes/positions the thumb and is called on tab click, when the screen becomes active (`showScreen('submission-results')`), when the History tab is revealed, and on window resize; it no-ops while the pill is off-screen. Transition is enabled only after first placement (`.is-ready`) to avoid a slide-in from the left on initial render, and disabled under `prefers-reduced-motion`.
- 2026-06-17 — History audit log: added a modern event-type filter (chip multi-select) in the header, below the description. Renders an "All" chip plus one chip per event type present in the current scope (Grading level, Feedback, Regrade requested, Submission graded, Submission received), each with its type icon and a live count; active chips highlight in `--primary` tint (`.audit-filter`, `.audit-filter__chip`, `data-audit-filter`). Multi-select toggles individual types; "All" clears the filter. Chips derive from the scope set (changes vs full log) so a selected chip never removes itself, the control hides when fewer than two types exist, and selections no longer valid after a scope toggle are dropped automatically. Added a "No events match this filter." empty state (`.audit-empty`). New `eventTypeKey()` / `AUDIT_TYPE_META` / `AUDIT_TYPE_ORDER` helpers; `renderHistory` now filters by scope → type filter, and resets the filter when switching submissions (kept across scope toggles). Works in both the default timeline and settings-style views.
- 2026-06-24 — Grading Criteria tab (new-test-submission): reinstated the previous **Level-based rubric** as the default and demoted the newer rubric variants to the dev bar. (a) The on-page rubric-type switch is now **Level-based rubric | Points-based rubric** (was Competency | Points-based); the Level-based rubric markup (Grading Categories → cat-block with Levels/Settings/Context tabs, level rows, Add grading statement / Add Level / Add Category) was restored verbatim from the committed (HEAD) version into a new `[data-rubric-panel="level"]` and shows by default. (b) The **Competency rubric** (the V2 `.rl-*` level-sheet) is kept intact but default-hidden (`[data-rubric-panel="competency"]`), now reachable only via a new dev-bar group **Rubric (dev): Default (on-page) | Competency rubric**. (c) The **Points-based** rubric's on-page **Current | Enriched (V2)** view switch was removed from the page and moved into a new dev-bar group **Points view: Current | Enriched (V2)**. New JS: `setRubricType()` (level/points on-page + competency via dev bar) and `setPtsView()`, wired through delegated `[data-rubric-dev]` / `[data-pts-view-dev]` handlers (delegation chosen so they survive the dev bar's group→dropdown rebuild). No code was deleted — the Competency sheet and both Points views remain. Verified via headless Chrome: default = Level-based; on-page Level↔Points; dev-bar Competency show/restore; dev-bar Points Current↔Enriched; no console errors.
- 2026-06-24 — Matched the test prototype's page chrome to the dashboard org/workspace settings pages. (a) Removed the white page-container card: `.content-window` (the single wrapper around all test screens) is now `background:transparent; border:none; border-radius:0` — content sits directly on the app surface (`var(--surface-50)`), white reserved for input fields / content cards (unchanged). Applies to every test screen. (b) Matched the header text settings to the dashboard (`.org-page-header`): `.builder-title h1` 700/30 → **600 28px/1.2 -0.01em**; the builder crumb now doubles as the overline kicker (`.builder-header .crumbs` → 600 11px, `--primary-600`, uppercase, 0.06em letter-spacing) matching `.org-page-header__overline`; added a `.builder-desc` description line (400 14px/1.5 `--surface-500`, matching `.org-page-header .sub`) under the title on all 5 test-builder pages, with placeholder copy "Configure the settings, grading criteria and feedback for this test." Result: test builder pages now read overline → title → description → tabs on the surface, identical to the dashboard settings pages. Verified via headless Chrome screenshots (test builder + dashboard workspace settings + test-list). Note: description copy is placeholder — easy to tailor per page/test-type.
- 2026-06-24 — Grading Criteria tab (new-test-submission) rubric header polish. (a) Increased the gap between the page tabs and the rubric-type selector: `.rubric-switch` margin `margin-bottom:20px` → `margin:32px 0 24px`. (b) Made both rubric-type headers use one consistent header+description UI, with the Points-based heading as the source of truth: replaced the Level-based panel's `.section-header` ("Grading Categories") with the points heading pattern — `.pts-rubric-hd` > `.pts-rubric-title` "Level-based rubric" + `.pts-rubric-desc` description ("Each category is graded across achievement levels… grading statements describe what a candidate must demonstrate to reach each level."). Points-based header unchanged (title + desc already present). Verified via headless Chrome (grading tab driven in an iframe): switch margin-top 32px, level header now uses .pts-rubric-title/.pts-rubric-desc with a description, old section-header removed, points header intact.
- 2026-06-24 — Grading Criteria editing upgrades (new-test-submission). LEVEL-BASED RUBRIC: made all text inline-editable via a `.led` click-to-edit pattern (hover shows the field, click edits, blur trims+saves, Enter saves single-line, Esc reverts) — category name + description, each level's name + description, and the achievement level name; a new self-contained script also wires the previously-static actions: "Add grading statement" (appends an editable **textarea** row, reusing the `.pts-stmt`/`.pts-stmt-text` chrome), "Add Level" (clones an editable level row; trash removes, min one kept), and "Add Category" (clones an editable category block). Updated the level-rubric description to state categories AND levels are fully customizable (not fixed to "not achieved → achieved"). POINTS-BASED RUBRIC: grading statements and sub-question text changed from single-line inputs to **textareas** (rows=2, vertical-resize, min-height) in both the Current and Enriched editors (`stmtRow`/`subBlock` in both IIFEs); `.pts-stmt` now top-aligns its controls. Sub-questions are now visually distinct from their statements: `.pts-sub-block` gets a `--surface-50` fill + 3px `--primary-400` left accent, the sub-question textarea sits in a head with a bottom divider, and the statements below carry an uppercase "Grading statements" label and an indented left-bordered group. Verified via headless Chrome (grading tab driven in an iframe): level names editable, add statement/level/category work, points statement+sub textareas present, sub-block accent = 3px, statements label present.
- 2026-06-24 — Points-based rubric · question config modal updates (both Current + Enriched editors). (a) Modal heading is now "Edit question · Q{#}" (was "Q{#} · edit question"). (b) The question type is shown as a status tag in the modal header via the tv-modal-card marker (marker-kind=neutral, marker-label=q.type), next to the heading. (c) Marking guidelines moved from question-level to sub-question level: when a question has sub-questions, each sub-block now carries its own tab strip — Grading statements / Model answer / Common mistakes — with the model-answer and common-mistakes textareas living per sub-question (data-submodel / data-submist → sub.guideModel / sub.guideMistakes); the question-level marking guide is suppressed when sub-questions exist, and retained for questions without sub-questions. New CSS .pts-sub-tabs/.pts-sub-tab/.pts-sub-panel. Verified via headless Chrome (current editor, which has the sub-question sample): heading "Edit question · Q1", type marker "Text" rendered in the modal shadow, 6 sub-tabs across 2 sub-questions, per-sub model/mistakes textareas, question-level guide hidden for sub questions + present for plain questions, sub-tab switching toggles panels.
- 2026-06-24 — Rubric description + level-total polish (new-test-submission grading). (a) Replaced the rubric description copy — Points-based: "Define grading statements worth fixed points; the grader checks each one against the submission as met or not met, and the points roll up to a question and test total. Best for criterion-referenced marking where specific things either appear in the answer or don't." (Current view); Level-based: "Score each category against worded performance levels (e.g. Not yet → Strong), with points allocated per level. Best for holistic, quality-of-response judgements rather than a checklist." (b) The Level-based rubric now shows a "Total N pts" on the top-right, aligned to the heading, mirroring the Points-based rubric (`#lvlGrandTotal` added to its `.pts-rubric-hd`); a `recalcLevelTotal()` in the level IIFE sums each category's highest level points and updates live on level-points input + add/remove level/category. (c) Widened the rubric-description container `.pts-rubric-desc` max-width 540px → 680px (a little wider, not full width). Verified via headless Chrome: both new descriptions in place, desc max-width 680px, level total in the header (space-between) recomputing 0→3 pts when a level's points change.
