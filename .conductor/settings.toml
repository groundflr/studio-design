"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
run_mode = "concurrent"

[scripts.run.prototype]
command = "python3 -m http.server $CONDUCTOR_PORT"
default = true
icon = "globe"
