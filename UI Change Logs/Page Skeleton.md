# Page Skeleton — change log

- 2026-08-10 — Design-readiness system: page marked `ready` via `data-status` on `<body>` (design-facing pattern page, deliberately excluded from the launchpad index — empty `#prototype-manifest`), and placed `<tv-status-ribbon>` as the canonical in-prototype status pattern (top-right ribbon; `?chrome=0` / ctrlbar "Hide status" toggle). No visual change to the skeleton itself.
