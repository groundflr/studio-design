# Component Library — change log

- 2026-06-22 — Created `component-library/`: browsable gallery (`index.html`), canonical `registry.json`, one-line loader (`tv-components.js`), and three web components — `tv-status-tag`, `tv-button`, `tv-modal-card` — built on `colors_and_type.css` tokens. Added reuse rule to `CLAUDE.md`. Seeded registry with planned components (tv-diff, tv-field, tv-excerpt, tv-toggle, tv-audit-row).
- 2026-06-22 — Switched the component library to the **Geist** typeface, scoped via a new `--tv-font` token (default Geist) with the webfont loaded by `tv-components.js`. Global `--font-sans` (Inter) untouched, so existing prototypes are unaffected. New indigo to follow.
- 2026-06-22 — Built four planned primitives: `tv-diff`, `tv-excerpt`, `tv-field`, `tv-toggle`. Registered in `tv-components.js`, flipped to built in `registry.json` with full specs, added gallery previews. Only `tv-audit-row` (composed) remains planned.
- 2026-06-22 — Built `tv-audit-row` (composed: tv-status-tag + tv-diff; click emits `tv-open` to open the modal card). Registry now 8/8 built — library complete. Gallery preview wires rows to a read-only detail card.
- 2026-06-22 — Extended `tv-status-tag` with four additive kinds for member management: `admin` (indigo role chip), `deactivated`, `cancelled`, `expired` (status dots). Updated registry kind enum + gallery preview. No change to existing kinds.
- 2026-06-22 — Extended `tv-status-tag` with nine more additive kinds for the prototype-wide rollout: roles `owner`/`moderator`/`candidate`, state `removed`, indicator `live`, publication `draft`/`published`/`archived`/`coming`. Registry enum + gallery preview updated (now grouped: roles / member states / publication & indicators). No existing kind changed.
- 2026-06-22 — Added three audit-event kinds (`created`, `promoted`, `revoked`) for activity-feed markers. Registry enum + gallery preview updated. Additive only.
- 2026-06-26 — Added the side-nav, page-header and tabs component families (atoms-first). Navigation: `tv-nav-item` (menu link — default/hover/active/collapsed + optional count), `tv-nav-heading` (group eyebrow), `tv-nav-group` (heading + slotted items), `tv-nav-user` (profile button + View profile/Sign out menu, composes `tv-avatar`), `tv-nav-header` (wordmark + collapse toggle), and the composite `tv-sidebar` (open ⇆ collapsed; animates 240⇆64 and propagates `collapsed` to slotted children; wired to the header's `tv-toggle-collapse`). Page layout: `tv-page-header` (overline/title/description + optional secondary+primary buttons — both/one/none, composes `tv-button`), `tv-tab` (single underline tab, all states + optional icon/count), and `tv-tab-group` (owns active state, emits `tv-change`). All lifted from the dashboard `.sb-*` / `.org-page-header` / `.tab` styling onto tokens, on `--tv-font`. Registry +9 (now 21), gallery previews + two new categories (Navigation, Page layout), loader updated.
- 2026-06-26 — Introduced a central type-role system so tv-* components hold no type literals. Added `--text-{h1,h2,h3,overline,body,meta}-{weight,size,leading,tracking,color}` role tokens to `design-system/colors_and_type.css` (single source per role), and a shared adopted stylesheet built once in `tv-components.js` (`.tv-h1/.tv-h2/.tv-h3/.tv-overline/.tv-body/.tv-meta` → the tokens, exposed via `window.__tvType(shadowRoot)`). Piloted on `tv-page-header`: removed its hardcoded `h1`/`.overline`/`.sub` font declarations and applied the role classes (component now owns layout/spacing only). Verified headlessly — identical render (h1 28px/600), no type literals left in the component, and changing `--text-h1-size` at the root alone re-sizes the rendered h1. Other components still hold literals — propagation to follow.
- 2026-06-26 — Expanded the type-role system to a full SaaS scale (23 roles) ahead of the component rollout: display-lg/sm, h1–h6, body-lg/body/body-strong/body-sm, label/label-strong/label-sm/tag, caption/overline/eyebrow/micro, stat (tabular numerals), mono, link. Tokens in `colors_and_type.css` now alias the `--fs-*` ladder (scale stays single-source); shared adopted sheet in `tv-components.js` carries every `.tv-<role>` class; dropped the interim `meta` role (folded into caption/body). Decisions: scale-pure (no 13px — existing 13px usages retrofit to 12px caption/body-sm), page title h1 = 30px (`fs-3xl`, up from off-scale 28), colour is the one re-pointable axis. Updated `tv-page-header` accordingly (h1 30px; description colour re-pointed inline to `--fg-3`). Documented the role system + colour-override rule in COMPONENTS.md. Verified all 23 roles headlessly (sizes/weights/colours, `--fs-*` alias flow-through, uppercase/mono/tabular, no errors). Component rollout next.
- 2026-06-26 — Added named `button` / `button-sm` type roles that alias `label-strong` / `label-sm` (`--text-button-* : var(--text-label-strong-*)`), so button text has its own self-documenting role but stays single-source until forked. Verified: resolves to 14/600 and 12/600, and redefining `--text-button-size` moves buttons alone (label-strong unaffected). Role count now 25. On button rollout, the component will apply `.tv-button`/`.tv-button-sm` and bump variant colour selectors to `button.primary`/`button.secondary`/… so per-variant colour outranks the role class.
- 2026-06-26 — Type-role rollout batch 1 (nav / tab / page components). Refactored to the shared `.tv-<role>` classes with no type literals: `tv-nav-item` (label→`tv-label`, count→`tv-tag`; colour follows row state via `:host .label{color:inherit}`), `tv-nav-heading` (→`tv-eyebrow`, converged to fg-3/.05em), `tv-nav-user` (name + menu items→`tv-label`, 13→14px; `.mi.danger` colour outranks the role), `tv-tab` (label→`tv-label`, active swaps to `tv-label-strong` for weight while colour inherits primary-600; count→`tv-tag`). `tv-nav-group`/`tv-nav-header`/`tv-sidebar`/`tv-tab-group` had no literals. Each calls `window.__tvType()` after render. Verified all states headlessly (default/active/hover/disabled/danger) — sizes, weights and colours correct, no errors. Established the rollout pattern: role class for type metrics; stateful colour via `:host`-scoped / inline token overrides that outrank the flat role class.
- 2026-06-26 — Type-role rollout batch 2 (the 12 older primitives) — every tv-* component now holds zero type literals. Applied the shared `.tv-<role>` classes (each component calls `window.__tvType()` after render): `tv-button` (→`tv-button`/`tv-button-sm`; variant colour bumped to `button.primary/secondary/danger/text` so it outranks the role), `tv-status-tag` (chip→`tv-tag`; tone colour/bg/border moved inline so the per-tone fg outranks the role colour), `tv-diff` (value pair→`tv-label-sm`; child `.was/.now/.single` keep their colours, unit `.u` keeps opacity only), `tv-excerpt` (label→`tv-eyebrow`, quote→`tv-body-sm`), `tv-field` (label→`tv-eyebrow`, hint→`tv-caption`; `.req` keeps a casing reset; slotted native control styling left as-is), `tv-toggle` (label→`tv-label`, 13→14px), `tv-audit-row` (summary→`tv-body-strong`, meta→`tv-caption`; `.time` keeps mono family, dropped the actor weight bump), `tv-metric-tile` (label→`tv-label-sm`, figure→`tv-stat`, trend→`tv-label-sm` with `:host`-scoped success/flat colour, desc→`tv-caption`), `tv-settings-section` (title→`tv-h5`, subtitle→`tv-caption`), `tv-modal-card` (title→`tv-h6` 14px, subtitle→`tv-eyebrow`), `tv-peek-rail` (name→`tv-h5`, email/empty-sub→`tv-caption`, empty-title→`tv-h6`, meta `dt`→`tv-eyebrow` / `dd`→`tv-body-strong`, quick-actions overline→`tv-eyebrow`, action→`tv-label` with `.action.danger` colour outranking). `tv-avatar` left unchanged — its initials size is intrinsic to the avatar diameter (sm/md/lg), not a text role. Verified all 12 headlessly (25 checks): role metrics correct and the three dynamic-colour cases (status tone, button variant, metric trend) all win over the flat role colour. Note: the metric figure now renders at the single `tv-stat` size (30px) in both full and compact tiles — compact no longer shrinks the number (only its padding); add a `stat-sm` role if a smaller compact figure is wanted.
- 2026-06-26 — Switched all component icons from inlined SVG to the **Lucide font**, so any icon is now selectable by name (`icon="rocket"`) with no per-component source edit. Vendored the font locally at `fonts/lucide/` (lucide.css with cache-bust queries + eot/svg refs stripped, plus woff2/woff/ttf) so icons render fully offline. Added `window.__tvIcons(root)` + `window.__tvIcon(name)` to `tv-components.js`: components render `<i class="icon-NAME">` and link the vendored stylesheet INTO each shadow root (the `.icon-*` class rules don't cross the shadow boundary — only the document-global `@font-face` would). Centralised a small legacy-alias table (`rotate→rotate-cw`, `sliders→sliders-horizontal`, `userplus→user-plus`, `chevron→chevron-up`, `arrow→arrow-up-right`, `up→trending-up`) so existing prototype call sites keep working while any raw Lucide name passes through. Font location overridable via `window.__TV_LUCIDE_HREF__`. Converted all 9 icon-bearing components (tv-tab, tv-button, tv-status-tag, tv-nav-item, tv-nav-user, tv-nav-header, tv-modal-card, tv-metric-tile, tv-peek-rail); icon sizing now via `font-size` on the wrapper, colour via `currentColor`. tv-avatar unaffected (initials, no icon). Verified headlessly across all 9 (12 checks): each `<i>` resolves font-family `lucide` with a real glyph, an arbitrary name not in any old map (`rocket`) renders, and every legacy alias resolves. Updated COMPONENTS.md Icons convention.
- 2026-06-26 — Made the vendored Lucide font load in every context by embedding the woff2 directly into `fonts/lucide/lucide.css` as a base64 data URI (css 72KB → 370KB; no separate font request). This removes the `file://` `@font-face` block, server-root sensitivity, and font-404 caching that were rendering icons as tofu boxes when the gallery was opened outside the repo-root http server. Also added an explicit `<link rel="stylesheet" href="../fonts/lucide/lucide.css">` to `component-library/index.html`'s head so the page is self-evidently icon-enabled and any plain light-DOM `<i class="icon-*">` works there too (components still self-inject into shadow roots). Verified rendering over both http and file://.
- 2026-06-30 — Built two new Navigation components, completing the side-menu family ahead of migrating the dashboard rails: `tv-nav-back` (the settings/admin sub-rail back link — chevron + label, collapses to the chevron, bubbles a native click + `tv-back`; lifted from `.sa-back`) and `tv-nav-workspace` (the workspace picker — trigger row with avatar/name/optional org sub-line → dropdown with a hover/keyboard "Switch workspace" sub-drawer + a `slot="menu"` for persona-conditional actions; the sub-drawer renders a flat list, or for super users an org-grouped, searchable, A–Z-navigable list with scroll-spy; lifted from `.sb-workspace`/`.ws-dropdown`/`.ws-sub-drawer`). Deliberately **presentational** per the agreed split: the component owns the markup, all UI state (open/close, drawer, hover-intent, search query, A–Z rail), sorting and row rendering; the page owns the data (fed via `.workspaces` / `.orgs` properties + `ws-name`/`ws-initials`/`ws-org`/`current`/`multi`/`grouped`/`static` attributes) and the policy, reacting to `tv-workspace-select`. Registered in `tv-components.js`, added full specs to `registry.json` (now 23 built; `tv-nav-workspace` carries a new `properties` block) and gallery previews to `index.html` (normal/super/collapsed picker demos wired with sample data, plus a `propRows` renderer so JS-set properties show). No dashboard markup changed yet — migration of the four `.sidebar` variants to the component family is the next step.
- 2026-06-30 — `tv-nav-workspace`: switched the persona-conditional dropdown actions from a `slot="menu"` to a data-driven `.menuItems` property ([{ id, label, icon?, data? }]). `::slotted()` can't reach a slotted element's descendants, so slotted raw buttons rendered with an unstyled lead icon (smaller, left-aligned) and read as having different padding from "Switch workspace". Rendering the items as real shadow `.menu-item` rows makes them pixel-identical to the switch row, and a click now emits `tv-menu-action` (detail `{ id, data }`) for the page to route. Updated registry (new `.menuItems` property + `tv-menu-action` event, dropped the slot) and the gallery previews (now feed `.menuItems`).

- 2026-06-30 — Fixed `tv-modal-card` vanishing when an observed attribute changed while it was open. `attributeChangedCallback` re-renders the shadow DOM, which rebuilt the overlay without its `.is-open` class — so changing `heading` / `primary-label` / `confirm-disabled` mid-flow made an open modal disappear while the host kept its `open` attribute, leaving `open()` to early-return forever (modal could not be reopened without a full reload). `render()` now re-applies `is-open` to the overlay when the host has the `open` attribute, so attributes can safely change during a multi-step flow. No API change.

- 2026-07-01 — tv-metric-tile: added two opt-in attributes. `accent` (primary | purple | sky | orange | teal | success | info, or a raw colour) renders the icon inside a tinted coloured box, mirroring the v1 metric-card icon tile. `trend-pill` renders the trend as a background pill (up = green, flat = grey, down = red) instead of plain coloured text. Both default off, so existing usages are unchanged. Updated the component doc header and registry.json.

- 2026-07-07 — tv-modal-card: added an optional `cancel-label` attribute (default `Cancel`) so the secondary/dismiss button can carry a custom label — e.g. "Keep invitation" on a destructive cancel-invite confirm. Backward-compatible (all existing usages keep the default "Cancel"). Documented in the component header and registry.json. First consumer: the dashboard invite Resend/Cancel/Re-invite modals.

- 2026-07-07 — tv-modal-card: added an optional `primary-variant` attribute (`primary` | `danger`, default `primary`). Set `danger` when the confirm action itself is destructive (cancel / delete / remove) — the primary button keeps its standard right-hand position but renders red, rather than using the left-pinned `danger-label` (which is now reserved for a *secondary* destructive action alongside a non-destructive primary, e.g. "Revert to AI" next to "Save adjustment"). Documented the convention in the component header, registry.json, and the shared-modal-card-rulebook (§4). Backward-compatible; existing usages default to the indigo primary.

- 2026-07-07 — tv-field: restyled to match the original `.org-modal-*` form fields (the prior Geist/overline styling read too big/cramped in modal forms). Label is now sentence-case 12px/500 `surface-700` (was an uppercase `.tv-eyebrow` overline); inputs/selects/textareas are 13px with 8/10 padding, 0.5px `surface-200` border and 6px radius (were 14px / 9-11 padding / 1px / 8px). Added default inter-field spacing — `:host(:not(:last-child)) { margin-bottom:16px }` — so stacked fields breathe like the old `.org-modal-form` (16px gap) instead of butting together. Applies to every tv-field usage (settings modals, all the form modals).
- 2026-07-07 — tv-modal-card: added an optional `footer-hint` attribute — a bottom-left hint in the footer (`margin-right:auto`, muted 12px), for form modals to show validation/guidance ("Add email address", "Select access level", etc.). Renders only when set; absent on non-form modals. Footer stays `:empty`-hidden when there is neither a hint nor buttons. Documented in the header + registry.

- 2026-07-07 — tv-modal-card: fixed primary/confirm buttons staying disabled after a form became valid. The form modals' init code set `.confirmDisabled` while the component script was still loading (pre-upgrade); the resulting own data-property shadowed the class accessor, so later `confirmDisabled = false` writes never removed the `confirm-disabled` attribute and the button never re-enabled. Added the standard lazy-upgrade pattern — `connectedCallback` now calls `_upgradeProperty('confirmDisabled')` to delete any pre-upgrade own-property and re-apply it through the setter. Verified by simulation (without: attribute stuck present; with: cleared when valid).

- 2026-07-15 — Added **`tv-token-select`** — a multi-select chip input with autocomplete (component-library/components/tv-token-select.js), registered in `tv-components.js` + `registry.json` (status: built) with a 3-mode preview in the gallery. Two modes: `mode="email"` adds validated free-text email tokens (inviting new users); `mode="options"` filters a supplied `.options` list and adds picks as chips (user rows render `tv-avatar` + email). API: `.options` (get/set), `.value`/`.selected` (get), `.clear()`/`.setDisabled()`/`.count()`, `tv-change` event; shadow-DOM, tokens-only, self-contained (works under file://). Handles the codebase's async-upgrade pattern via `_upgradeProperty('options')`. Promoted from an inline factory first shipped in the dashboard invite/add-user modals (2026-07-15) after feedback that all new elements must be real `tv-*` components — the dashboard now consumes `<tv-token-select>` and the inline factory/CSS was removed. In-browser QA pending (extension unavailable this session); component + dashboard scripts pass `node --check`.

- 2026-07-16 — tv-peek-rail: reduced the meta-row value type from `.tv-body-strong` (14px/600) to `.tv-body-sm` (12px/400) — e.g. "Workspace Admin" under "Role in this workspace" — for review. Eyebrow label (`.tv-eyebrow`) left unchanged.

- 2026-07-16 — tv-peek-rail: settled the meta-row value type on `.tv-caption` (12px/500, muted) — replacing the interim `.tv-body-sm`. Eyebrow label unchanged.

- 2026-07-16 — tv-peek-rail: kept the meta-row value at `.tv-caption` size/weight (12px/500) but overrode the colour to body text (`--text-body-color`, not the muted caption grey) via a `.meta dd` colour rule.

- 2026-07-16 — Added **`tv-start-step`** — a guided get-started / onboarding step row (components/tv-start-step.js), registered in tv-components.js + registry.json (built) with a 4-state gallery preview. A numbered row (icon tile · Step N · title · desc · trailing action) with three states carrying availability: `todo` (actionable button), `done` (muted + green check, no button), `locked` (prerequisite unmet — disabled action + lock tile). Composes tv-button; emits `tv-action`. Built for the dashboard empty-workspace get-started path (simulation → assessment → test → invite).

- 2026-07-16 — Added **`tv-create-tile`** — a compact "create X" tile (entity icon · label · plus affordance) for get-started / empty-state hubs (components/tv-create-tile.js), registered in tv-components.js + registry.json (built) with a gallery preview. Emits `tv-action`; supports `disabled`. Built for the workspace-admin first-run hub (building-block + content-piece create launch points).

- 2026-07-16 — Extended **`tv-create-tile`** with `accent` (family-tinted icon box via `color-mix` over `--cat-*`, mirroring tv-metric-tile) and `featured` (flagship treatment: 18/20 padding, 48px icon box, title at `tv-h5`, description always shown). Backward compatible — plain tiles unchanged. Registry attributes + tokens + usage updated; gallery preview now shows a featured Simulation tile above the compact grid. Built for the redesigned first-run hub's flagship create tiles (Simulation / Assessment).

- 2026-07-16 — Added **`tv-path-card`** — a large selectable "choose your path" card (components/tv-path-card.js), registered in tv-components.js + registry.json (built) with a gallery preview. Family-accented icon tile (or a row of sanctioned sim-icon PNG thumbnails via `images`), title, purpose line, and a reflected `selected` state (primary border + `--primary-50` tint + check). Emits `tv-select`; parent owns single-select. Built for the first-run dashboard path chooser (Simulations vs Assessments) driving progressive disclosure.

- 2026-07-16 — Added **`tv-guided-tour`** — a step-through onboarding tour (components/tv-guided-tour.js), registered in tv-components.js + registry.json (built) with a 7-step gallery preview. Walks a `steps` array (tier · icon · accent · title · body · chips · cta) one at a time, top of hierarchy → smallest pieces, with progress dots, Back/Next/Skip and a per-step create CTA. Composes tv-button; emits `tv-action` (step CTA), `tv-step`, `tv-dismiss`; honours `prefers-reduced-motion`. Built for the first-run dashboard's content-model primer (Environment → Simulation/Test/Content → Assessment/Sequence → Templates/Feedback/Review).

- 2026-07-16 — Added **`tv-priority-actions`** — a role-adaptive strip of the most-important actions, ordered by importance (components/tv-priority-actions.js), registered in tv-components.js + registry.json (built) with a 2-role gallery preview. Renders an `.actions` array of {icon, accent, label, desc, value} as cards; the first gets primary emphasis (indigo border + tinted icon), the rest are supporting. Emits `tv-action`; optional `heading` overline; `:host([hidden])` respected. Built for the role-adaptive dashboard header (Admin leads with Configure/Manage users; Member with Create; Moderator with Grade; Viewer with View).

- 2026-07-16 — Reworked **`tv-create-tile`** to a vertical card matching the `tv-priority-actions` pattern (icon tile on top · label · description · corner plus), filling its grid cell so rows stretch full width. The `featured` flagship stays a wider horizontal hero (icon left, description always shown). Unifies the empty-state hub and the role dashboard into one card language. Gallery preview updated (accented vertical tiles, auto-fit grid).
- 2026-07-16 — Removed the default "Your priorities" heading usage from `tv-priority-actions` instances (dashboard + gallery). The `heading` attribute remains optional on the component.

- 2026-07-17 — Added three components for the stepper + working-canvas onboarding layout: **`tv-setup-stepper`** (compact selectable vertical stepper with completed/current/upcoming states + connector line; emits `tv-step-select`), **`tv-build-card`** (a "starting point" card with an abstract token-built placeholder skeleton graphic — workspaces/users/settings/chart/simulation/grading/content/templates variants — icon, title, desc, CTA, `featured`; emits `tv-action`), and **`tv-resource-rail`** (slim resources/tips rail of icon·title·desc·chevron rows + optional emphasised footer; emits `tv-action`). All registered in tv-components.js + registry.json (built) with gallery previews. Built for the redesigned Org Owner landing.

- 2026-07-17 — Added **`tv-launch-tile`** — a launchpad tile (accent icon · title · one-liner · trailing arrow; the whole tile is the action) with `featured` (larger, elevated — core actions) and `tint` (accent-washed background + border — colourful platform-area tiles) variants and a `meta` count label. Elevation via `--shadow-*` + hover lift; emits `tv-action`. Registered + gallery preview. Built to replace flat white cards on the dashboard with layered, colourful launchpad grids.

- 2026-07-17 — Added **`tv-setup-checklist`** — a condensed setup card (one action per line: icon · label · inline progress/status · action button; per-item todo / in-progress (mini bar + %) / done states; emits `tv-action`). Built to sit inside a dismissible welcome banner. Also extended **`tv-build-card`**: new `environment` graphic (abstract "stage" skeleton) and the graphic frame now carries a faint accent wash so each card reads with colour. Registered + gallery previews.

- 2026-07-20 — tv-setup-checklist: action buttons are now equal width via a fixed-width action column (150px) + `tv-button::part(button){width:100%}`, so rows with different CTA labels line up.

- 2026-07-21 — Fixed the "have to double-click tabs/nav to activate" bug at its source in **`tv-tab`** and **`tv-nav-item`**. Both rebuilt their entire shadow DOM (`shadowRoot.innerHTML = …`) on every `active`/`disabled`/`collapsed` toggle, which tore out the `<button>`/`<a>` the user was clicking mid-gesture and dropped the click. Since those states are already driven purely by `:host([active])` / `:host([disabled])` / `:host([collapsed])` CSS, the rebuild was unnecessary: `attributeChangedCallback` now routes state-only attribute changes to a lightweight `_syncState()` (updates `aria-selected` / `aria-current`, `disabled`, and the label weight class in place) and only does a full render for structural attributes (label/icon/count/href). Single source of truth — fixes every page using these components (system-admin tab strip + sidebar, org/workspace tabs, all nav). No visual change.

- 2026-07-21 — Added **`tv-quick-actions`** — a persistent right-rail panel of the role's most-important actions, built to sit in the same 325px sticky slot as `tv-peek-rail`. Titled panel ("Quick actions") of full-width action cards; each card has an accent icon, label, optional description, and an **up-right (↗) arrow** in the top-right corner that nudges toward the corner on hover. `.actions = [{icon,accent,label,desc?,value}]`; emits `tv-action {value}`. Registered in `tv-components.js` + `registry.json`. (Gallery preview tile still to add.)

- 2026-07-23 — Added **`tv-presence-pill`** (Composed, built) — the live-presence pill from the SA summary header: pulsing green dot + online `count` + `label`, with the `sessions` active-session count divider-separated *inside* the same pill. Includes an **empty state** (`count="0"` → static grey dot + muted "No <label>", no session count) for when nobody is online. Reads `--surface-*`/`--success-500` tokens; motion honours `prefers-reduced-motion`. Registered in `tv-components.js` + `registry.json`, preview template (online + empty states) added to `index.html`.
