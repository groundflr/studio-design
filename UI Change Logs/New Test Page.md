# New Test Page — change log

- 2026-06-22 — Converted all five new-test builder tab strips (Test Settings / Grading Criteria / Feedback Settings / Questions etc.) from the rounded **pill** style to the **underline** style (2px primary bottom border on active), matching the org/workspace tabs, and added a Lucide icon to the left of each tab label. `.tabs-underline` standardised (full-bleed removed; icon support added).

- 2026-06-22 — Rebuilt the **competency rubric** in `new-test-submission` as V2's **level-based sheet** (category × level accordion table): expandable level columns (click a collapsed column to open it, collapse to minimise), per-cell grading statements (add/remove), per-level points stepper, "not assessed" per category×level, a level-scale stepper (2–6 levels) with editable level names + descriptions, a catch-all "default floor", a derived weighting bar, and a per-category grading-context popover (Instructions / Scope / Model answer / Feedback / Guidelines). V1-tokenised (`colors_and_type.css`), scoped to the submission screen; the Grading Settings section below is untouched. Replaces the old categories→levels cat-block. NEEDS in-browser QA. Next: bring the points mode up to V2 treatments (required ★, deductions, pass mark, weighting) — points stays question-based (inferred questions).
- 2026-06-22 — Added a **toggle-able "Enriched (V2)" points view** beside the existing Phase 1 points panel (Current | Enriched switch). The current view + its state are preserved untouched; the enriched view brings V2's points treatments onto the same question→statement (inferred-question) model: per-statement **required ★** and **negative points (deductions)**, a derived **weighting bar** (each question's share of the total), and a rubric-level **pass mark**. Its own model + modal (`#ptseEditor`), independent of the current view. Scoped to the submission screen. Needs in-browser QA.
- 2026-06-22 — Added Points-based grading (Phase 1) to the `new-test-submission` builder's Grading Criteria tab. A rubric-type switch (Competency | Points-based) toggles between the existing competency rubric (untouched) and a new flat points-based panel: a question list with running test total, each question opening a `tv-modal-card` editor for question text, type (Text/Table/MCQ/True-False/Formula), grading statements with point values (binary met/not-met), optional sub-questions (statements move to the sub-question when present), and a marking guide (model answer + common mistakes, reference-only). Live rollups: statement → question → test total. Built on `colors_and_type.css` tokens + component library (`tv-modal-card`, `tv-status-tag`, `tv-toggle`). Scoped to the submission screen only; other new-test-* screens unaffected. Deferred to later phases: grading-category roll-up (Open Q4), grading-mode/lifecycle status markers, results/pipeline-status display.
