# Dashboard — change log

- 2026-06-22 — Added Lucide icons to the left of each tab label on the underline tab strips: Organisation tabs (Summary / Workspaces / Users / Settings) and Workspace settings tabs (Summary / Users / Integrations / Settings). CSS: `.tabs .tab` set to inline-flex with gap; icon size rule for `.tabs .tab i` and `.org-tab i`.

- 2026-06-22 — Added a redesigned System summary as a second view (`data-screen-name="system-admin-summary-v2"`), toggled against the original via a new "SA · Summary v2" dev-bar button (registered in `sidebarFor`). Balanced two-column layout: header with a standalone presence block, platform-at-a-glance metrics with contextual descriptions that open a `tv-modal-card` detail on click, a separate Live now rail, recent activity with `tv-status-tag` markers, and quick actions. Original summary untouched. Validated with new `created`/`promoted`/`revoked` audit-event kinds on the activity feed.
- 2026-06-22 — Rolled `<tv-status-tag>` across the remaining static status/role/state chips (workspace-settings + system-admin screens + org billing): role badges (admin/neutral/candidate tiers), member status cells (Active → active, etc.), the billing "COMING" badge, and the "Live" system badges. Transform scoped to the HTML body (before the inline scripts) so JS-coupled chips were untouched. Loader already present. Left for a careful follow-up (JS-coupled / dynamic): `role-chip` (persona- and profile-driven, 6 instances), `org-modal-role-chip` "Default" (4), and the `status-cell`s built in `td.innerHTML` (Online/Offline, 3).
- 2026-06-25 — Organisation screen, workspaces list: renamed the first workspace row label from "Compliance" to "Human resources" (visual-edit overlay). Updated the visible `.ws-row-name` plus the row's display-name data attributes (`data-org-ws-name`, `data-org-ws-name-open`) so the View detail panel stays coherent; kept the internal `compliance` id slug (`data-org-ws-id` / `data-org-ws-open`) and left unrelated "Compliance" references on other screens and in JS data untouched.
- 2026-06-26 — Removed the custom visual-edit "Inspect" overlay entirely (superseded by built-in Cursor functionality): deleted the dev-only loader `<script>` before `</body>`, the `tools/visual-edit/` directory (`edit-mode.js`, `README.md`), the `PLAN-visual-editing.md` / `COWORK-CONTEXT-visual-editing.md` notes, and the three `tools/visual-edit` Bash permissions in `.claude/settings.local.json`. No production markup or styles affected.- 2026-06-30 — Migrated all four sidebar variants (workspace · system-admin · workspace-settings · organisation) from hand-authored `<aside class="sidebar">` markup to the `tv-*` side-menu component family. Each is now a `<tv-sidebar>` composing `tv-nav-header` (brand + collapse toggle) and `tv-nav-user` (profile menu) in its header/footer slots, `tv-nav-item`s in `tv-nav-group`s for the nav, plus `tv-nav-workspace` (workspace variant) and `tv-nav-back` (the three settings/admin variants) slotted into the header. Re-pointed the supporting JS to drive the components instead of the old DOM: collapse now syncs all variants + `.app` grid + localStorage off the bubbled `tv-collapse` event (with a re-entrancy guard, restore gated on `customElements.whenDefined`); active-state, org/workspace-settings tab sync and the org-user-profile highlight toggle the `active` attribute on `tv-nav-item`; the whole workspace-picker block (dropdown / sub-drawer / search / A–Z rail / row building / identity line — ~330 lines) was replaced by `syncWorkspacePicker()` which feeds the persona's data via `.workspaces`/`.orgs` + `ws-name`/`ws-initials`/`ws-org`/`current`/`multi`/`grouped`/`static` and reacts to `tv-workspace-select` (toast + `currentWorkspaceId`) and `tv-menu-action` (routing); persona-conditional Settings items show/hide by toggling `tv-nav-item`/`tv-nav-group` host display; the profile menu now routes off `tv-nav-user`'s `tv-action`. `currentWorkspaceId` retained (read by capability gating + the toast observer). Legacy `.sb-*`/`.ws-*`/`.sa-back` CSS left in place but dead (harmless; the `.ws-switch-toast` + SA `.ws-row*` list styles are still live and untouched). All 7 inline script blocks pass `node --check`; no live references to removed functions remain. Visual/interaction verification still pending (browser extension was unavailable this session).
- 2026-06-30 — Reworked the System summary (v2) screen, which had become visually scattered. Replaced the hand-rolled `.ss2-tile` metrics (a big Members tile + a 3-up sub-tile row) with a clean 4-up row of `<tv-metric-tile>` (the component originally lifted from this very screen) wired through its `tv-select` event; dropped the per-tile descriptions (the click-through detail modal already carries them) so the KPI row reads at a glance. Removed the duplicated live-presence widget from the page header (it repeated the "Live now" figures) and gave the header a description line instead. Reorganised the ragged 2×2 grid into a clear hierarchy: full-width metrics row on top, then a balanced lower section — recent super-user activity (wide) beside a right rail stacking "Live now" + "Quick actions". Unified every panel under one `.ss2-card` treatment with a consistent uppercase section heading, and normalised the ad-hoc font sizes/colours onto tokens. Behaviour (metric → detail modal → navigate, quick-action nav) preserved.
- 2026-06-30 — Fixed persona-driven sidebar visibility broken by the tv-sidebar migration. `closeDropdown()` (called by `setScreen()` during initial load) invoked `el.close()` on `tv-nav-workspace` before the component had upgraded, throwing `TypeError: el.close is not a function` — which aborted the rest of the shared `<script>`, so the persona IIFE below never defined `_setPersona`/`renderSbSettings` and persona switching + the Settings-section show/hide stopped working. Guarded the component-method calls in `closeDropdown()` and `toggleDropdown()` (`typeof el.close === 'function'`) so a pre-upgrade call is a no-op. Verified headlessly (jsdom): no runtime errors, and the Settings group (System/Org/Workspace) + the workspace picker's menu items / multi / static / grouped flags now track the persona correctly across org-admin, member, super-admin and single-workspace-admin.

- 2026-06-30 — Added the "Move workspaces between organisations" admin-superuser feature. Because a user belongs to one org but many workspaces, workspaces move in batches: a `tv-modal-card` flow (#move-ws-modal) steps select → review. Step 1 lists the pool (any workspace not already in the target org) with searchable checkboxes; step 2 shows a moving-workspaces/members summary and a per-affected-user conflict resolver — for each member who also belongs to an unselected workspace, the admin chooses "Bring workspaces" (pull those in too, default) or "Remove access" (member loses them); the moving-workspaces chip set and the primary button count update live. Apply re-homes the workspaces, strips removed access, and toasts via `ws-switch-toast`. Three entry points all call `window._openMoveWorkspaces(targetOrg)`: a new "Move workspaces here" secondary button on the org page header (shown only on the Workspaces tab, via `syncOrgHeaderActions`), a "Move workspaces here" item in the org summary Workspaces-card kebab (both `[data-mvw-open]`, target derived from `#org-page-header` title), and a new SA organisations row menu. The SA organisations row kebab no longer opens the rename modal directly — it now opens a floating Rename · "Move workspaces to org" menu (the SA workspaces kebab keeps its rename/archive modal). Self-contained demo data model; all 8 inline scripts pass `node --check`; flow verified headlessly via jsdom (6-workspace pool, conflict on a shared member, bring→2 / remove→1, back-nav, SA menu items).

- 2026-06-30 — Restricted "Move workspaces" to super users only and tagged it as a super action. All three affordances are now gated on `getPersona().isSuperAdmin`: the org page header "Move workspaces here" button (gated in `syncOrgHeaderActions` alongside the existing workspaces-tab check — so it shows only when super AND on the Workspaces tab), the org summary Workspaces-card kebab item (marked `data-super-only`, toggled by a new `window._syncSuperActions()`), and the SA organisations row menu (inherently super, on the System-admin surface). Persona switching re-evaluates them live via `_syncSuperActions()` + the now-exposed `window._syncOrgHeaderActions()` in `_setPersona`. Org owners and every lower role see none of them; `window._openMoveWorkspaces()` also hard-guards (no-op for non-super). Added a primary-tone `<tv-status-tag icon="shield" label="Super action">` next to each occurrence — beside the header button and inline (right-aligned via `.super-tag`) in both dropdown menu items. Verified headlessly (jsdom): hidden for org-admin (sarah) + org-owner (tobias), visible for super (sysadmin) on the Workspaces tab and hidden on Summary; hard guard blocks open for tobias, allows it for sysadmin; no runtime errors. All 8 inline scripts pass `node --check`.

- 2026-06-30 — Reworked the Move-workspaces feature per feedback. (1) The "Super action" tag no longer floats among the org-page header actions — it now sits inside the "Move workspaces" button itself (and inline, right-aligned, in every menu-item occurrence). (2) Renamed "Move workspaces here" → "Move workspaces" everywhere. (3) Added a super-only "Move workspaces" item to each workspace row's kebab on the org Workspaces tab (injected in JS, opens the modal; safe because the row-open handler ignores `.ws-row-actions`). (4) Fixed the SA organisations row kebab menu not appearing: it had borrowed the `.ws-row-menu` class, so the global `closeWsRowMenus(null)` outside-click handler slammed it shut on the same click that opened it — gave it a dedicated `.sa-org-float-menu` class + styles instead. (5) Redesigned the modal to be general (same on any page): heading is now just "Move workspaces" with a generic description; added a From → To organisation selector row (styled as a from/to route using the existing `.org-modal-select` UI) so the source/destination are chosen in the modal rather than implied by the page; entry points just pre-set the destination. Changing From repopulates the workspace list (workspaces belong to the source org) and From/To can't be set equal. (6) The search bar now uses the shared `.search-input` component UI and filters the list in place (no re-render) instead of a bare `<input>`. (7) Fixed the modal breaking on first selection / refusing to reopen — root cause was the `tv-modal-card` re-render bug (see Component Library log); the page also no longer re-renders the whole body on every keystroke. Verified headlessly via jsdom: select-stays-open, in-place search, From-change repopulation with From≠To, conflict review (Grace Hopper, bring→2/remove→1), apply + reopen, SA floating menu opens & survives the click and opens the modal pre-set to that org (added to the org options), and full super-gating (9 `[data-super-only]` affordances hidden for org-owner/​admin). All 8 inline scripts pass `node --check`.

- 2026-06-30 — Reworked the Move-workspaces review step (step 2) per feedback. (1) Conflicts now read as a warning that needs attention: an amber triangle-alert banner ("N members need attention"), amber-bordered conflict cards, and the primary button is disabled until every conflict is explicitly resolved (no silent "bring" default) — resolving a card swaps its amber border for a green "Resolved" tag and re-enables the button once all are done. (2) Removed the auto-added "brought" workspace chips under "Workspaces moving" — that row now shows only the explicitly selected workspaces plus a subtle "Add more workspaces" link button (returns to selection, keeping picks); any workspaces pulled in by a "bring" choice are surfaced as a quiet plain-text note instead of chips. (3) Made the resolution consequences explicit: "Bring" now states the whole workspace moves into the destination org entirely, leaves the source org, and that the member (and everyone in it) join the destination and lose access to the previous org; "Remove access" states the member loses those workspaces (which stay in the source org) and joins the destination with only the selected workspaces. Tokens-only (warn-100/500/600, success-700). Verified via jsdom: warning banner + gating, resolved state + re-enable, chips show selected-only with brought-as-note, both consequence strings, and the add-more link round-trip. All 8 inline scripts pass `node --check`.

- 2026-06-30 — Move-workspaces copy: surfaced the one-organisation-per-user rule that drives the whole flow. The step-1 intro now reads "…and because a user can belong to only one organisation, move related workspaces together so nobody loses access," and the step-2 conflict warning sub-text now reads "Each also belongs to a workspace you’re not moving. A user can belong to only one organisation, so they can’t stay in both — choose what happens to each before continuing." (per-user consequence text already spelled out the org switch).

- 2026-06-30 — Expanded Move-workspaces entry points and added workspace pre-selection. New super-only entry points (all carrying the "Super action" tag): the System Admin Summary "Workspaces" metric-card kebab ("Move workspaces", destination mode); the Workspace settings page-header secondary button ("Move workspace", right of the workspace name, pre-selects the current workspace via #ws-settings-header title); and the SA Workspaces list row kebab — converted from a direct rename/archive modal to a floating dropdown (Rename · Archive · Move workspace) matching the SA Organisations row menu, with Move pre-selecting that row's workspace. The org Workspaces-tab per-row kebab item now also pre-selects its workspace and is labelled "Move workspace" (singular). Reworked the entry API: `window._openMoveWorkspaces(arg)` accepts a destination-org string/`{to}` (move workspaces in) or `{workspace,…}` (pre-select a single workspace, From = its org, To defaults to another org); a new `ensureWorkspace()` registers any workspace the list markup references but the demo data didn't seed. The `[data-mvw-open]` handler reads optional `data-mvw-workspace` / `data-mvw-workspace-current` / `data-mvw-to` to declare intent. Improved modal copy to foreground the rule that drives the flow: the step-2 intro now reads "…A member can belong to only one organisation at a time, so everyone in these workspaces leaves <From> and joins <To>," and the conflict warning now leads with "A member can belong to only one organisation at a time. These members also belong to workspaces you haven't selected to move, so decide for each: bring those workspaces into <To> too, or leave them in <From> (the member loses access)." Verified via jsdom across all entry points (open, pre-selection, From-org derivation, tags present, full super-gating for non-super personas); all 8 inline scripts pass `node --check`.

- 2026-06-30 — Reworked Move-workspaces conflict resolution to two mutually-exclusive choices and removed the "bring" option entirely. For each conflicted member the options are now "Move to <To>" (join the destination with the moving workspaces, removed from the workspaces left behind) or "Keep in <From>" (stay in the source org, removed from the moving workspaces — which still move, just without that member). A member conflict can no longer pull extra workspaces into the move; `finalMoveSet()` is now exactly the selected workspaces and `apply()` drops either the left-behind set ('move') or the moving set ('stay') from the member so they always land in one org. The only way to move more workspaces is to add them in step 1: each conflict's "Also in …" line now carries a subtle inline "Add them to the move" link that pre-checks those workspaces and returns to step 1 so conflicts recompute against the larger selection (the general "Add more workspaces" link remains). Conflicts still gate the primary button until every one is resolved. Updated copy: step-2 intro now says "…a member can belong to only one organisation at a time, so anyone split across a moving and a staying workspace needs a decision below," and the warning sub-text frames the two choices. Added demo member "Omar Farouk" (in Onboarding Academy + Partner Enablement + Leadership 101) so moving Onboarding Academy surfaces a member with multiple left-behind workspaces. Verified via jsdom: two-option cards (no "bring"), multi-left-behind member rendered, gating + resolve, both consequence strings, and the add-to-move round-trip (pre-checks + recomputes to zero conflicts). All 8 inline scripts pass `node --check`.

- 2026-06-30 — Redesigned the Move-workspaces review step (step 2) to cut overwhelm, after a UX critique. Replaced the stack of heavy per-member conflict cards (avatar + meta line + inline "add to the move" link + segmented control + a paragraph of consequence text each, all gated) with a compact conflict table: columns Member · "Also staying in <From>" · After move ([Move|Keep] segmented control), preceded by one short rule statement and a "Move all / Keep all behind" bulk toggle. A sensible default (members move to the destination with their workspaces) is pre-applied to every conflict, so the move is no longer gated on hand-resolving each one; the bulk toggle highlights the shared state and de-activates when rows are mixed. Removed the redundancy flagged in the critique: the one-org rule is now stated once (in the table note) instead of three times, the verbose per-member consequence sentences are gone, and the two stat tiles were dropped for a single summary line ("Moving N workspaces from <From> to <To>"). Removed the inline per-member "Add them to the move" link (the layout the user disliked) and the separate "Add more workspaces" link, replacing both with a single "Edit selection" link back to step 1 (which still recomputes conflicts against the new selection). Added demo member Omar Farouk earlier so the table shows a member with multiple left-behind workspaces. Behaviour preserved: 'move' drops the member's left-behind workspaces, 'stay' drops the moving ones, so everyone lands in exactly one org. Verified via jsdom (default applied + ungated, table rows + multi-left-behind, per-row override, bulk move/keep, mixed-state, apply); all 8 inline scripts pass `node --check`. Note: the old card/banner/consequence CSS rules are now unused but left in place (inert).

- 2026-07-01 — Made loss-of-access explicit in the Move-workspaces conflict table. Each member row now has a compact consequence line under the Member/Also-in/toggle grid that spells out the revocation for the current choice: "Moves to <To> and loses access to <left-behind workspaces>" or "Stays in <From> and loses access to <moving workspaces>". It updates live as the Move/Keep toggle (or bulk toggle) changes, with a circle-alert icon and the words "loses access" plus the workspace names in the warning (amber) colour so the consequence reads at a glance. Restructured the table row (`.mvw-trow` wrapping a `.mvw-tr-main` grid + `.mvw-tr-loss` line; header renamed to `.mvw-thead`). Verified via jsdom (loss text correct for both choices and both members, updates on toggle); all 8 inline scripts pass `node --check`.

- 2026-07-01 — Added a `?mvw` deep-link so the landing page can open the move-workspaces journey directly. `?mvw=1` (or `true`) opens the modal in destination mode into Acme Org (From = Traverse, the conflict-rich demo); `?mvw=<workspace name>` opens it with that workspace pre-selected. The auto-open waits on `customElements.whenDefined('tv-modal-card')` before calling `modal.open()` (the component hasn't upgraded at the point the deep-link block runs, so an unguarded call threw `modal.open is not a function`). Persona is applied earlier by the main IIFE's deep-link block, so pairing with `?persona=sysadmin` clears the super-user guard. Verified via jsdom: `?screen=organisation&persona=sysadmin&mvw=1` auto-opens the visible modal (From Traverse → To Acme Org), `?mvw=Sales%20Training` opens pre-selected, and no `?mvw` leaves it closed — no errors; all 8 inline scripts pass `node --check`.

- 2026-07-01 — System summary (v2): added a "Grading queues" sub-section to the Live now card. Below the users/candidates presence rows, a divided block shows the two queues — Standard and Priority — as small tiles, each with a Waiting count and an In progress count (Standard 12/5, Priority 3/2). Uses `icon-layers` / `icon-zap` and design tokens throughout.

- 2026-07-01 — System summary (v2) redesign. Restructured the layout: the four KPI tiles (Members / Orgs / Workspaces / Super users) moved into the left column above Recent super-user activity as compact tiles, so the right rail (Live now → Quick actions) now rises to the top of the page. Adopted two things liked from v1: coloured icon boxes on the metric tiles and background trend pills. Replaced the hardcoded "+X this week" text with a page-level "View by" filter (This week / This month / Last week / Custom range) that drives the trend-pill values and the metric detail-modal text; selecting Custom reveals a from/to date pair. Redesigned the Live now card as a distinct dark "live monitor" panel: a pulsing live dot, users + candidates shown side by side, and the grading queues (Standard / Priority) below with animated indeterminate progress bars to visualise active processing (priority animates faster; respects prefers-reduced-motion). Supersedes the 2026-07-01 white-card grading-queues entry above.

- 2026-07-01 — System summary (v2): replaced the grading-queue indeterminate sweep animation (too distracting, not informative) with a static determinate composition bar — the whole bar is the queue, the solid accent portion is the share in progress and the faint accent remainder is waiting (Standard 5/17 ≈ 29%, Priority 2/5 = 40%). No bar animation now; the single pulsing live dot remains the "live" cue.

- 2026-07-07 — Replaced the bespoke invite-action confirm modal (`.modal-overlay#invite-confirm-modal`) in the user lists with the `tv-modal-card` component. The single shared modal serves all invite row actions — Resend, Cancel, Re-invite — across both the Organisation users list and the Workspaces users list (delegated handler unchanged). `openInviteConfirm` now sets the card's `heading`/`icon`/`primary-label`/`cancel-label` per action. All three use the component's standard footer formation — confirm on the primary (right) button via `tv-confirm`, dismiss on the secondary (left) button nudged beside it: Resend → "Resend invitation" / "Cancel"; Cancel → "Cancel invitation" / "Keep invitation" (the "Keep invitation" dismiss uses the new `cancel-label`); Re-invite → "Send invitation" / "Cancel". No danger button (avoids the full-width destructive-left layout). Icons: resend/reinvite = send, cancel = circle-x. Removed the old `invite-confirm-title/-btn/-dismiss` markup and its click handlers. Member-row actions (manage role, deactivate, edit profile) already used `tv-modal-card`, so all user-list action modals are now on the component.

- 2026-07-07 — Applied the destructive-primary convention to the user-list modals. The invite Cancel confirm now renders its primary (right) button "Cancel invitation" in red via `primary-variant="danger"` (secondary "Keep invitation" stays to its left) instead of the plain indigo primary. Migrated the org "Deactivate user" and workspace "Deactivate member" confirms from the left-pinned `danger-label` button to the same red primary formation (`primary: 'Deactivate …', primaryVariant: 'danger'`), moving their handlers from `tv-danger` to `tv-confirm`. The System-admin "Manage user" modal keeps `danger-label` for its Deactivate action because there the primary action is the non-destructive "Save role" (Deactivate is a secondary action). Going forward, any modal whose primary action is cancellation / deletion / removal uses `primary-variant="danger"`.

- 2026-07-07 — Migrated all remaining form/creation-flow modals to the `tv-modal-card` component. Converted the 4 System-admin `.modal-overlay` modals (Add users, Invite user, Add to workspace, Edit profile) and the 7 Organisation `.org-modal-backdrop` modals (Invite user, Workspace · Add user, Add to workspace, New workspace, Add organisation, Edit profile, Close account). Each keeps its existing form body (the `.org-modal-*` / `.modal-body` field markup slots in unchanged), while the bespoke backdrop + header + footer shells are replaced by the component's — so all modals now share one shell, focus-trap, esc/overlay close, and footer grammar. Made `openModal`/`openOrgModal`/`closeOrgModal` tv-modal-card-aware (detect the tag → `.open()`/`.close()`), so triggers were untouched. Rewired each modal's confirm from a bespoke button + click handler to the component's primary button + `tv-confirm` event, and each modal's validation (previously toggling `aria-disabled` on the send/create button) to the component's `confirmDisabled` property (email-present, name+workspace, workspace-name, org-name+default-ws, and typed-name-matches gates all preserved). The Close-account modal's destructive confirm uses the `primary-variant="danger"` convention (red primary "Close account", gated until the typed name matches). Add-to-workspace title name injection now sets the card `heading`; the SA add-to-workspace typeahead retargets to `confirmDisabled` and resets on `tv-close`. Footer status hints ("Add email address" etc.) were dropped — the disabled primary conveys the gate — matching the existing tv-modal-card form pattern. Legacy `.org-modal-*` / `.modal-*` CSS is left in place but now largely dead (harmless). Note: org modals were 480px and now use the component's md/sm widths (600/440). All inline scripts pass `node --check`; validation gating verified by simulation; in-browser QA still pending (extension unavailable this session).

- 2026-07-07 — Follow-up to the modal migration (component-level, per feedback). Brought back the footer action hints on the converted org form modals via the new tv-modal-card `footer-hint` attribute: Invite user ("Add email address" → "Select access level"), Workspace · Add user ("Add email address"), New workspace ("Add workspace name"), Add organisation ("Add organisation name" → "Add a default workspace name") — each driven live from the modal's existing validation (guarded so it only re-renders on change). Restored the 4 role tiles (the role explanations) in the Workspace users → Add user modal (`wsutModal` 'invite'): the role `<select>` is replaced by the shared `.org-modal-role-grid` 4-card grid via a new `wsutRoleTilesHtml()` helper + a radio-style selection handler on the modal body, and that action now opens at `size="md"` so the 2-column grid isn't cramped. Field styling/spacing across all these modals now comes from the updated `tv-field` (matches the original `.org-modal-input` look). No rollback needed — the org forms the conversion had preserved keep their layout, now with hints back.

- 2026-07-15 — Invite architecture refactor (workspace side + shared component). Added a shared **multi-select token component** — `createTokenSelect` factory + `.token-select`/`.token-chip` CSS + `ORG_USERS`/`ORG_WORKSPACES` data — reused by every invite/add-user modal; two modes: free-text **email** tokens and **options** (existing users / workspaces) with autocomplete chips. **Workspace settings · Add users** modal: free-text email → search-existing-org-users multi-select chips (workspace admins can no longer invite brand-new emails), single role applied to all selected users, **removed Personal note**. Workspace **Invited** sub-tab rebuilt as **read-only** (org invites in flight that include this workspace; managed at org level) — dropped Resend/Cancel, added an explainer line. Upgraded the two legacy System-admin stub modals in place: **Invite user** → org-level multi-email + multi-workspace + org-admin invite; **Add users to workspace** → existing-org-user multi-select. Decision: token component built as an inline factory (matching the existing single-select `typeahead` precedent) rather than a registered `tv-*` web component — flag for promotion if it should live in the component library. Inline scripts pass `node --check`; in-browser QA pending (extension unavailable).

- 2026-07-15 — Fixed the **real** Workspace settings · Add user modal. The workspace Users "Add user" button opens the JS-rendered `#wsut-modal` (built by `wsutSetModal`/`openWsutModal`), which still had the old free-text email `<input>` + personal note — the earlier refactor had only updated the separate devbar `#org-ws-add-user-modal`. Rebuilt the 'invite' action to use `<tv-token-select mode="options" id="wsut-inv-users">`: multi-select existing organisation users as chips, searchable by name or email, comma-separated, fed from `window._orgUsers` (excluding org owner/admins, who already belong to every workspace). Heading/primary → "Add users"; removed the personal note; primary gated on ≥1 selected user via `tv-change`; confirm toasts the count ("N users added to this workspace"). The legacy `#wsut-inv-email` input-gating handler now bails when the field is absent. Also added **comma-to-commit** to `tv-token-select` options mode (comma adds the active/first match), so the component supports comma separation in both modes. Scripts pass `node --check`; in-browser QA pending (extension unavailable).

- 2026-07-16 — First-run dashboard body: added the **empty-workspace get-started** state. New `.ws-getstarted` section in `data-screen-name="dashboard"` — heading + "0 of 4 complete" progress pill + a guided path built from four `<tv-start-step>` rows (1 Create a simulation · primary → 2 Create an assessment → 3 Build a test · locked → 4 Invite candidates · locked), reflecting the real content model (a test needs a simulation + assessment first). New shell class `.dashboard-shell.is-empty-ws` shows the get-started and hides the populated `.dashboard-grid`, `.action-card`, and the fake summary counts (`.ws-pill`); `renderWelcome()` toggles it for `role === 'empty'`, so it persists after the welcome banner is dismissed. Step CTAs emit `tv-action` (logged for now — no builder in this prototype). Reworded the empty welcome-hero copy from "[Admin] can create content" to an author-appropriate greeting, since the member now has the create actions. Reachable via the devbar Welcome-state → **Empty** button. Scripts pass `node --check`; in-browser QA pending (extension unavailable). Groundwork for the wider role×availability first-run matrix (Moderator/Viewer role-scoped bodies still to design).

- 2026-07-16 — First-run dashboard body for the **workspace-admin** role (compact hub). New `.ws-admin-setup` section shown via `.dashboard-shell.is-admin-setup` (hides the populated grid / action-card / summary counts). Contents: a top-right **Add user** secondary action (`tv-button`, opens the existing add-existing-users modal — deliberately not a primary CTA); a primary **Configure this workspace** launch point (`tv-start-step`, opens Workspace settings via the sidebar nav item); and two tile groups built from the new `tv-create-tile` — **Workspace building blocks** (Environment · Template · Character) and **Create content** (Assessment · Test · Simulation · Sequence). Encodes the two-tier model (workspace-level vs content-piece) that permeates all roles; per the confirmed "independent, linked" hierarchy, assessment and test are peers with no locking. Wired to a new welcome-state `admin-empty` (ROLE_LABELS/HERO_COPY added; `renderWelcome` toggles `.is-admin-setup`); reachable via the devbar Welcome-state → **Workspace admin · new**. Create tiles log intent (no builders in this prototype). Scripts pass `node --check`; in-browser QA pending (extension unavailable).

- 2026-07-16 — Reworked the **Member** first-run to match the workspace-admin hub model, and **unified both into one `#ws-firstrun` section** (replaces the separate `#ws-getstarted` linear path and `#ws-admin-setup` hub, so the two roles can't drift). The old Member guided path (simulation → assessment → test → invite with **locked** steps) is gone — it implied a hard dependency that contradicts the confirmed "independent, linked" hierarchy. Both roles now see the same compact hub: two-tier create tiles — **Workspace building blocks** (Environment · Template · Character) + **Create content** (Assessment · Test · Simulation · Sequence), no locking. Role differences are handled in `renderWelcome`: the **Configure this workspace** primary step and top-right **Add user** action show for Workspace admin (`admin-empty`) and are hidden for Member (`empty`); the heading/subcopy swap per role ("Finish setting up …" vs "Get started"). One shell class `.is-firstrun` drives both (replaces `.is-empty-ws` / `.is-admin-setup`). Reachable via devbar Welcome-state → **Empty** (Member) or **Workspace admin · new** (admin). Assumption to confirm: Members are shown building-block creation too (they "author any content") — easy to restrict to content-only if building blocks are admin-managed. Scripts pass `node --check`; in-browser QA pending (extension unavailable).

- 2026-07-16 — Redesigned the first-run hub (`#ws-firstrun`, Member + Workspace admin empty workspace) from a flat two-group create-tile grid to a "Spotlight + path chooser" layout: admin-only "Start here" configure spotlight (overline + `tv-start-step`), then a hero path chooser ("What are you setting up?") of two `tv-path-card`s — Simulations (sky, with sim-icon PNG thumbnails) vs Assessments (pink). Picking a card progressively discloses its steps: a featured flagship `tv-create-tile` (Simulation / Assessment, accent + description) plus supporting tiles (Environment / Character / Template under Simulations; Test / Sequence under Assessments), each carrying purpose copy. Hierarchy now reads path > flagship > building block. Selection resets on every state change; reveal panels fade in honouring `prefers-reduced-motion`. Also fixed a latent bug where the title element's id (`firstrun-h`) didn't match the id `renderWelcome` looked up (`firstrun-title`).

- 2026-07-16 — Realigned the first-run hub to the canonical content hierarchy (from product-assets/documentation/Traverse Studio Feature Exploration.md). Replaced the Simulations-vs-Assessments path chooser + reveals with three top-to-bottom tiers that teach the model by their shape: **Foundation · start here** (ordered `tv-start-step`s — admin Configure workspace, then Set up environment; environment step is #1 for members, #2 for admins), **Build** (featured Simulation `tv-create-tile` + Test and the new Content tile), **Compose** (Assessment + Sequence), plus a cross-cutting "Templates · Feedback · Review" throughline. Corrects the earlier model: Environment/Characters are now the foundation (not simulation building blocks — Characters live inside the environment), Content is a first-class Build piece, and Sequence sits at the Compose tier. Added a **step-through onboarding tour** (`tv-guided-tour`) launched from a "Take the 2-minute tour" line, walking the pillars with per-step create CTAs. `tv-path-card` is no longer used by the hub (kept in the library as a general component).

- 2026-07-16 — Removed the "Add user" button from the first-run hub header (and its click wiring + admin-only toggle reference). The empty-state dashboard no longer shows an add-user CTA for either the member or admin view; adding users stays on the Workspace settings → Users panel.

- 2026-07-16 — Structured the dashboard by role using the Users & Permissions V2 PRD (§1.2 role definitions + §4 permission matrix + §6.1 role-adaptive nav). Added a **role priority strip** (`tv-priority-actions`, #dash-priority) below the workspace header that leads with each role's most-important actions in PRD order: **Workspace Admin** → Configure workspace · Manage users · View reports; **Standard User (Member)** → Create content · Review feedback · View reports; **Moderator** → Grade submissions · Review feedback · View results; **Viewer** → View content · View submissions · View reports. Disallowed actions are omitted (hidden, per §6.1). The strip is suppressed on the first-run hub (itself the structured hierarchy) and the plain no-welcome dashboard; it stays visible in the moderator/viewer takeover so those roles still lead with their action hierarchy. Admin governance actions (Configure/Manage users) route to Workspace settings; the rest log intent. Org Owner / Org Admin are treated as Workspace Admin on the workspace dashboard.

- 2026-07-16 — Removed the "Your priorities" overline from the role priority strip. Aligned the first-run (empty/new) hub with the role-dashboard styling: `tv-create-tile` is now a vertical card matching `tv-priority-actions`, and the `.create-tile-grid` uses `auto-fit` (was `auto-fill`) so short rows of cards stretch to fill the width instead of sitting small and left-aligned. The flagship Simulation stays a full-width horizontal hero above the stretched supporting cards.

- 2026-07-17 — Built the **Org Owner landing** (`#org-firstrun`), the organisation-level analogue of the workspace first-run hub, shown via a new welcome state `?welcome=1&role=org-owner-new` (devbar "Org owner · new"). The owner lands here after accepting the invite: a hero banner ("Welcome to Traverse, … · Org owner"), then guided **org-settings setup** as ordered `tv-start-step`s — Organisation details & branding (routes to Organisation → Settings), Authentication & sign-in (stub), Billing & subscription (locked/coming soon) — followed by a **"What's next?"** three-way fork (`tv-priority-actions`): Edit your workspace (→ Workspace settings), Add a workspace (→ New workspace modal), Invite users (→ Invite member modal). Routing uses the exposed `window._navTo` / `window._openOrgModal`. New `.is-org-firstrun` state hides every workspace-level surface. Added a launchpad row for the state.

- 2026-07-17 — Redesigned the Org Owner landing (`#org-firstrun`) from a simple step list into a stepper + working-canvas layout, drawing from four reference mockups. Now: an in-page **welcome banner** with a progress bar (the floating hero is suppressed for this state); a main body of three columns — a compact **`tv-setup-stepper`** (completed/current/upcoming) | a **working canvas** whose panel swaps per selected step with inline mini-fields (Organisation details & branding: name/logo/brand-colour swatches; Authentication: sign-in method options; Billing: coming-soon) and a "Complete step" that marks done, updates progress and advances | a slim **`tv-resource-rail`** of resources/tips with a "Book a call" footer. Underneath, role-specific **`tv-build-card`** starting points (org-owner focus: Add a workspace ★, Invite users ★, Set up your workspace) with abstract placeholder graphics. Stacks to one column under 1024px. Routing reuses `window._openOrgModal` / `window._navTo`.

- 2026-07-17 — Reworked the Org Owner landing from a setup stepper into a **launchpad** (per feedback that the dashboard felt flat, white and unfocused). Removed the stepper/working-canvas/resource-rail. Now a role launchpad: header → **Start here** core-action tiles (`tv-launch-tile featured`: set up org, add workspace, invite users) → **Explore your organisation** colourful tinted area tiles (Workspaces, Users, Reports, Settings, Billing-coming-soon) → **Pick up where you left off** recent-activity card. Visual layering: grey page → elevated white core tiles (shadow + hover lift) → accent-tinted area tiles → elevated recent card. Routing via `window._navTo` / `window._openOrgModal`. (tv-setup-stepper / tv-build-card / tv-resource-rail remain in the library, now unused by the dashboard.)

- 2026-07-17 — Reworked the Org Owner landing again per feedback. Replaced the launch-tile grid with: a **dismissible welcome banner** (× hides banner + setup together) containing a **condensed setup card** (`tv-setup-checklist` — Set up organisation [in progress 33%], Add your first workspace, Invite your first user, each a one-line row with an action button), followed by a **"Start working in Default Workspace"** section of `tv-build-card`s *with graphics* (Set up the environment, Create a simulation ★, Create an assessment ★, Add a workspace). Removed "Pick up where you left off". The org owner is now prompted to work within the default workspace or add another. (tv-launch-tile now unused by the dashboard.)

- 2026-07-17 — Built the **role × journey-stage dashboard system** per the new strategy doc (`product-assets/documentation/dashboard-role-landing-states.md`): every role now has an empty and an active landing sharing one five-band grammar (Header → Act → Attention → Resume → Monitor). New **Resume band** (`#dash-resume`, `tv-launch-tile` rail — drafts for Workspace admin/Member, continue-grading for Moderator, assigned content for Viewer) and **Monitor band** (`#dash-metrics`, 4-up `tv-metric-tile` row, role-scoped figures), both fed from a new `ROLE_DASH` config via `renderDash()` (mirrors the `ROLE_ACTIONS` pattern so roles can't drift). The **Attention card** (`#action-card`) is now role-scoped and live: feedback queue for admin/member, grading queue ("Grade next") for moderator, hidden for viewer. Workspace inventory (assets card + `ws-pill` counts) is hidden for Moderator/Viewer, whose access is assignment-scoped (PRD §3.3). New **org-active dashboard** (`#org-active`, `.is-org-active`) for `role=org-owner`/`org-admin`: featured core-action tiles (invite/add workspace/reports), tinted workspace cards, org metrics and org activity — billing line owner-only (PRD §5 P7). New **Org admin · new** landing (`role=org-admin-new`) reuses `#org-firstrun` with a per-role checklist (`ORG_CHECKLISTS`: SSO step instead of billing focus) and swapped overline/copy. Devbar + launchpad rows added for all four new states; greeting logic split (new org roles → "Welcome to Traverse", returning → "Welcome back"). Tokens/components only; all 8 inline scripts pass `node --check`; 11 welcome states verified headlessly via jsdom (band visibility, role scoping, billing gating, checklist variants).

- 2026-07-20 — Org owner · new variant tweaks: setup-checklist button "Setup organisation" → "Complete setup"; the three setup buttons are now equal width (fixed action column in tv-setup-checklist). Reworked "Start working in Default Workspace": names the workspace and explains it's already set up; three build cards (environment / simulation / assessment) with the primary emphasis strokes removed from simulation & assessment; split out less-important manage actions (Rename workspace · Add users to workspace as link buttons) and a visually distinct dashed "Add a new workspace" button (no longer a peer build card).

- 2026-07-20 — Rebuilt the Workspace admin · new variant (role=admin-empty) to follow the org-owner-new / org-admin-new launchpad structure. It now uses a new `#wsa-firstrun` (`.ofl`, shown via `.is-wsa-firstrun`) instead of the old `#ws-firstrun` content-hierarchy hub: a dismissible welcome banner ("Welcome to <workspace>, <name> · Workspace admin") with a condensed `tv-setup-checklist` (Configure your workspace [in progress] · Set up your first environment · Invite members), then a "Start building in your workspace" section of `tv-build-card`s (Create a simulation · Create an assessment · Create content) with a less-important "Manage this workspace" row (Workspace settings · Add members). No "add workspace" (workspace admins can't create workspaces). The old `#ws-firstrun` hub is now member-only (role=empty). Routing reuses `oflRoute` (setup-ws/ws-settings → workspace settings; invite-members/ws-add-members → add-users modal; content cards log intent).

- 2026-07-21 — Rebuilt the **System Admin · Users tab** ("All Users") to match the organisation / workspace members tables and gave it peek functionality. Replaced the old plain `data-table` (First/Last/Email/Role/ID + row-menu kebab) with the shared `users-table` two-pane layout: avatar name cell (name + platform-role tag Super/Member + email), an "Organisations & roles" column (`role-line` chips), Online/Offline status (with `· N sessions`), and Last active. Single-click a row opens a `tv-peek-rail` (`#sa-peek-rail`) showing identity, meta (job title, department, member since, last active, sign-in, per-org role) and quick actions (Open full profile → existing profile screen · Change platform role · Deactivate) that open a `tv-modal-card` (`#sa-users-modal`); Cmd/Ctrl-click opens the full profile directly. Added a right-aligned action row with search (name/email), a Filter popover (Platform role + Status) with active-count badge + inline clear, and checkbox selection driving a fixed bulk-action bar (`#sa-users-bulk-bar`). Data is a self-contained `SA_USERS` model (13 users) rendered by a scoped IIFE; reuses the outer `navigateToUserProfile`. `#sa-peek-rail` added to the shared peek-rail width/sticky + `is-peek-hidden` CSS. The old `[data-user-list="system"]` enrichment / row-menu / row-click handlers now simply no-op (table removed).

- 2026-07-21 — Refined the System Admin · Users table per feedback: (1) removed the platform-role tag from the name cell (just name over email now); (2) split "Organisations & roles" into separate **Organisation** and **Role** columns that stay aligned row-for-row via a shared `.sa-orgrole` / `.sa-orgrole-line` rhythm (e.g. Ashleigh Wrensch: Groundflr → Org Admin, Acme Corp → Workspace Admin); (3) shrank the `.sa-session-note` ("· N sessions") to 12px / weight 400 to match the organisation value text, kept on one line (nowrap); (4) brought back the **ID** column (monospace UUID, ellipsis + hover title for the full value) and added **User ID** to the peek window. The peek rail now collapses by default (`is-peek-hidden`) so the table runs full-width with the whole ID column visible while browsing, and only reserves its 325px when a row is peeked (where the full ID is shown).

- 2026-07-21 — System Admin · Users table: reverted the collapsing-rail approach — the `tv-peek-rail` is now **always shown**, and the table itself scrolls horizontally (`.sa-users-tablescroll` `overflow-x:auto`, `#sa-users-table min-width:1040px`) so the full ID column is reachable beside the rail. Removed the ID-cell ellipsis truncation (full UUID shows, td `nowrap`). Added `margin-top:22px` to the SA `users-body` so the table no longer nests tight under the search / Filter / Invite user action row.

- 2026-07-21 — System Admin · Users table — org/role interaction (chosen: click-to-expand). Collapsed the separate Organisation + Role columns back into a single **Organisation** column with a chevron toggle. Hovering an org name shows its role as a dark CSS tooltip (`.sa-org-name[data-role]:hover::after`). Clicking the Organisation cell (`data-sa-org-toggle`, stops propagation so it doesn't open the peek) expands an inline `tr.sa-user-detail` row showing the full role breakdown per organisation — the org-level role plus each workspace role within that org (e.g. Ashleigh: Groundflr → Org Admin, L&D Hub → Workspace Admin, Compliance → Workspace Admin; Acme Corp → Workspace Admin, Onboarding → Workspace Admin). Enriched `SA_USERS` orgs with a `workspaces:[{name,role}]` list; added `orgCell` / `detailHTML` / `toggleDetail`; `applyFilters` collapses open detail rows for consistency. Table `min-width` lowered to 940px now that Role is gone (still scrolls beside the always-on rail).

- 2026-07-21 — Reworked the active workspace-role dashboards (Workspace admin, Member, and the assigned Moderator/Viewer — the default shell; the `-new`/first-run and org-active screens are untouched) into a two-column grammar. Left-aligned the `.ws-header` ("Workspace summary / David Guetta") and removed the 7/5/6 `.ws-pill` count. New `.dash-layout` = `.dash-main` (fluid) + `.dash-rail` (325px sticky). Main column order: condensed **asset chips** (`#asset-chips`, small pills replacing the old 4-tile `.assets-card`; hidden for assignment-scoped roles via `renderDash`) → **Pick up where you left off** → **At a glance** → the **role alert** `#action-card` (now expanded by default — the "bulk", per role: feedback ready / submissions to grade) → **Recent activity** (moved to the bottom). Quick actions now live in the rail as a **`tv-quick-actions`** panel (`#dash-quick`, full-width cards + ↗ arrows), fed the same `ROLE_ACTIONS` and routed via the shared `routeQuickAction`; the old horizontal `tv-priority-actions` strip is kept only for the unassigned moderator/viewer takeover (CSS-gated to `.is-takeover`). The rail hides itself (main goes full-width) when the role has no quick actions (plain dashboard). Verified across admin/member/moderator/viewer with real renders — no JS errors.

- 2026-07-22 — System Admin **Summary (v1)** live-presence row: replaced the two large "Users online / Candidates online" presence cards with a compact, left-aligned `.sa-presence-strip` — a green `.sa-online-dot` + "LIVE" label, then a bordered pill each ("47 users online", "128 candidates online") with the session count ("63 active sessions" / "141 active sessions") in smaller text under each pill. The four metric cards (Organisations / Super users / Workspaces / Members) and Recent super-user activity are unchanged. Only the v1 summary screen (`system-admin-summary`) was touched — the v2 screen is untouched.

- 2026-07-22 — System Admin **Summary (v1)**: hid the `.sa-tabs` tab strip on the summary screen (navigation is the side menu; `style="display:none"` on that screen's `tv-tab-group` only — other SA screens keep their tabs). Moved the live-presence strip into the `tv-page-header` `actions` slot so the ● LIVE dot + "47 users online" / "128 candidates online" pills (with the session counts beneath) now sit right-aligned inline with the "System summary" heading. Zeroed `.sa-presence-strip` bottom margin since the header now owns the spacing.

- 2026-07-23 — System Admin **Summary (v1)** live-presence pills: brought the active-session count *inside* each pill (no longer floating beneath) as a divider-separated, muted `.sa-presence-sub`, and prepended a pulsing green dot (`.sa-live-dot`, reusing the `sa-pulse` keyframe) to the left of the count inside each pill. Pills now read: ● 47 users online │ 63 active sessions / ● 128 candidates online │ 141 active sessions. Only the `system-admin-summary` screen was touched.

- 2026-07-23 — System Admin **Summary (v1)** live-presence pills: removed the "LIVE" label + its standalone pulsing dot. Stacked the two pills vertically (candidates online now sits under users online), right-aligned in the header actions slot, both fixed to the same 240px width with contents left-aligned inside each pill. Each pill keeps its inner pulsing green dot + inline session count. Only the `system-admin-summary` screen was touched.

- 2026-07-23 — System Admin **Summary (v1)** live-presence pills: dropped the fixed 240px width — switched `.sa-presence-strip` to `inline-grid` so both pills size to the widest content (long enough for their text) while staying equal width and right-aligned. Tightened the gap between the count and its label (`.n` margin-right 5px → 2px). Only the `system-admin-summary` screen was touched.

- 2026-07-23 — System Admin **Summary (v1)** live-presence: replaced the two hand-authored presence pills with the new **`tv-presence-pill`** component (`count`/`label`/`sessions`), keeping `.sa-presence-strip` as the right-aligned `inline-grid` wrapper. Removed the now-orphaned `.sa-presence-pill`/`.sa-presence-sub` inline CSS. Visual result is unchanged; the pill is now a single source of truth with a built-in empty state. Only the `system-admin-summary` screen was touched.

- 2026-08-10 — Design-readiness system: added `data-status` to all 13 screen sections (confirmed with Lara — dashboard `wip`; workspace-settings, system-admin-users, system-admin-summary, system-admin-organisations, system-admin-workspaces, organisation, organisation-user-profile `ready`; system-admin-summary-v2 `explore`; system-admin-simulations/-characters/-voices `wip`; user-profile `superseded` → `data-superseded-by="organisation-user-profile"`), added the `#prototype-manifest` JSON block after `<body>` (20 index entries seeded verbatim from the launchpad), and placed `<tv-status-ribbon>` — top-right status ribbon + devbar status dots (SA · Summary vs Summary v2 now visually distinct; superseded nav buttons struck through). `?chrome=0` or the devbar "Hide status" toggle hides all status chrome (sessionStorage). No visual change to any screen itself.
- 2026-08-13 — System summary (v2) rail rebuilt from Lara's Paper artboard "B5 · Queue-first split panels". Replaced the three switchable Live-now variants (dark monitor / light framed / compact list) with two components stacked in the rail — **`<tv-queue-panel>` above `<tv-live-panel>`**, queues now leading — and deleted the whole `#ss2LivePanels` block, its 64 lines of now-dead `.ss2-live*` / `.ss2-livelt*` / `.ss2-livec*` / `.ss2-q*` CSS, the variant-switcher JS, and the devbar "Live panel (v2)" control that drove it. Per B5: Priority is listed **above** Standard and its accent moved from `--cat-orange` to `--cat-teal`; presence figures dropped from 30px to the `.tv-h4` role (18px) so the queues own the rail; the live dot now sits on the queues header and the total session count moved into the Live-now header. The two waiting-swatch techniques in the artboard (`--cat-teal` at 22% vs solid `--primary-100`) were unified on accent-at-22% so each dot matches its own bar remainder. Row titles snapped from the artboard's 13px to `.tv-label-sm` (12px/600) — hierarchy now carried by weight, confirmed with Lara. Counts feed in as raw `inProgress`/`waiting`; totals and bar fills are derived. Metrics row, View-by filter, activity feed and Quick actions untouched; screen stays `data-status="explore"`.
- 2026-08-13 — System summary (v2) grading-queues rail switched from the Priority/Standard pair to **one queue per test**, using names and types from the test-list fixture in `prototypes/test-journey/index.html` (Musango UX Design Fundamentals · Debugging and Enhancing a Web Application · Fundamentals of Web Development · [DYL] Chat test test · Self-Evaluation of Web Development Skills · Designing a Responsive Web Application). The panel now runs `max="4" sort="busiest" scroll`, so the rail shows the four busiest queues in a height-capped scroll area with the remainder on a "2 more queues · 8 submissions →" overflow line. In-progress / waiting semantics unchanged. Live panel, metrics, View-by filter, activity feed and Quick actions untouched; screen stays `data-status="explore"`.
- 2026-08-13 — System summary (v2) grading queues: the long-queue view now scrolls inside a `<tv-scroll-area>` with a persistent tokenised scrollbar rather than an overlay one that disappeared at rest, and each queue row gained a hover arrow (`arrow-up-right`) beside the test name. Wired both exits: `tv-queue-select` navigates to that test's summary in the test-journey prototype (`../test-journey/?screen=test-summary&test=<value>`) and `tv-queue-overflow` goes to the test list. Multi-coloured (per-test-type) queue accents were dropped — all bars share one accent, the icon carries the type.
